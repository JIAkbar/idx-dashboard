# Backlog Sweep Visual — Pasca-Migrasi Lantai Bursa

> ⚠️ **Antrean pindah ke `docs/antrean.md` (20 Agu 2026).** Baris yang masih
> terbuka dari berkas ini (dan yang ternyata sudah selesai saat dicoba ulang
> — #25, #33, #35, #19) sudah dipindah ke sana. **"Ada backlog?" dijawab dari
> `docs/antrean.md`**, bukan dari tabel di bawah — berkas ini tetap dibaca
> untuk detail akar masalah tiap temuan.

> Ditemukan user lewat sweep manual di browser (Claude in Chrome extension), 11-12 Agustus
> 2026, setelah 13 tugas migrasi + review akhir selesai (branch
> `claude/artifact-react-migration-c789ac`, HEAD `969e3ab`). **Backlog dulu — belum
> dikerjakan** sampai user bilang "kerjakan".

| # | Tugas | Perintah asal (kutipan) | Halaman | Komponen (`file:baris`) | Akar masalah | Status |
|---|---|---|---|---|---|---|
| 23 | Grid Indeks Dunia: ruang kosong di bawah Market Fundamental | "ada space kosong nih rapikan grid nya pakai 2 viewport kalau bisa desktop dan mobile" | `/` (Indeks Dunia) | `IndeksDunia.tsx` | **Selesai (12 Agustus 2026).** Panel Market Fundamental `flex:1` isi sisa tinggi kolom kanan. Verified live: kedua kolom grid2 sama persis 468px, nol ruang kosong | **Selesai** |
| 24 | Tema default kelihatan gelap, padahal kode default `'light'` | "tema default nya gelap" | Semua halaman | `ThemeContext.tsx:20` | **Selesai diverifikasi (12 Agustus 2026, Chrome extension, fresh navigate ke localhost:5173)**: `ThemeContext.tsx` sengaja gak persist ke localStorage (komentar baris 14-17: port dari `index_live.html`, theme SELALU reset ke `'light'` tiap reload — bukan bug, perilaku asli dipertahankan). Screenshot live: dasbor terang, tombol rail "Gelap" nunjuk mode terang aktif. Bukan bug kode — kemungkinan toggle nyangkut sesi browsing user sebelumnya | **Selesai — bukan bug, gak perlu perubahan kode** |
| 25 | Dropdown `.dd-it`: latar putih nyala di tema gelap | "menu dropdown nya gak sesuai artifact perlu di sweep ke semua tombol dropdown harus juga support warna light dan dark mode" | Semua dropdown (`.dd`/`.dd-menu`/`.dd-it`) — Kalender bulan dipakai 4 halaman, kemungkinan tempat lain juga | `lantai.css:493` (`.lantai button{font:inherit;color:inherit}`) lupa `background`; `lantai.css:598` (`.dd-it`) gak set background sendiri | `<button>` browser default (putih/abu) gak ketimpa — item dropdown selain yang terpilih render kotak terang di tengah panel gelap | Antre — akar sudah ketemu, fix satu titik (`.lantai button` tambah `background:none;border:none`) otomatis benerin semua turunan |
| 26 | Kalender: gak ada navigasi geser per-hari | "ada tombol geser tanggal tidak hanya bulan saja kesulitan gak simple gitu" | Kalender (4 halaman pemakai) | `Kalender.tsx:63-69,234-258` | **Selesai (12 Agustus 2026).** Tombol prev/next (SVG chevron, pola sama dropdown bulan) di `.hari-nav`, geser index `tanggalTersedia` (otomatis skip weekend/libur karena cuma berisi hari yang ada data). Disable di batas data. Verified laptop+mobile | **Selesai** |
| 27 | Board Indeks Dunia: grafik mini IHSG (board-side) gak pernah dipasang | "di header ini mana grafik realtime seperti di artifact ? rapikan juga" | `/` (Indeks Dunia) | `IndeksDunia.tsx` (komponen baru `IhsgYtdChart`) | **Selesai (12 Agustus 2026).** Chart.js line chart IHSG YTD dari `tanggalTersedia` (data sudah ada, nol fetch tambahan), label "IHSG — Tahun Berjalan". Verified live: canvas render 516×120px nyata (bukan collapsed 300px default) | **Selesai** |
| 28 | Sweep umum teks & warna "AI slop" | "sweep teks dan warna AI Slop jelek tau" | Semua halaman | — | **Ditutup via verifikasi visual final (12 Agustus 2026, Chrome extension, screenshot nyata 1536px + JS-check 375px)**: cek langsung 7 halaman (Indeks Dunia, Stock Detail, Peta Investor, Bulletin, Feedback, Login modal, Kalkulator). Gak ketemu pola AI-slop klasik — nol gradient ungu, nol emoji (udah #31), palet amber/gelap konsisten, copy Feedback spesifik/jujur ("dikembangkan satu orang di luar jam kerja"), bukan template generik. Sumber "AI slop" konkret udah ketangani lewat item #21/#25/#31/#34/#42 spesifik | **Ditutup — gak ketemu temuan baru di luar yang udah difix item lain** |
| 29 | Kalender full-width di desktop — sel kegedean, gak efisien | "kalau segede gaban gini gak efektif di desktop... masak hanya lihat tanggal besar sekali" (dikonfirmasi ulang di /broker: "ketemu lagi kalender segede ini gilaaak") | `/stocks`, `/broker` (dan kemungkinan `/sector` — belum dicek) | `Kalender.tsx` (dipakai standalone, gak ikut pola grid2 seperti Indeks Dunia Task 7d) | 7 kolom grid stretch penuh lebar viewport tanpa cap — beda dari Indeks Dunia yang sudah dipasangkan Net Foreign/Market Fundamental (Task 7d) | Antre — dikonfirmasi 2 halaman |
| 30 | Sesi bursa: detik hilang + label Pre/Post-Closing gak akurat | "munculkan detik nya juga, lalu berikan batas-batas waktu ada pre opening, ada istirahat dll" | Kalender (4 halaman) | `Kalender.tsx:49-61,289-297` | **Selesai (12 Agustus 2026).** Label "P" dipecah jadi Pre-Closing (15:50-16:00)/Post-Closing (16:01-16:15), Post-Closing sekarang berlaku SEMUA hari termasuk Jumat (bug lama: Jumat berhenti 16:00). Jam digital HH:MM:SS live dikembalikan + status sesi aktif dgn warna. 11 test case (WIB boundary, Jumat regression, edge sesi) 35/35 pass | **Selesai** |

| 31 | Emoji tersebar di ~13 berkas — ganti ke ikon SVG React | "kok bisa, icon-icon nya AI banget, ganti ke react" | 23 berkas (lebih banyak dari catatan awal, di-grep ulang fresh) | `IkonMenu.tsx` (+25 ikon `IKON_*` baru) | **Selesai (12 Agustus 2026).** ~55 titik emoji diganti ikon SVG React (stroke `currentColor`, ikut tema). Glyph tipografis polos (✓✕←→↓↩⊞) SENGAJA gak diganti — itu bukan color emoji, udah ikut `currentColor` sejak awal, gak kena masalah "beda tampilan tiap OS" yang jadi alasan awal. Grep akhir: 0 emoji tersisa di UI chrome | **Selesai** |

| 32 | Chart: simbol Co-Branding salah + tombol vs artifact + bug layar penuh | "tombol nya juga nih ganti sesuai artifact, lalu untuk pilihan pakai web search juga... backtest ada bug juga di tombol layar penuh sweep" | `/chart` | `ChartIndeks.tsx` | **Selesai (12 Agustus 2026).** Verifikasi 20 simbol via WebFetch tradingview.com langsung: `IDX:BISNIS27`→`IDX:BISNIS_27`, `IDX:SRIKEHATI`→`IDX:SRI_KEHATI` diperbaiki, 18 lain sudah benar. Pefindo25 TIDAK ditambah (3 URL TradingView 404, gak terverifikasi). Tombol vs artifact: dibandingkan `index_live.html` — sudah identik (cuma beda lokalisasi ID/EN, disengaja). Fullscreen: tombol toggle jadi "Keluar Layar Penuh" + ikon X, sinkron via `fullscreenchange` listener (benar juga kalau keluar via Esc). `?sym=` siap buat task #41 | **Selesai** |
| 33 | Sektor: heatmap tile 11 sektor gak pernah diport | "perlu ditambah heatmap di menu sektor" | `/sector` | `SektorIndeks.tsx` (belum ada), `lantai.css` (`.tiles`/`.tile`, sudah ada nganggur sejak Task 1) | Artifact `view-sek` baris 584 (`#sekTiles`) adalah blok PERTAMA halaman — grid tile warna per sektor, gak pernah diimplementasi. Review akhir salah tebak kelas ini "diganti BatangPeringkat" — nyatanya gak pernah dipakai sama sekali | Antre — data sudah ada (`hari.sectors`), tinggal render ulang jadi grid |

| 34 | **[KRUSIAL]** Stock Detail: grid fixed bukan fluid + header dorong kanan jauh | "di stock detail ini SANGAT KRUSIAL... banyak space kosong tabel gak rapi... kenapa baca harus kanan sekali" | `/stock-detail` | `lantai.css` (`.fd-layout`, `.fd-board .board-main/.board-side`) | **Selesai (12 Agustus 2026).** `.fd-layout` sekarang `repeat(auto-fit,minmax(300px,1fr))` pola sama `.grid3`. `.board-main`/`.board-side` di-scope lewat kelas baru `.fd-board` (bukan `:has()` sibling rapuh yang sempat numpang ke Indeks Dunia — ditemukan & diperbaiki sendiri oleh agen sebelum lapor) `flex:0 1 auto` keduanya. Verified live: grid 3×381.6px sama rata, nama↔harga 26px (dulu jauh) | **Selesai** |

| 35 | **[KRUSIAL]** Peta Investor: tooltip nyantol stale + "klik detail" gak nyambung | "tooltips nya lama gak ketutup-ketutup... klik untuk detail lengkap juga gak berfungsi, gak teliti kerjaanmu" | `/peta-investor` | `GrafikJaringan.tsx:51-52`, `graphRender.ts:150` | (1) mode fokus render dengan `tooltip:null`, elemen `.pi-tooltip` persisten gak pernah di-`display:none` saat ganti mode → nyantol nunjukin node lama. (2) teks "klik untuk detail lengkap" HTML statis, gak pernah ada `onClick` — deteksi klik beneran cuma di node, bukan tooltip | Antre — akar presisi, 2 bug kepisah, prioritas tinggi |
| 36 | Kalkulator JIA: grid terlalu lebar di desktop | "rapikan sesuai rekomendasimu saja backtest di viewport desktop dan mobile" | `/kalkulator` | `.dasbor-main` (gak ada max-width), `.grid2.w-kiri` | Gak ketemu bug CSS spesifik (beda dari #34) — grid teknis benar isi 100% lebar. Rekomendasi: cap max-width halaman ~1400-1600px biar kartu gak dipaksa melar di monitor sangat lebar | Antre — rekomendasi, bukan bug pasti, perlu backtest 2 viewport begitu dieksekusi |
| 37 | Kritik & Saran: redesain full + menu Bulletin Arus Pasar (fitur baru) | "kritik dan saran masih AI Slop banget rapikan full pages, dan tambahkan menu bulletin... unduh dokumen analisa Arus Pasar dalam bentuk tabel" | `/feedback`, `/bulletin` (baru) | `Feedback.tsx` (ditulis ulang), `Bulletin.tsx` (baru), `arus-pasar/generate_index.py` (baru) | **Selesai (12 Agustus 2026).** Bulletin: generator manifest baru (`arus-pasar/keluaran/index.json`) dari edisi existing, halaman publik tabel Edisi/Tanggal/Emiten/Unduh, menu masuk Sidebar+MobileNav publik. Feedback: redesain penuh, form topik+pesan live-generate link WhatsApp. **TEMUAN PENTING dari agen — belum ditindaklanjuti**: repo ini SUDAH PUNYA sistem Arus Pasar kedua yang lebih maju (`app/src/arus-pasar/Terbitan.tsx` + Supabase `edisi` table via `supabaseEdisi.ts`, dipakai `/admin/edisi/:kode`), agen sengaja TIDAK pakai itu buat Bulletin publik karena gak bisa verifikasi RLS-nya publik-aman. Sekarang ada 2 sistem Arus Pasar paralel di repo — perlu keputusan user: konsolidasi ke Supabase (kalau RLS memang publik-aman) atau biarkan 2 jalur (file-based publik, Supabase admin) | **Selesai kode — ada follow-up arsitektur perlu keputusan user (lihat kolom akar masalah)** |
| 38 | Login jadi modal, desain ikut token Lantai | "halaman login ini cukup pakai modal saja tidak perlu pindah page, dan desain nya disamakan dengan artifact" | `/login` | `LoginModal.tsx` (baru), `DasborLayout.tsx` (state di-lift, dipakai Sidebar+MobileNav bareng) | **Selesai (12 Agustus 2026).** Modal reuse animasi `.dasbor-laci` (fade bg+slideUp, satu-satunya pola modal non-statik established), center + aktif semua lebar layar. Route `/login` lama dipertahankan sbg redirect kecil (sudah login→/admin, belum→/ dgn modal auto-buka via `location.state`). Verified 2 viewport+dark/light. **Gak keverifikasi:** tampilan `/admin` pasca-login beneran (butuh kredensial Supabase asli — agen sempat coba bypass auth buat QA, diblok classifier keamanan [benar], di-revert bersih) | **Selesai kode — user perlu login manual sekali buat cek visual final `/admin`** |

| 39 | Admin Arus Pasar: gaya lama flat, gabung 1 halaman, hapus sebutan Claude Code | "samakan seperti artifact... gak perlu ke sub page... semua yang bahas claude code github di hapus sudah aturan baku... jadikan kartu gaya react" | `/admin` | `AdminHome.tsx` (ditulis ulang, `Unggah.tsx` dihapus, lebur jadi 1 halaman) | **Selesai (12 Agustus 2026).** `/admin/upload` dihapus, gabung 1 route 5 section (header→form unggah→tabel→kotak masuk→rak terbitan). Semua primitif Lantai reuse (`.panel`/`.field`/`.inp`/`.btn-p`/`.tbl`/`.chip`/`.vcard`), nol kelas CSS baru. Grep "Claude Code" = 0 hasil seluruh app/src. `App.css` 448→20 baris (dead code CSS lama dibuang) | **Selesai — belum diverifikasi visual login-in beneran** (butuh kredensial Supabase asli, lihat catatan verifikasi task #38) |

| 40 | Admin: gak ada link balik ke dasbor + tombol "Masuk" gak sadar status login | "perlu ada halaman dashboard ke awal dong ada tombolnya, terus tombol masuk dibawah jika sudah login ganti ke menu admin" | `/admin`, rail publik | `AdminHome.tsx`, `Sidebar.tsx`, `MobileNav.tsx` | **Selesai (12 Agustus 2026).** AdminHome tambah "← Dasbor". Sidebar + MobileNav (bug sama ditemukan di 2 tempat, diperbaiki root-cause keduanya) pakai `useAuth()`: sudah login→link "Admin", belum→tombol buka modal | **Selesai** |

| 41 | Kode emiten di semua tabel jadi klik → buka chart TradingView | "setiap kode emiten ini di klik bisa langsung open chart di trading view" | Lintas halaman | `TopStocks.tsx`, `TopBroker.tsx`, `broker-summary/Nego.tsx` | **Selesai (12 Agustus 2026).** `<Link to="/chart?sym=KODE">` ganti `<span className="tick">`, style gak berubah (CSS `a.tick{color:inherit;text-decoration:none}`). Kode broker (`.bchip`) SENGAJA gak diubah (bukan ticker saham). **Peta Investor di-skip dengan alasan eksplisit**: dokumentasi kode itu sendiri (`PetaInvestor.tsx:34`) bilang interaksi sengaja gak pindah halaman + node SVG cuma 1 `onClick` yang baru dapat fix krusial tooltip — pecah jadi 2 target klik berisiko rusak fix itu. Verified via DOM/JS click (BBCA→`/chart?sym=BBCA`→iframe load `IDX:BBCA`) | **Selesai** |

| 42 | Font dark mode buram + StockAutocomplete dropdown lama | "font yang dark mode di perjelas tidak buram... menu dropdown nya juga lebih rapi seperti react" | Semua halaman (font), `/stock-detail` (dropdown) | `index.css`, `StockAutocomplete.tsx` | **Selesai (12 Agustus 2026).** `-webkit-font-smoothing`/`-moz-osx-font-smoothing` dihapus dari `index.css`. `.fd-ac-*` bespoke diganti primitif `.dd/.dd-menu/.dd-it`, grep akhir 0 hasil `fd-ac-` | **Selesai** |

## Item lama (sebelum sweep sesi ini), masih terbuka

| # | Tugas | Ringkas |
|---|---|---|
| 19 | Expose `financial_currency` + caveat badge di Stock Detail | Backend tahu mata uang laporan, JSON belum menyertakan — Stock Detail belum bisa kasih caveat otomatis |
| 20 | `hist_fcf`/`hist_bv` masih mentah USD | Kelas bug sama dengan fix Critical final (99 emiten), field ini di luar cakupan fix itu |
| 21 | Konsolidasi debt: 2 sistem tab, ~34 CSS mati, scaffold duplikat 4× | **Selesai (12 Agustus 2026).** `KolomKuartalan.tsx` pakai `.tabs`/`.tab` (+ `role="tablist"`/`aria-selected`), `.fd-tabs`/`.fd-tab-btn` dihapus dari `lantai.css`, grep akhir 0 hasil |
| 22 | `Kalender.tsx` `todayIso` pakai `toISOString()` | **Selesai (12 Agustus 2026).** `todayIsoJakarta()` (`Kalender.tsx:45-47`) pakai `Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Jakarta'})`, satu titik sumber dipakai 4 halaman. Test WIB-boundary lulus |

## Cara lanjut

Semua item di atas **cuma dicatat, belum dikerjakan**. Kalau mau eksekusi:
- Satu per satu: sebut nomornya ("kerjakan #25")
- Sekaligus per kategori: sebut kategorinya ("kerjakan semua yang CSS/dropdown")
- Semua: "kerjakan semua backlog sweep ini"
