# Papan Pekerjaan

> **Papan pekerjaan yang diwajibkan `kemampuan-workflow.md` §174.**
> Sebutannya tetap "Papan Pekerjaan" walau produknya juga bernama PAPAN —
> usulan mengganti nama jadi "Lembar Kerja" ditolak Johan 17 Agu 2026.
>
> **Bukan papan progress.** Papan progress melapor apa yang sudah jalan; papan
> ini mencatat perintahnya, sebelum-sesudahnya, alasannya, dan buktinya —
> sehingga bisa dipakai MENOLAK perubahan sebelum dikerjakan.
>
> **Bentuk baku sepuluh kolom** untuk baris BARU mulai 17 Agu 2026:
>
> `# · Tugas · Asal perintah · Halaman · Komponen (file:baris) · Sebelumnya ·
> Jadi · Alasan · Status & bukti · Changelog`
>
> Baris sesi 16 Agu di bawah ditulis sebelum konvensi ini diadopsi di sini,
> jadi bentuknya masih empat kolom (permintaan · sebelum · sesudah · commit).
> **Sengaja tidak ditulis ulang** — memundurkan pekerjaan yang sudah selesai
> untuk memenuhi format hanya memindahkan waktu dari pekerjaan berikutnya, dan
> isinya toh sudah menjawab pertanyaan yang sama. Yang mengikat mulai sekarang
> adalah baris baru.

---

## Sesi 17 Agustus 2026 — bentuk baku sepuluh kolom (§174)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 37 | Betulkan setelan lapis AI yang salah isi | "jadi TANYA_AI_AKTIF, TANYA_AI_BATAS_IP, dan TANYA_AI_MODEL jadi true ya" | — (Edge Function) | `supabase/functions/tanya-ai/index.ts:26` | `Number("true")` jadi NaN dan `n <= NaN` selalu false — tiap pertanyaan dianggap lewat batas, "Batas NaN pertanyaan" bocor ke layar | `angkaSetelan()` mengabaikan isi tak masuk akal, jatuh ke bawaan | Setelan opsional yang salah isi tak boleh melumpuhkan lapis; sakelar hidup/mati tetap mutlak | Selesai — diuji langsung ke fungsinya | Fixed |
| 38 | Rantai model + matikan penalaran | (lanjutan #37) | — | `supabase/functions/tanya-ai/index.ts:44` | Satu nama model: `gemini-2.0-flash` ditarik Google (404), `gemini-flash-latest` kena 503 | Rantai 4 model + `thinkingBudget: 0`, 700 token | Nama model yang dihafal justru sumber pemadaman; jatah keluaran habis untuk menalar jadi jawaban terpotong | Selesai — `0b67b2a9`, jawaban utuh dari `gemini-flash-latest` | Fixed |
| 39 | Unggahan kontributor selalu ditolak | "ada bug nih terkait logika, akun warda upload tapi data tidak masuk" | `/admin` tab Unggah | policy `unggah_screenshots` + `emiten_sudah_disetor()` | `ada_alasan` menuntut baris setoran ADA, `NOT emiten_sudah_disetor` menuntut TIDAK ada — ditolak oleh barisnya sendiri, selalu | `emiten_sudah_disetor` mengecualikan baris milik sendiri | Maksud aturannya "sudah disetor AKUN LAIN", dan itu sudah tertulis di pesan galatnya sejak awal | Selesai — migrasi `emiten_sudah_disetor_kecualikan_diri`; diuji dua arah (AADI lolos, PADI/ESSA tetap terkunci) | Fixed |
| 40 | Galat unggah bawa keterangan server | (temuan saat #39) | `/admin` tab Unggah | `UnggahHarian.tsx:153`, `supabaseEdisi.ts:99` | Empat kemungkinan disebut, keterangan server dibuang — #162 tak pernah terlacak | Keterangan server ikut + penanda tahap `[baris setoran]`/`[unggah berkas]` | Menerjemahkan galat menolong; MENGHAPUS galat aslinya membutakan yang memperbaikinya | Selesai — `71733906`; tanpa ini #39 tak akan ketemu | Fixed |
| 41 | Rem borongan setoran | "apakah jika nanti dia upload tanggal 10, 11, 12, 13, 14 otomatis dia bisa cepat naik level?" | `/admin` tab Unggah | `tanggal_dalam_jendela()`, `setoran_hari_ini_saya()` | Tak ada batas mundur; Perak memborong 145 hari bursa = 435 setoran, melampaui syarat Diamond | Jendela 5 hari kerja + laju 2x kuota per hari kalender | Kuota berlaku per TANGGAL bukan per hari nyata — selisih itu yang dipakai memborong | Selesai — migrasi `rem_borongan_setoran`; diuji (14 Agu lolos, 7 Agu lolos, 5 Jan ditolak) | Added |
| 42 | Bedah boleh lintas tanggal | "upload per tanggal per emiten itu bisa misal 5 langsung yakan? karena ini single bukan data daily" | `/admin` tab Bedah | `hitung_bedah_emiten_tanggal()`, `bedah_hari_ini_saya()` | `hitung_bedah_hari < 1` lintas emiten — berkas KEDUA selalu ditolak, tanggal kedua ikut terkunci | 2 berkas per emiten per tanggal, lintas tanggal bebas, laju 50/hari | Bedah itu studi satu emiten lintas waktu, bukan potret satu hari bursa | Selesai — migrasi `bedah_boleh_rentang_tanggal` + `bedah_laju_harian_50` | Fixed |
| 43 | Tab Bedah setara tab Unggah | "harusnya sama sih dengan fitur unggah di kasih fungsi hapus dan cara screenshot broker summary" | `/admin` tab Bedah | `BedahUnggah.tsx`, `PanduanScreenshot.tsx` | Tak ada hapus, tak ada panduan, arsip cuma menyimpan tanggal terakhir | Hapus per berkas, panduan dipakai ulang (prop `bedah`), arsip per tanggal, kunci emiten + Tambah Emiten | Dua tab yang sifatnya sama tak boleh beda perlakuan | Selesai — `94133bdc` + `b3520ded`; hapus diuji live (berkas 2 jadi 1) | Added |
| 44 | Reset akurasi kontributor | "saya sebagai superadmin bisa reset akurasi itu penting, karena pendidikan tidak harus selalu punishment" | `/admin` tab Akun | `profil.akurasi_sejak`, `AkunAdmin.tsx`, `jenjang.ts:34` | Akurasi dihitung dari SELURUH riwayat; satu periode buruk menempel selamanya | Titik mulai `akurasi_sejak` — riwayat tak dihapus, jumlah disetujui tak berubah, bisa dibatalkan | Menghapus baris memalsukan sejarah; menggeser jendela hitung tidak | Selesai — `6e27a167`, 266 tes, diuji 2 akun dua arah | Added |
| 45 | Kata tunggal dikenali Tanya PAPAN | "perlu kembangin rule engine yang lebih luas lagi supaya dengan 1 kata saja sudah paham" | Semua halaman (panel) | `pengetahuan.ts:41,70` | `kontributor`, `tier`, `level`, `model ai` semuanya gagal dijawab | Kata tunggal jadi kunci + entri `model-ai` baru | Entrinya sudah ada, yang kurang cara menemukannya | Selesai — `94133bdc`, 265 tes; sisanya jadi #171 | Fixed |
| 46 | Definisi divergensi tiga lapis | "harus bisa tentukan chart itu membentuk pola bearish divergent atau bullish divergen, kolaborasi dengan indikator stochastic, mungkin volume" | — (spesifikasi) | `docs/rencana-berjalan.md` | #146 menggantung sejak 15 Agu; #130 terhalang tanpa definisi | Harga + Stochastic + Volume sebagai PENGESAH, berikut derajat keyakinan | Lapis volume sebagai pengesah itu yang membedakan dari indikator divergensi kebanyakan | Selesai — `ad6f6c79`; #145 dilewati atas keputusan Johan | — |
| 47 | Bedah ASK SPLE | "kalau mau coba-coba bisa tuh di sple-mf... pakai devtools chrome lebih leluasa" | (situs pihak ketiga) | `docs/riset/sple/ask-sple.md` | Kedalaman AI mereka belum diketahui sebabnya | Prompt + 29 ruas konteks terdokumentasi; API terbukti Anthropic dari `stop: end_turn` | Gerbangnya kolom sandi — dibedah dari sisi klien, kolom sandi tak disentuh | Selesai — `06a3efd6`, `f9c13f71`, `898bbf44` | — |
| 48 | Urutan kerja ringan ke mahal | "buatlah ceklist tabel workflow dari yang paling ringan sampai yang paling mahal" | — | `docs/ceklist-backlog.md` | Antrean dikelompokkan per tema, tanpa urutan eksekusi | 21 baris berurut + kolom bergantung/membuka | Urutan ditentukan oleh apa yang DIBUKA, bukan besar-kecilnya | Selesai — `93a7d835` | — |
| 49 | Adopsi §174 + parkir #167/#129 | "gunakan selalu Papan Pekerjaan (bukan papan progress)" · "tetap jadikan backlog sampai saya panggil kmu lagi" | — | `CLAUDE.md`, berkas ini, `ceklist-backlog.md` | Sesi ini tak memakai papan §174; #167/#129 bisa terangkat sendiri | Papan wajib tiap balasan; #167/#129 ditandai diparkir | Yang diparkir hanya boleh diangkat kalau dipanggil — menawarkan berulang mengabaikan keputusan | Selesai — commit ini | — |
| 50 | Tombol buka arsip Bedah tak ketemu | "dimana tombol delete nya ya ?" · "yang gak beres itu tombol nya terutupi bro" | `/admin` tab Bedah | `BedahUnggah.tsx:379`, `AdminShared.css:222` | Tombol buka memakai ikon CENTANG dengan label ber-`opacity:0` sampai disentuh kursor; hapus cuma ada di dalam baris yang terbuka; tabel meluber 87px keluar panel di 412px | Tombol `dd-btn` berlabel tetap "Buka N berkas" + panah berputar; tabel dibungkus `.af-gulir` | Centang terbaca "sudah selesai", bukan "klik untuk membuka" — dan di telepon yang tak punya hover, label itu tak pernah muncul sama sekali | Selesai — diperiksa 1536×960 dan 412×915: label opacity 1, `elementFromPoint` mengembalikan tombol itu sendiri, tabel menggulung di dalam panel | Fixed |
| 51 | Kolom "Tanggal" arsip Bedah menampilkan angka | (temuan saat #50) | `/admin` tab Bedah | `BedahUnggah.tsx:380` | Kepala kolom "Tanggal", selnya `{tanggalList.length}` — menampilkan "1" di tempat yang menjanjikan tanggal | Kepala jadi "Rentang tanggal", sel jadi `3 Agu – 14 Agu (10)` | Kepala kolom yang berbohong sekelas dengan angka yang berbohong — pembaca tak punya cara tahu ia salah baca | Selesai — terbaca `3 Agu – 14 Agu (10)` di layar | Fixed |
| 52 | Tinggi chip rentang disamakan tab | "tinggi tombol ini disamakan dengan tinggi tombol bulan atau harian biar gak kecil kasian kalau manusia punya mata minus" | Seasonality (dua tab), Forum, Detail Saham | `lantai.css:821` | `.bchip-klik` mewarisi padding `1px 6px` milik lencana pasif — tinggi ±17px, jauh di bawah `.tab` 26px | `.bchip-klik` jadi `inline-flex`, tinggi 26px, padding `0 12px`, font 11px | Chip yang bisa diklik itu kendali, bukan lencana; dipasang di `-klik` supaya lencana pasif tetap ringkas | Selesai — terukur 26px di 11 chip, sama persis dengan `.tab` | Changed |
| 53 | Kotak keterangan tak lagi menggantung | "informasi ini lebih baik di rapikan lagi tidak mengambang disitu karena mobile view kan ?" | Seasonality (dua tab) | `lantai.css:2485`, `lantai.css` (akhir) | `.sea-kaki` dibatasi `max-width: 82ch` — di layar lebar menggantung sendirian di kiri; `.panel-h` flex satu baris menggencet judul dan keterangan jadi dua kolom sempit di 412px | `.sea-kaki` selebar panel; `.panel-h` boleh `flex-wrap` di bawah 560px | Batas lebar baca 82ch benar untuk teks panjang, tapi keterangan pendek yang dipatok segitu terbaca sebagai sisa yang lupa dirapikan | Selesai — 1432px di laptop, 396px di telepon tanpa luber horizontal | Changed |
| 54 | Bedah dan Broker Summary tak terbedakan | "desain nya sama antara bedah dan broker summary dimana gak ada badge yang membedakan" | `/admin` tab Kurasi | `KurasiSetoran.tsx:304`, `KurasiSetoran.css:78` | Jenis cuma teks abu-abu 10,5px satu baris dengan nama kontributor | Lencana berwarna di baris ticker: BEDAH biru, BROKER SUMMARY netral, CHART ungu | Dua produk berbeda (Bedah jadi PDF studi satu emiten, Broker Summary jadi edisi harian) tak boleh terlihat identik | Selesai — terbaca di 1536×960 dan 412×915, tinggi kepala kartu seragam 23px | Added |
| 55 | Tombol kartu kurasi naik-turun | "tombol-tombol itu naik turun tangga perlu di rapikan" | `/admin` tab Kurasi | `KurasiSetoran.css:78` | Tinggi blok info mengikuti panjang alasan tiap setoran; tiga kartu bersebelahan menaruh tombol di tiga ketinggian berbeda | `.ks-info { flex: 1 }` — tombol menempel ke dasar kartu | Kartu sudah sama tinggi karena grid `align-items:stretch`; yang belum ikut cuma isinya | Selesai — terukur tiga kartu `aksiAtas` sama persis (698px) | Fixed |
| 56 | Toast "Dimasukkan ke edisi" menyesatkan | "toast yang membingungkan kalau saya backtest artinya setiap yang di setujui itu masuk edisi buletin ya ?" | `/admin` tab Kurasi | `KurasiSetoran.tsx:159`, `:521` | "Dimasukkan ke edisi." terbaca seolah PDF sudah berubah; padahal `dimuat` cuma penanda dan perakitan manual | "Ditandai masuk edisi — berlaku saat edisi dirakit ulang" + satu butir aturan baru yang menyebut `build.py` manual | Kalimat yang menyiratkan penerbitan otomatis pada sistem yang manual adalah janji yang tak ditepati sistemnya sendiri | Selesai — butir aturan terbaca di panel Aturan Kurasi | Changed |
| 57 | Salinan daftar edisi salah polaritas | (temuan saat #56) | `/admin` tab Kurasi | `KurasiSetoran.tsx:197`, `arus-pasar/build.py:837` | Tombol menyalin daftar yang MASUK edisi (`dimuat`), sedangkan `build.py --kecuali=` menerima daftar yang DIKELUARKAN — menempelkannya membuang persis yang harus dimuat, dan kalau semua dimuat `build.py` berhenti "Semua emiten dikeluarkan" | Salinan memuat dua daftar + argumen `--kecuali=` siap tempel yang berisi yang DIKELUARKAN | Perangkap senyap: hasilnya bukan galat, melainkan edisi yang isinya terbalik | Selesai — `tsc` bersih, teks salinan diperiksa | Fixed |
| 58 | Warna baris rentang Seasonality Harian | "berikan warna yang berbeda" (baris "BRMS · 2025-08-20 → 2026-08-14 · 241 hari bursa" satu warna abu-abu rata) | Seasonality tab HARIAN | `SeasonalityHarian.tsx:238`, `lantai.css` (dekat `.sea-hari-kepala`) | Tiga informasi (sumber, rentang tanggal, jumlah hari) dijejalkan satu `<span className="v-note">` satu warna — tak bisa dipindai cepat | Dipecah 3 `<span>`: sumber amber tebal mono, tanggal `--text2` mono, jumlah hari `--green` tebal mono; pemisah `·` dan "hari bursa" tetap `--text3` lewat warisan | Warna berbeda per jenis informasi memungkinkan mata memindai tanpa membaca kalimat penuh | Selesai — `e7d76f68`; warna terukur `getComputedStyle` di tema gelap & terang, keduanya kontras cukup | Fixed |
| 59 | Sudut chip sumber diseragamkan (K10) | "kok masih rounded ini ?" (chip BRMS pil 99px vs tombol IHSG kotak 4px bersebelahan) | Seasonality tab HARIAN + tab BULANAN (kelas sama) | `lantai.css:2399` `.sea-chip` | `border-radius: 99px`, padding `4px 10px` — tak seragam dengan `.bchip.bchip-klik` (4px, tinggi 26px) di sebelahnya | `.sea-chip` jadi `border-radius: 4px`, `height: 26px`, `padding: 0 12px` — sama persis dengan `.bchip-klik` | K10 (`docs/spek-kendali.md`): kendali yang bisa diklik pakai sudut 4px, pil 99px cuma untuk lencana status pasif (`.chip`, tak disentuh) | Selesai — `e7d76f68`; `getComputedStyle` kedua chip identik (`borderRadius:"4px"`, `height:"26px"`) di kedua tema | Fixed |
| 60 | Tooltip crosshair grafik Balapan | "apakah bisa di setiap hari nya di munculkan kenaikan dan penurunan saham nya ? ... ok Opsi A dulu saja, jadi ketika kursor menyentuh akan muncul setiap titik dimana kenaikannya berapa gitu ya ?" | Seasonality tab HARIAN | `SeasonalityHarian.tsx:379` (`Balapan`), `lantai.css` (dekat `.sb-*`) | Grafik balapan tak punya cara melihat angka harian sebenarnya — cuma garis kumulatif | Overlay Pointer Events (mouse+touch) menampilkan tanggal+hari, persen perubahan harga NYATA dari `tutup` mentah (hijau/merah), dan penanda garis hari mana yang disuapi tanggal itu | Opsi A (tooltip) dipilih karena biaya rendernya tetap satu overlay di rentang apa pun — batas "maksimal 1Y" yang dikhawatirkan jadi tak perlu untuk fitur ini | Selesai — `a00fd593`; `tsc`+`vitest` bersih, 3 tanggal diverifikasi lewat `pointermove` sintetis, satu angka (+1,24% @ 10 Feb 2026) dicocokkan manual dari `ihsg_harian.json`; sentuh diuji `pointerdown/move/cancel` di 412×915, `scrollWidth` tetap 412 | Added |
| 61 | Rel navigasi ditunda, jadi backlog | "untuk menu rail untuk sementara dibiarkan gini dulu kan bisa scrolling ya, jadi dipikirkan kemudian jadikan backlog" | semua halaman (rel kiri) | `.dasbor-rail-list` (`lantai.css`) | Dianggap bisa digulung sehingga penambahan ikon aman | Dicatat sebagai #174 dengan angka terukur; tak ada perubahan kode | **Relnya tidak bisa digulung** — `overflow-y: visible`, `scrollHeight` = `clientHeight` = 960. Sisa ruang 96px = 2 ikon; yang ke-18 meluber ke area kaki tanpa gulir dan tanpa galat | Dicatat — `docs/ceklist-backlog.md` §175; keputusan (gulir vs kelompokkan) menunggu Johan | — |
| 62 | Lapis AI dikunci untuk yang sudah masuk | "fitur-fitur baru tersebut ubah ke perlu login supaya non publik karena biaya mahal harus di usahakan dengan berkontribusi" | semua halaman (panel Tanya PAPAN) | `supabase/functions/tanya-ai/index.ts:134,256`, `TanyaPapan.tsx:145`, `tanyaAI.ts:80` | Panel AI terbuka untuk siapa pun; kuota cuma per IP, dan IP bisa diganti dengan pindah jaringan | Gerbang login di Edge Function (401 `perluLogin`) + kuota per **pengguna**; klien tak memanggil lapis AI sama sekali kalau belum masuk | Menyembunyikan tombol di klien tak menahan siapa pun — fungsinya bisa dipanggil langsung dengan curl, dan di situlah biaya Gemini terjadi. Jadi gerbangnya di server, klien cuma kenyamanan | Selesai — diuji di konteks tamu terpisah: `adaSesi:false`, jawaban mesin aturan tetap keluar plus keterangan sebab, tanpa panggilan AI. Pengguna masuk tetap normal. `tsc` bersih, 284 tes hijau | Changed |
| 63 | Grafik Emiten — chart PAPAN tahap 3 | "Tugas: Chart PAPAN tahap 3 — lilin + volume + zoom, satu emiten. Ini tahap 3 dari jalur chart yang sudah diputuskan Johan (opsi A, `lightweight-charts`)." + "fitur baru tidak boleh publik karena biayanya harus diusahakan dengan berkontribusi" | Rute baru `/grafik` | `GrafikEmiten.tsx`, `grafikEmiten.ts`, `GrafikEmiten.css`, `App.tsx`, `menu.ts`, `aksesHalaman.ts`, tabel `akses_halaman` | Chart PAPAN tahap 3 belum mulai (☐, `jejak-permintaan.md` referensi lama); `/chart` cuma widget TradingView, tak ada kanvas milik PAPAN sendiri | Halaman baru: lilin+volume dari `ohlc/<KODE>.json`, zoom/geser bawaan lightweight-charts, pemilih rentang 1/3/5 thn/Semua, cari emiten (pola SeasonalityHarian+kamusEmiten), dijaga login (`akses_halaman` kunci `grafik`), ikon rail GRF | Overlay khas PAPAN (tahap 6: pita musiman, akumulasi broker, Radar) cuma bisa dipasang di kanvas sendiri, bukan iframe TradingView | Selesai — `3d47eca5` (+ `fe5abf16` docs). `tsc` bersih, 291 tes (dari 284). Terukur devtools: 244 lilin BBCA cocok hitungan manual `BBCA.json`, tinggi kanvas 460px, zoom+geser dibuktikan lewat rentang waktu sebelum/sesudah, kedua tema terbaca, mobile tanpa luber (`scrollWidth` 412), guard tamu terbukti (kerangka terkunci, nol request `ohlc/BBCA.json`). Bug ditemukan+ditambal saat verifikasi: kanvas numpuk gara-gara `display:none` vs `autoSize`/`ResizeObserver` (`kemampuan-web-dev.md` §189) | Added |
| 64 | Panen laporan keuangan resmi IDX (XBRL) | "seharusnya panen dari XBRL resmi IDX yakan ?" · "kerjakan sampai selesai" | — (data) | `scripts/panen_keuangan_idx.py` (baru), `data-idx/json/keuangan_idx/` | Satu-satunya sumber laporan keuangan yfinance, 646 dari 959 emiten; 313 sisanya kosong | Sumber kedua dari `GetFinancialReport`, 774 emiten TW2 2026, skema identik + ruas `sumber` | Panen ulang yfinance untuk 313 emiten memberi `0 berhasil, 313 kosong, 0 gagal` — nol "gagal" membuktikan bukan rate limit, melainkan datanya memang tak ada di sana | Selesai — `1e4a40be`; gabungan 873/959, 227 emiten hanya ada di XBRL, **99 emiten justru hanya ada di yfinance** sehingga penggantian ditolak | Added |
| 65 | Label sumbu waktu grafik berbahasa Inggris | (temuan saat memeriksa hasil #63) | `/grafik` | `GrafikEmiten.tsx:69` | `Oct`, `Dec`, `May` — bawaan `lightweight-charts` | `Okt`, `Des`, `Mei`; Januari menampilkan tahun | Dua jalur harus disetel: `localization` mengurus tooltip & harga, label sumbu punya `tickMarkFormatter` sendiri — satu saja meninggalkan separuh antarmuka tetap Inggris | Selesai — `7e461c83`, 291 tes hijau, terbaca setelah muat ulang | Fixed |
| 66 | Hari tanpa perdagangan ikut digambar | "data kosong kok masih di gambar di canvas ?" | `/grafik` | `grafikEmiten.ts:44` | Baris bervolume 0 dengan buka=tinggi=rendah=tutup ikut jadi lilin — garis mendatar setipis benang + batang volume nol, dan tooltipnya menyebut hari yang tak punya transaksi | Baris seperti itu dilewati; BBCA 1 tahun 244 baris jadi **240 lilin** | Terukur pada 400 emiten: **398 punya baris begini, 28.225 baris total** — bukan kasus pinggiran. Kedua syarat diminta bersamaan supaya hari auto-reject (datar tapi volume besar) dan data volume rusak (bergerak tapi volume nol) tidak ikut terbuang | Selesai — 4 tanggal BBCA yang dibuang `2026-05-14/15/27/28`, persis tanggal yang disorot kursor Johan di tangkapan layarnya. 294 tes hijau (3 uji baru) | Fixed |
| 67 | Laporan keuangan IDX disambungkan ke Stock Detail | "katanya yahoo ada yang bolong bagusan dari IDX kok gak di sambungkan ?" | `/stock-detail` | `stockDetailData.ts`, `fundamentalGabungan.ts:192,219`, `PanelLaporanKeuangan.tsx` | Panel hanya membaca `keuangan/` (yfinance) yang berlubang; `keuangan_idx/` menganggur | Tiga sumber digabung per-ruas; kuartal IDX kumulatif dikonversi ke diskret; lencana asal per sel | Alasan menunda yang saya pakai (kumulatif vs diskret) ternyata cuma pengurangan, bukan jalan buntu — dan tahunan tak kena masalah itu sama sekali | Selesai — `bde4b97e`; ACRO yang tadinya kosong kini terisi penuh, TLKM tidak jadi dua kali lipat, total aset tidak ikut dikurangi. 310 tes | Added |
| 68 | Arsip berkas mentah untuk semua pemanen | "jangan asal maen buang data yang sudah di panen... simpan backup saja" · "tidak hanya tiap XLSX, tapi semua data yang berkaitan dengan Papan simpan di folder itu" | — (data) | `scripts/arsip_mentah.py` (baru) + 10 pemanen | Sepuluh pemanen membuang respons mentahnya setelah diparse | Semua mengarsipkan ke `_arsip-mentah/` lebih dulu, dan membacanya sebelum menembak jaringan | Menambah ruas jadi tak berbiaya jaringan. Yang mahal mengambilnya, bukan menyimpannya | Selesai — `770c48d7` + `1465c214`; **414 MB, 8 sumber**. `panen_sektor_idx` belum terbukti hidup (IDX 403) dan dilaporkan begitu | Added |
| 69 | Tanda PAPAN: besar di tengah → kecil di pojok | "logo ini di telakkan persis tradingview yaa, jelek terlalu besar juga" | `/grafik` | `GrafikEmiten.css:77` | 112px di tengah kanvas, menutupi lilin | 28px di pojok kiri bawah, opacity 0,3 → 0,85 saat kursor masuk | Yang keliru ukurannya, bukan posisinya — di 28px tak ada tabrakan dengan label sumbu maupun batang volume | Selesai — `b77bc035` | Fixed |
| 70 | Indikator jadi dropdown, pola Double Bottom, template | "indikator itu dijadikan dropdown... setiap indikator berikan fungsi bisa input sesuai kebutuhan... pola double bottom coba kerjakan... ada fungsi simpan nya juga, buat template juga" | `/grafik` | `GrafikEmiten.tsx`, `grafikEmiten.ts` | Indikator sebagai deretan tombol nyala/mati, satu instans per jenis, tanpa parameter, tanpa pola | Dropdown Indikator + dropdown Pola terpisah; indikator jadi daftar instans berparameter; Double Bottom ber-status; template di `localStorage` | Toleransi pola berbasis ATR, bukan persen — persen tetap memperlakukan saham 50 dan 50.000 rupiah dengan cara yang salah satunya pasti keliru | **Berjalan** — agen aktif; verifikasi layar terhalang sesi login kedaluwarsa | Added |
| 71 | Tombol Hari Ini menjelaskan kenapa mati | "soal tombol pun kmu juga lupa nih" | Kalender Bursa (strip) | `Kalender.tsx:444`, `lantai.css:1712` | Tombol dinonaktifkan karena tanggal aktif memang sudah hari bursa terkini, tapi judulnya tetap berbunyi "Lompat ke tanggal bursa terkini" dan opacity-nya .35 | Judul mengikuti sebab matinya; opacity .55; kursor `default`, bukan `not-allowed` | Tombol pudar yang menjanjikan sesuatu tanpa memberi tahu kenapa tak bisa ditekan terbaca sebagai kerusakan, bukan keadaan | Selesai — `12638508` | Fixed |
| 72 | Panah langkah 44px — komponen kanonis (K1) | "sekalian gelombang #170" (setelah "cek di artifact pernah kita diskusi soal komponen") | Kalender Bursa, DatePicker (semua halaman), Radar | `LangkahTanggal.tsx` (baru), `Kalender.tsx`, `DatePicker.tsx/.css`, `Radar.tsx` | Lima ukuran panah berbeda; `.dpk-nav` 24px dan `.dpk-step` 26×32px **di dalam komponen kanonisnya sendiri**; nol yang mencapai 44px padahal sel tanggal yang diapitnya sudah 44px | Satu komponen, dua ukuran visual, satu target sentuh: `penuh` 44×44 di sebelah kendali besar, `sebaris` 32×32 dengan area klik 44px lewat `::after` | K1 verbatim: *"ada box kalender besar tapi tombol kiri kanan nya kecil gak presisi"* — yang terbaca bukan cuma sulit ditekan, tapi tak seimbang | Selesai — `aa4bceeb`. Terukur devtools: panah 44×44 vs sel 46px (laptop 1536), strip mobile 412px tanpa luber | Fixed |
| 73 | Sebelas kelas tombol ikon jadi satu | (lanjutan #72, audit §3.1.4) | Unggah, Bedah, Radar, Panduan, Akun, Bulletin, Forum, Peta Investor | `TombolIkon.tsx` (baru) + 11 pemakaian | Sembilan ukuran berbeda 14–32px; dua di halaman yang SAMA tetap beda (`.blt-cari-x` 24px vs `.blt-modal-x` 32px). `.pi-search-x` sebuah `<span>` tanpa fokus/Enter/nama; `.pi-panel-close` merender `×` telanjang | Kotak visual 32px, area klik 44px lewat `::after`; pasangan bersebelahan dibungkus `.ti-grup` (gap 12px) | Area klik yang melar 6px ke tiap sisi akan saling tindih tanpa jarak — di pasangan ubah/hapus, klik yang meleset menghapus | Selesai — `5a362d4a`; `tsc` bersih, nol rujukan kelas mati tersisa | Fixed |
| 74 | Enam wadah kotak cari jadi satu `.af-cari` | (lanjutan #72, audit §3.1.3) | Bulletin, Seasonality ×2, Akun Admin, Peta Investor, Forum | `Bulletin.tsx`, `Seasonality*.tsx`, `AkunAdmin.tsx/.css`, `PetaInvestorSearch.tsx`, `Forum.tsx`, `lantai.css` | Lima wadah ditulis ulang; Forum tanpa ikon kaca pembesar sama sekali | Semua memakai `.af-cari`; kelas masing-masing tinggal menyimpan lebar/posisi | Setiap perubahan gaya kotak cari bersama tak pernah sampai ke lima halaman itu | Selesai — `c2cafbf1`; terukur di Peta Investor (wadah 300×34, ikon muncul) | Fixed |
| 75 | K10 chip seragam, `.bchip` kembali ke tugasnya, satu tombol layar penuh | K10: "kenapa BUMI itu malah rounded kan aneh jadi tidak seragam" | Seasonality Harian, Chart Indeks, Broker Summary, Peta Investor, Forum, Grafik Emiten | `TombolLayarPenuh.tsx` (baru), `SeasonalityHarian.tsx:168`, +8 berkas | Chip IHSG `.bchip`, chip emiten terpilih `.sea-chip` — dua bentuk bersebelahan untuk pilihan yang sama. `.bchip` (badge kepemilikan) dipakai jadi pemilih/tombol/link di 9+ titik, tiap kali ditambal `cursor:pointer` inline. Tombol Layar Penuh punya 3 implementasi | Keduanya `.chip-t`; pemilih lain ikut ke `.chip-t`, tombol aksi ke `.dd-btn`; satu `TombolLayarPenuh` | Satu kelas yang menanggung lebih dari satu tugas memaksa tiap pemakaian menambal bedanya sendiri | Selesai — `9a21058c`; terukur: IHSG dan emiten terpilih sama-sama 32px radius 4px | Fixed |
| 76 | K3 Export XLS pakai `Dropdown`, gaya inline jadi kelas | K3: "keliatan tombol export nya kaku sekali" | Peta Investor, Top Stocks, Top Broker, Bulletin, Bedah, Kalkulator | `Dropdown.tsx` (prop `ikon`/`rata`), `PetaInvestor.tsx:157`, +5 berkas | `.dd`/`.dd-btn`/`.dd-menu`/`.dd-it` ditulis ulang dengan tangan + posisi inline + `onBlur` bertimer; objek `thBtn` identik didefinisikan dua kali lalu dipakai 12 titik | `Dropdown.tsx` yang sudah ada; `.th-sort`, `.dd-btn-kecil`, `.btn-p-kecil`, `.dd-it:disabled`, `.blt-dl.on`, modifier `.merah` umum | Menu buatan tangan itu kehilangan Escape-menutup dan navigasi panah yang sudah dimiliki komponennya | Selesai — `57d5d8a2`; terukur: menu rata kanan, di dalam viewport, item nonaktif opacity .45 | Fixed |
| 77 | Satu kosakata waktu + `PemilihRentang` (K2) | K2: "tombol rentang itu ya jarak antar waktu dalam bentuk kalender" | Indeks Dunia, Broker Summary, Seasonality Harian, Grafik Emiten | `periode.ts` (`LABEL_RENTANG`), `PemilihRentang.tsx` (baru), +5 berkas | Tiga ejaan untuk hal yang sama: `1 Tahun` · `1T` · `1 thn`; `20 thn` vs `10T` untuk maksud identik | `LABEL_RENTANG` satu-satunya tempat kata itu dieja; kata penuh dipilih mengikuti `PRESET_RENTANG` yang memang sudah dipakai bersama tiga halaman | `1T` dibuang karena `T` bisa terbaca triliun di halaman yang di sebelahnya menampilkan nilai transaksi dalam triliun | Selesai — `ebaa3f95`; terukur: Indeks Dunia 5 chip muat satu baris di 412px, Broker Summary 6 chip membungkus tanpa luber | Added |
| 78 | K5 dua pemilih tanggal jadi satu bilah rentang | K5: "perlu kalender rentang data juga perlu di update" | Broker Summary, Seasonality Harian | `BrokerSummary.tsx:207`, `lantai.css` (`.bilah-rentang`) | Dua DatePicker kotak lepas; baris `flex:0 0 auto` sehingga di 412px ujung kanannya jatuh di luar viewport **tanpa gulir** — tanggal akhir tak bisa diubah dari telepon | Satu bingkai `.bilah-rentang` (pola Seasonality, kelasnya di-umum-kan); di bawah 560px membungkus jadi tiga baris | Nama kelas ber-awalan halaman (`.sea-*`) persis alasan kenapa pola disalin, bukan dipakai bersama | Selesai — `2749499a`; terukur: 412px tiga baris semua dalam viewport, laptop satu bilah tersambung | Fixed |
| 79 | Ruas fundamental kosong di Stock Detail (EPS dkk) diisi yang memang bisa diisi | "kirim agent untuk harvest data OHLCV 10 tahun terakhir dan Fundamental nya, bnyk di Stock detail masih kosong datanya EPS dan lain-lain" | Stock Detail (strip rasio, Valuasi, Per Saham, Solvabilitas, Efektivitas, Dividen) | `scripts/lengkapi_fundamental.py` (baru), `LencanaTurunan.tsx` (baru), `KolomValuasi.tsx:33,59,97,121`, `KolomKuartalan.tsx:176`, `StockDetail.tsx:226,231,237`, `stockDetailData.ts:200`, `.github/workflows/update-fundamental.yml:52` | `eps` 84% · `pe` 62% · `hist_eps` 67% · `f_score` 66% · `der` 79% · `roe` 88% — semuanya SALINAN LANGSUNG `info` yfinance (`trailingEps`/`trailingPE`/`debtToEquity`/`returnOnEquity`), jadi kosong begitu yfinance tak punya kuncinya, walau `ttm_net_income`+`shares`+`last_price` ada di berkas yang sama | `eps` 98% · `pe` 69% · `hist_eps` 97% · `f_score` 97% · `der` 93% · `roe` 98%; tiap angka turunan bertanda `asal_turunan` dan berlencana `≈` (dihitung) / `B` (laporan resmi bursa) | Lubangnya bukan "sumbernya tak punya" melainkan "tak pernah dihitung" — dan angka tanpa jejak asal tak boleh terlihat sama dengan angka sumber | Selesai — `py -3.14 scripts/lengkapi_fundamental.py --uji` LOLOS (9 penegasan, angka ACRO dicek tangan), `tsc` bersih, 392/392 vitest hijau, dua viewport (1536×960×1.25 · 412×915×2.625) di `/stock-detail` ACRO | Added |
| 80 | Riwayat OHLCV diperdalam 5 → 10 tahun | "kirim agent untuk harvest data OHLCV 10 tahun terakhir dan Fundamental nya, bnyk di Stock detail masih kosong datanya EPS dan lain-lain" | `/grafik`, semua yang membaca lilin harian | `scripts/panen_ohlc.py` (+`--lewati-cukup`/`--paksa`/`th_full`), `scripts/test_panen_ohlc_cukup.py` (baru), `data-idx/json/ohlc/` (962 berkas) | `--tahun` **default 5**, jadi 709 emiten mulai 2021; median 1.208 baris; `--penuh` menarik ulang SEMUA emiten tanpa cara melanjutkan padahal skrip berhenti setelah tiga penolakan Yahoo beruntun | 10 tahun (`--tahun 10`); median 2.256 baris; 472 emiten mulai 2016; panen bisa dilanjutkan lewat `th_full` | Patokan lanjut memakai **kedalaman yang diminta**, bukan tanggal `mulai`: emiten yang IPO 2024 tak akan pernah punya `mulai` sedalam target, jadi patokan tanggal menariknya ulang selamanya | Selesai — `ce0b03e`; tuntas sekali jalan (29 menit, tak sampai kena batas tolak). Terukur: BBCA/ASII/TLKM `mulai` 2016-08-10, **0 dari 962 berkas menyusut** (n lama vs baru dibandingkan per berkas lewat git), cermin 964/964, arsip mentah 107 MB. GOTOM tak ada di Yahoo — dan memang tak pernah punya berkas | Added |
| 81 | Kendali grafik masuk ke dalam kanvas + label pola jadi tooltip | "perbaiki chart grafik kita, jauh dari seperti trading view, indikator masih buat baris baru, harusnya di dalam chart saja termasuk setup nya, lalu Pola nya juga tembus harusnya dibuat tooltips saja, pola nya tembus" | `/grafik` | `GrafikEmiten.tsx:189,668,742,970`, `GrafikEmiten.css:17,110`, `DaftarInstans.tsx:123`, `grafikEmiten.ts:868` (`penandaDiSekitar`) | Tiap indikator/pola menumbuhkan satu baris `<li>` DI ATAS kanvas (`.grf-ind-daftar`), ditambah blok template dan daftar hasil pola — 4 indikator + 1 pola mendorong kanvas 579px ke bawah di laptop dan **979px di telepon** (kanvas sama sekali di luar layar). Label penanda pola ditulis di ruas `text` penanda, dan penanda yang berdempetan membuat "Leher 10.050", "Lembah 2 · batal", "Tembus leher · volume menguat" saling menimpa sampai tak satu pun terbaca | Legenda dalam-kanvas gaya TradingView: dropdown penambah + satu baris per instans (nama · nilai pada kursor · gir/mata/hapus), setelan parameter berupa panel melayang (lembar bawah di telepon); template & daftar hasil pola PINDAH ke bawah kanvas. Penanda pola tetap, teksnya pindah ke tooltip yang menyebut semua penanda ±1 lilin di sekitar kursor | Kendali yang menumbuhkan baris berarti makin serius halaman ini dipakai, makin sedikit grafik yang terlihat — kebalikan dari maksudnya. Tooltip juga satu-satunya bentuk yang bisa menyebut DUA penanda berdempetan tanpa salah satunya menang-menangan | Selesai — `tsc` bersih, 408/408 vitest hijau (5 uji baru `penandaDiSekitar`). Terukur laptop 1536×960×1.25: tinggi kanvas **460px pada 0, 1, dan 4 indikator** (`getBoundingClientRect().height`), jarak kanvas dari atas kartu tetap **66px** (sebelumnya 579px), panel setelan terbuka pun tetap 460px. Telepon 412×915×2.625: kanvas **129px** dari atas kartu (sebelumnya 979px), luber horizontal **0**, lembar bawah tak menabrak `.tp-tombol` (851 vs 857). Tooltip diuji: satu penanda "Double Bottom · Leher 7.925" (2026-02-05), dua penanda berdempetan 2026-07-02 + 2026-07-03 keduanya disebut, jalur ketuk (tanpa hover) juga | Changed |
| 82 | Pola Musiman hari-dalam-pekan di kanvas + empat indikator pustaka | "berikan pola di chart untuk bisa insert seasonality, misal hari senin kan tinggi si BBCA nah di chart nya muncul misal tampilkan selasa, berarti menunjuka ke candle selasa" · "gunakan sumber ini untuk diaplikasikan" (repo `lightweight-charts-indicators`, `oakscriptJS`) | `/grafik` | `grafikEmiten.ts` (`cariMusiman`, `plotPustaka`, SPEK stoch/stochrsi/wpr/vwap), `GrafikEmiten.tsx` (penanda + `RingkasanMusiman`), `DaftarInstans.tsx` (ruas ber-`pilihan`), `GrafikEmiten.css` | Musiman cuma ada di halaman `/seasonality` — tak ada hubungannya dengan lilin; Stochastic/StochRSI/W%R/VWAP tak ada sama sekali | Pola "Musiman" di dropdown `+ Pola`: pilih hari lewat chip, lilin hari itu ditandai kotak, tooltip membawa `n` + vonis uji permutasi; empat indikator baru dihitung pustaka MIT | Angkanya WAJIB dari `lib/seasonality.ts` yang sama dengan `/seasonality` — dua hitungan untuk hal yang sama membuat keduanya berhenti bisa dipercaya. Rentangnya mengikuti chip yang tergambar, bukan rentang tersembunyi. Enam indikator lama TIDAK ditukar ke pustaka (sudah ada ujinya); RSI diuji SILANG dengannya, selisih maks 2,8e-14 | Selesai — `tsc` bersih, 420 uji hijau (+12), dua viewport 1536×960×1,25 & 412×915×2,625: penanda 100% jatuh di hari yang dipilih (481 Senin / 490 Selasa / 47 Kamis, `getUTCDay` dicek satu per satu), tinggi kanvas tetap 460/340px, nol luber di 412px | Added |
| 83 | Katalog penuh 457 indikator pustaka, digerakkan registry | "katanya indikator nya banyak? sudah ada repo github untuk menunjang" | `/grafik` | `katalogIndikator.ts` (baru), `grafikEmiten.ts` (`spekJenis`, `garisPustaka`, jenis `p:<id>`), `GrafikEmiten.tsx` (`opsiIndikator`, pemuat katalog), `Dropdown.tsx` (`grup`), `lantai.css` (`.dd-grup`, lebar menu) | Menu indikator berisi 10 jenis yang ditulis tangan; 447 rumus lain di pustaka tak terjangkau | Menu berkelompok + berkotak-cari berisi 386 pilihan: 10 kurasi PAPAN di atas, sisanya katalog pustaka per kategori registry; pustakanya diunduh lewat impor DINAMIS saat menu disentuh | Daftar tangan pasti basi begitu pustakanya naik versi. Kategori, penempatan pane (`overlay`), dan spek parameter semuanya dibaca dari registry — tak ada taksonomi karangan yang harus dijaga sinkron. Entri tanpa deret keluaran (73, hampir semua pola lilin) DITOLAK: indikator yang menyala tapi tak menggambar lebih buruk daripada yang jujur tak ada | Selesai — `tsc` bersih, 429 uji hijau (+9 uji katalog), dua viewport: menu 386 baris muat di 412px (lebar 360px, elipsis + `title` untuk 11 nama panjang), SuperTrend menumpang panel harga & ADX dapat pane sendiri sesuai `overlay` registry, nol galat konsol. Bundel: potongan /grafik 399,4 → 226,5 kB (gzip 120,2 → 74,4 kB), katalog jadi potongan terpisah 1.037,7 kB (gzip 257,0 kB) yang cuma diunduh saat menu indikator disentuh | Added |


---

## Sesi 18 Agustus 2026 — Katalis masuk edisi Bedah

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 84 | Bagian **Katalis — Sebab Pergerakan** di edisi Bedah | "ya, mulai dari Bedah — katalis masuk ke edisi" | Bedah Arus Saham (`BA-*`), halaman baru sesudah Arus Broker | `arus-pasar/build_bedah.py` (`TINGKAT`, `kabar_emiten`, `katalis`, `pct_hari`, `setema`, `hal_katalis`, CSS `.kat-*`), `arus-pasar/test_katalis.py` (baru), `arus-pasar/bedah/*.json` (blok `katalis`), `arus-pasar/bedah/katalis-<TK>-<tgl>.json` (arsip baru) | Lima halaman berisi **apa** saja: PCD, teknikal, arus broker, skenario. Edisi bisa menulis "volume 4,9× rata-rata, tutup di ARA" — benar, terukur, dan tak menjawab satu pun pertanyaan yang membuat pembaca membuka terbitan; angka itu sudah ada di aplikasi sekuritasnya | Halaman Katalis: aksi korporasi & keterbukaan IDX + berita media dari `kabar.json`, kurasi analis hasil penelusuran (tiap baris berisi nama sumber, tanggal, dan tautan), serta tabel **Bergerak Bersamaan** yang menyebut dasar keterkaitan tiap baris (disebut bersama di katalis / satu subindustri IDX-IC). Tiga tingkat keyakinan — (a) RESMI hijau, (b) MEDIA kelabu, (c) DUGAAN kuning — dibedakan warna pita + lencana, bukan cuma kalimat | Sebab adalah ruas yang paling mudah dikarang dan paling mahal kalau ketahuan: sekali pembaca menangkap satu sebab karangan, seluruh terbitan kehilangan kepercayaan. Karena itu tak ada baris tanpa sumber, tak ada (c) yang naik jadi (a), dan "tak ditemukan katalis publik" ditulis apa adanya — bukan dibiarkan kosong dan bukan ditambal karangan | Selesai — `py -3.14 test_katalis.py` 3/3 lolos; lima PDF dirakit ulang (BUMI/ARCI/CUAN/DSSA 6 hal, INET 7 hal), pindaian `PdfReader().pages[i].extract_text()` **0 temuan** `{` / `var(--` / `px;` / `</style` di SEMUA halaman kelimanya. Temuan nyata: DOOH 14 Agu dua pengumuman resmi perubahan pengendali langsung dari `kabar.json`; INET 18 Agu +23,08% berdampingan DOOH +14,29%, WIFI +9,56%, IRSX +6,67% — persis empat nama yang lolos dari edisi sebelumnya. ARCI: sesama subindustri Emas tak ada yang bergerak ±3% → dinyatakan bukan gelombang sektoral. DSSA: "Tak Ditemukan Katalis Publik" | Added |
| 85 | Arsip katalis per edisi supaya bangun ulang tak kehilangan sumber | (bagian dari #84) | — | `arus-pasar/build_bedah.py` (`katalis()`), `arus-pasar/bedah/katalis-<TK>-<tanggal>.json` | `kabar.json` cuma menyimpan **7 hari**, sedangkan edisi dirakit ulang berkali-kali (delapan PDF dirakit ulang pagi ini) | Hasil panen disimpan per edisi dan dibaca lebih dulu; kunci dedup **tautan + judul + tanggal**, bukan tautan saja | Bangun ulang bulan depan akan menghasilkan halaman Katalis kosong tanpa satu pun galat — pola gagal senyap yang sudah mahal di proyek ini. Kunci ber-tautan saja meringkas belasan pengumuman IDX tanpa lampiran (URL generik yang sama) jadi satu baris, juga senyap | Selesai — empat berkas arsip terbentuk (`katalis-{BUMI,ARCI,DSSA,INET}-*.json`); uji `test_kunci_dedup_bukan_tautan_saja` menjaga kuncinya | Added |
| 86 | Halaman Katalis pecah dua saat isinya melebihi satu A4 | (bagian dari #84) | Bedah Arus Saham | `arus-pasar/build_bedah.py` (`tinggi_mm`, `hal_katalis` mengembalikan daftar halaman) | Uji pertama INET: lima kartu memotong blok "Batas Halaman Ini" dan separuh catatan tabel — halaman A4 tidak menggulung, isi berlebih hilang **tanpa galat** dan PDF-nya tetap jadi | Taksiran tinggi per kartu; di atas 110 mm halaman dipecah dua dan kartunya dibagi setengah-setengah menurut berat | Klipping senyap persis kelas kerusakan yang cuma ketahuan dengan membaca terbitannya — sama seperti kebocoran CSS pagi ini | Selesai — INET 7 halaman, teks blok penutup utuh di pindaian PDF, tangkapan layar dua halaman katalis diperiksa | Fixed |

---

## Sesi 18 Agustus 2026 (malam) — Tanya PAPAN dilatih & diukur

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 89 | Latih & ukur mesin aturan Tanya PAPAN pada seluruh rentang panjang masukan | "kirim agent untuk melatih AI rule engine kita, masih payah itu, coba kata per kata, 2 kata, 3 kata, banyak kata, saling berkata" | Panel Tanya PAPAN (semua halaman publik) | `app/src/lib/dasbor/teksTanya.ts` (baru), `tanyaPapan.ts` (`normalTanya`, `MINTA_REKOMENDASI`, `CABANG`, `saring`, susulan ber-`subjek`), `pengetahuan.ts`, `glosarium.ts`, `tanyaPapan.test.ts` (korpus 139 pertanyaan), `components/dasbor/TanyaPapan.tsx` (`subjekRef`) | Terukur lebih dulu, bukan ditebak: **88/112 benar, 21 mengaku tak tahu, 3 salah sasaran**. Salah ketik paling parah (8/15). Kata tunggal ambigu ("broker", "emiten") jatuh ke "belum bisa saya jawab". Susulan "berapa?" sesudah "harga BBCA" balik bertanya karena topik cuma menyimpan JENIS, bukan emitennya | **112/112 benar, 0 salah sasaran** di korpus setelan, dan **27/27** di set tahan-simpan yang tak dipakai menyetel. Toleransi salah ketik berambang panjang kata, padanan kata dipusatkan satu berkas, kata ambigu menawarkan cabang alih-alih menebak, susulan mengingat emitennya | Menjawab topik lain dengan percaya diri lebih merusak daripada mengaku tak tahu — pembaca tak punya cara tahu jawabannya keliru. Karena itu golongan "salah sasaran" diuji satu per satu, bukan sebagai jumlah agregat yang bisa dilonggarkan | Selesai — `npx vitest run` **617 hijau** (450 sebelum sesi ini; +139 korpus, +3 kebocoran, sisanya dari pekerjaan lain yang sedang berjalan), `tsc -b` bersih, `oxlint` 0 galat. Tiga salah sasaran nyata yang ditutup: "cara kontribusi" → *"Penyumbang terbesar ke IHSG: BYAN 39,11 poin"*; "…rekomendasi saham yang layak dibeli minggu ini" → angka IHSG sepekan; "…target harga setelah ARA tiga hari berturut-turut" → *"IHSG naik 29 hari beruntun"* | Fixed |
| 90 | Blok valuasi pasar mencocokkan `per` sebagai potongan kata | (temuan saat #89) | Panel Tanya PAPAN | `app/src/lib/dasbor/tanyaPapan.ts` (blok "Valuasi pasar") | `punya(t, 'per', …)` memakai `includes`, jadi **setiap** kalimat bermuatan "perlu", "persen", "pertama", "perusahaan", atau "diperbarui" dijawab valuasi pasar. Terukur: "data diperbarui kapan" dan "kalau setoran saya perlu revisi…" dua-duanya dijawab *"Data valuasi pasar hari ini belum ada"* | `/\bper\b/` — kata utuh, sama seperti yang sudah dipakai blok valuasi per-emiten | Gagal senyap kelas terburuk: jawabannya sopan dan masuk akal, tak ada galat, dan tak ada cara pembaca tahu pertanyaannya tak pernah dibaca | Selesai — dua pertanyaan itu kini dijawab entri "Kapan data diperbarui" dan "Aksi kurasi setoran"; keduanya dikunci uji di korpus | Fixed |
| 91 | Catatan glosarium membocorkan jalur berkas repo ke pengguna | (pass kebocoran #89) | Panel Tanya PAPAN | `app/src/lib/dasbor/tanyaPapan.ts` (`saring()`) | Jawaban "apa itu PCD" ditutup dengan *"(lihat `arus-pasar/pcd.py`)"* — jalur berkas internal, tercetak apa adanya di panel publik | Saringan di TITIK KELUAR (`jawabTeks`): kurung yang memuat nama berkas dibuang, backtick dibersihkan | Isi glosarium ditambang otomatis dari korpus PAPAN, jadi rujukan semacam itu akan lahir lagi tiap kali dibangun ulang — menambalnya di datanya berarti menambal lagi bulan depan | Selesai — uji menyapu **seluruh** 75 istilah glosarium dan seluruh entri pengetahuan, bukan cuma yang sudah ketahuan | Fixed |
| 92 | Tiga jawaban percaya-diri yang salah sasaran, ketemu saat menyisir sisa kelemahan | (pass akhir #89) | Panel Tanya PAPAN | `app/src/lib/dasbor/tanyaPapan.ts` (blok arus asing, blok "kemarin", `MINTA_REKOMENDASI`) | (a) "net buy asing di BBCA berapa" dijawab arus asing **se-pasar** tanpa satu kata pun penanda bahwa itu bukan angka BBCA; (b) "IHSG kemarin berapa" dijawab ringkasan hari **berjalan**; (c) "BBCA vs BBRI bagus mana" dijawab *"BBCA: peringkat 1 nilai transaksi terbesar"* | (a) menyebut terus terang arus asing per emiten belum dihitung di sini dan menunjuk Stock Detail; (b) hari sebelumnya diambil dari seri dengan **mencocokkan tanggal hari berjalan lebih dulu** — kalau tak terlacak, menunjuk Chart alih-alih menebak barisnya; (c) masuk penjagaan "PAPAN tidak memberi rekomendasi" | Ketiganya bentuk kerusakan yang sama: angka yang benar untuk pertanyaan yang lain. Pembaca tak punya cara tahu — dan pada (a) angkanya berbeda ribuan kali lipat dari yang ia kira | Selesai — lima uji baru mengunci ketiganya, termasuk jalur "kalau tanggalnya tak terlacak"; `npx vitest run` **623 hijau** | Fixed |

## Sesi 18 Agustus 2026 (malam) — `/grafik` mengikuti tata letak TradingView

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 87 | Tata letak `/grafik` mengikuti acuan Stockbit/TradingView | "desain layout chartnya sama seperti trading view" · "kan sudah ada desain nya dari stockbit tuh" · "saya minta 100% mirip tradingview sudah ada bahan dan segala macam nya" | `/grafik` | `app/src/views/dasbor/GrafikEmiten.tsx`, `GrafikEmiten.css` | Panel "Emiten" terpisah di atas + chip Lilin/Garis dan chip rentang berdesakan di `panel-h`; tak ada bilah bawah; tak ada baris status OHLC | Bilah atas (cari · kerangka waktu · jenis chart sebagai tombol ikon · `ƒx Indikator` · `+ Pola` · kamera · layar penuh), kanvas bertingkat berlegenda per panel + tombol lipat `^`, bilah bawah (`1D 5D 1M 3M 6M 1Y 5Y All` · `UTC+7` · `%` `log` `auto`) | Kendali yang mengubah APA YANG DIGAMBAR duduk di atas kanvas, yang mengubah APA YANG TERLIHAT duduk di bawahnya — pembagian itu yang membuat bilah bawah bukan sekadar tempat sisa | Selesai — tinggi kanvas **tetap 460px** dengan 1, 3, dan 4 panel (terukur `getBoundingClientRect`); 412px `scrollWidth` = `innerWidth` = 412, nol elemen luber; nol galat konsol di kedua viewport | Changed |
| 88 | Kerangka waktu `5m 15m 30m 1h 4h D W M` | (bagian dari #87) | `/grafik` | `app/src/lib/dasbor/kerangkaWaktu.ts` (baru), `kerangkaWaktu.test.ts` (baru), `api/yahoo.js` (baru), `app/vite.config.ts` (`server.proxy`) | Cuma harian; nol data intraday di repo | 5m/15m/30m/1h dari Yahoo lewat proksi se-asal `/api/yahoo`; **4h dirakit dari 60m** (Yahoo tak punya interval itu); W/M dirakit dari harian. Waktu internal tetap STRING (`'yyyy-mm-dd HH:mm'` WIB) sehingga potong rentang, pola, dan legenda tak perlu tahu ada kerangka waktu | 1 menit sengaja dibuang — Yahoo cuma menyimpan ±5 hari, dan menu yang menjanjikan "1 menit" lalu memberi seminggu data lebih buruk daripada tak ada menunya. Proksi CORS publik ditolak: keempatnya (corsproxy.io, allorigins, codetabs, thingproxy) dibalas 429/520 — pembedanya User-Agent, bukan IP | Selesai — jarak antar-bar terukur (median 20 jarak terakhir): 5m=**300**s, 15m=**900**s, 30m=**1800**s, 1h=**3600**s, 4h=**14400**s, D=86400s, W=604800s, M=2678400s. Jumlah bar BBCA: 5m 1.361 (±1 bln), 1h 3.175 (±2 thn), 4h 1.400 | Added |
| 89 | Modal setelan tiga tab, `Cancel` benar-benar membatalkan | (bagian dari #87) | `/grafik` | `app/src/components/dasbor/ModalSetelanInstans.tsx` (baru), `DaftarInstans.tsx`, `grafikEmiten.ts` (`salinInstans`, `terapkanDraf`, `GayaPlot`) | Panel melayang yang menerapkan tiap ketikan SEKETIKA — tak ada jalan mundur sama sekali | `ModalKecil` + `.tabs` kanonis, tiga tab Inputs/Style/Visibility; seluruh perubahan hidup di draf hasil salinan DALAM dan baru dijahitkan saat `Ok`. Style: per plot (kotak centang, warna, gaya garis, tebal) + OUTPUTS (Precision, Labels on price scale, Values in status line) | `{...inst}` saja lolos tipe dan lolos tinjauan, tapi `param`/`gaya` objek — salinan dangkal membuat tiap ketikan langsung berlaku dan `Cancel` berbohong tanpa satu pun galat | Selesai — MACD 12/26/9 diubah ke 5/40/3 di **tiga kolom**, `Cancel` → legenda tetap `MACD 12/26/9` DAN modal dibuka lagi menampilkan `12/26/9`; `Ok` → `MACD 5/40/3`. Uji `salinInstans`/`terapkanDraf` menjaga keduanya | Changed |
| 90 | `/grafik` menerima `?kode=` dan `?tf=` | "semua kode emiten on click nya ke chart" | `/grafik` | `app/src/views/dasbor/GrafikEmiten.tsx` (`useSearchParams`, `pilihEmiten`) | Emiten cuma di state lokal — tiap tautan dari halaman lain mendarat di BBCA | `?kode=` menang atas bawaan; kode tak dikenal jatuh ke bawaan DAN disebut di layar; URL menyusul keadaan halaman dengan `replace` | `push` mengubah tombol Kembali peramban jadi riwayat tiap emiten yang pernah dibuka — enam emiten dilihat berarti enam kali Kembali sebelum benar-benar keluar | Selesai — `/grafik?kode=BBRI&tf=1h` membuka BBRI pada 1h (3.174 bar, jarak 3.600s); ganti emiten dari dalam halaman → URL jadi `?kode=TLKM&tf=1h` dengan `history.length` **tetap 3**; `?kode=ZZZZ` → BBCA + catatan "Kode ZZZZ tidak ada di daftar emiten" | Added |
| 91 | Ikon aksi legenda tak pernah muncul | (temuan verifikasi #87) | `/grafik` | `GrafikEmiten.css` (`.grf-legenda-baris`) | Cuma `.grf-legenda-aksi` yang ber-`pointer-events: auto`, dan selagi ikonnya tersembunyi ia `none` — tak ada bagian baris yang bisa di-hover, jadi ikon "muncul saat disentuh" tak pernah muncul | `pointer-events: auto` pindah ke BARISNYA | Wadah legenda tetap tembus supaya crosshair hidup; yang menangkap kursor cuma pita setinggi satu baris di pojok kiri atas | Selesai — terukur: baris yang di-hover `opacity` ikon **1** + bingkai `rgb(54,57,69)`, dua baris lain **0** + bingkai transparan | Fixed |

---

## Catatan asal: apa yang diminta, sebelum & sesudah

Dibuat atas permintaan Johan 16 Agustus 2026: *"perlu ada ceklist backlog dalam
bentuk tabel yang mana ada perintah saya dan sebelum dan sesudah perubahan."*

**Kenapa berkas ini ada.** `ceklist-backlog.md` menjawab "apa yang sudah/belum
selesai", `rencana-berjalan.md` menjawab "apa keputusannya". Yang tak terjawab
keduanya: **apa persisnya yang diminta, dan apa yang berubah karenanya.**
Tanpa itu, sebuah baris backlog bisa berumur berhari-hari tanpa ada yang tahu
maksud aslinya — persis yang terjadi pada #130 (lihat bagian paling bawah).

Aturan pengisian:

- **Permintaan** ditulis sedekat mungkin dengan kata Johan sendiri. Kalau
  diringkas, meringkasnya tak boleh mengubah maksud.
- **Sebelum** harus keadaan yang bisa diperiksa, bukan "belum bagus".
- **Sesudah** harus hasil yang bisa dibuktikan — angka, berkas, atau perintah.
- Baris ditambahkan **saat pekerjaannya selesai**, bukan saat direncanakan.

---

## Sesi 16 Agustus 2026 (siang–malam) — Beranda, Kabar Pasar, Tanya PAPAN

### Admin & kurasi

| # | Permintaan Johan | Sebelum | Sesudah | Commit |
|---|---|---|---|---|
| 1 | "orderbook di ubah ke Broker Summary / perlu ada tombol edit / kalau chart tidak kita perlukan hapus saja kolom nya" | Kolom & input Chart masih ada; setoran tak bisa diubah; istilah "orderbook" di layar | Kolom Chart dibuang, tombol Ubah + mode sunting, gerbang panduan screenshot muncul sebelum modal unggah pertama | `17b39c15` |
| 2 | "jenjang pemula sudah ada tapi jenjang tier nya misal perunggu+icon nya sampai diamond" | Kartu jenjang cuma menyebut tier saat ini | Tangga Perunggu→Diamond dengan lencana | `39f5da7b` |
| 3 | "icon ini berbeda sih / pakai ini saja" | Saya menggambar lambang baru (tunas/perisai/mahkota) tanpa cek proyek sudah punya | Semua pakai `IkonJenjang` yang memang sudah ada; lambang gambaran saya dibuang seluruhnya | `e70ed00d` |
| 4 | "searchable dan pagination ya per 10, sorting juga berdasarkan tinggi jenjang" | Tabel akun tanpa cari/urut/paginasi | Cari email+alias, 7 pilihan urutan (awal: jenjang tertinggi), 10 baris per halaman | `39f5da7b` |
| 5 | "kenapa kok waktu buka page ini ada delay yang berasa? apakah tidak ada di kemampuan terkait lazy load?" | Semua thumbnail diunduh sekaligus — **terukur 302 ms JS vs 5.042 ms jaringan**, gambar 420–520 KB ditampilkan di kotak 40 px | Dimuat saat terlihat saja (IntersectionObserver) | `ab7bd948` |
| 6 | "buatkan keterangan di superadmin untuk saya aturan nya gmn, kalau di tolak, di revisi, di hapus data hilang atau masih ada" | Aturan kurasi cuma ada di kepala | Panel lipat berisi tabel 4 status × 6 akibat, diturunkan dari kode & fungsi SQL yang **sedang berjalan** | `7d5daf78` |
| 7 | "kan kita pernah bahas soal di hapus itu terkait data di supabase tidak bisa sinkron?" | `hapusScreenshot()` menghapus **berkas dulu** lalu menelan galat hapus baris → baris yatim | Urutan dibalik (baris dulu) + hasilnya diperiksa, karena **RLS yang menolak DELETE tidak melempar galat** | `13489191` |
| 8 | "ok hapus saja" (baris yatim INDY) | 1 baris menunjuk berkas yang sudah lenyap | Dihapus; **0 baris yatim, 0 berkas yatim, 24 setoran** | — (SQL) |

### Halaman baru: Beranda & Kabar Pasar

| # | Permintaan Johan | Sebelum | Sesudah | Commit |
|---|---|---|---|---|
| 9 | "page ini sudah tidak lagi halaman utama... news dulu, kemudian kartu-kartu yang inspiratif... identitas sebenarnya itu 'DATA dan INFORMASI'" | Halaman utama = dasbor angka | Beranda baru: identitas, papan IHSG, ringkasan pasar, terbit terakhir, kabar, kartu menu | `29fdfd34` |
| 10 | "teks Papan besar itu bagus tapi masuk di section yang ada angka IHSG nya" | Judul PAPAN di kartu terpisah | Nama PAPAN jadi kepala papan IHSG, satu kartu | `6a493634` `13dd9b23` |
| 11 | "ubah chart ini ke dalam bentuk candle? data ytd yang di tampilkan supaya mewakili di sistem" | Garis | Lilin untuk YTD & 1 tahun (Chart.js floating bar, tanpa pustaka finansial). **Ketahuan bug**: YTD terbaca −26,82% padahal seharusnya −28,43% — dihitung dari lilin pertama, bukan penutupan tahun lalu | `faef198d` |
| 12 | "kalau ini terlihat terlalu luas dan panjang dibagi 2 atau 3 kolom" | Satu kolom selebar layar | Dua kolom | `fb170c00` |
| 13 | "berita di versi mobile di pangkas karena tujuan kita branding... jika berita lebih bnyk user males scroll menu dibawah" | Semua kabar tampil di telepon | Dipangkas di telepon supaya kartu menu tak terkubur | `f575f810` |
| 14 | "RSS Media dan Pengumuman Resmi dari IDX... buatkan juga rumah section news nya" | **Tak ada berita sama sekali** | Halaman `/kabar` + panen 4 sumber | `e1f1cd3e` |
| 15 | "apakah sudah bisa otomatis ini rss feed nya? kirim agentic untuk memperbaiki" | Panen manual dari mesin rumahan | GitHub Actions tiap 2 jam untuk sumber tanpa batasan IP; IDX & IPOT tetap di rumah (403 dari datacenter) | `e1a5bb05` |
| 16 | "informasi ini di landing saja dijadikan 4 kolom dari IDX, Ipot, Stockbit, dan Kontan" | Satu aliran campur | Empat kolom per sumber di Beranda saja | `067bb7f1` |
| 17 | 4 tautan kanal IPOT | IPOT dipanen dari daftar campur (ada politik Amerika, pemilu Zambia) | Empat kanal topik + waktu terbitnya | `094878c7` |
| 18 | "carikan stockbit snips penting juga, perintahkan agent untuk fetch 1 tahun ini" | — | 238 item setahun lewat `?format=json` Squarespace | `ab89418b` |
| 19 | "sweep teks dipanen atau panen diubah jadi update atau terbaru" | Kata "panen" bocor ke layar pengguna | Diganti "diperbarui" | `387573cd` |
| 20 | "IPOT News itu lebih penting bisa ambil datanya 1-3 bulan belakang kalau bisa ya YTD" | Cuma halaman pertama tiap kanal | 737 item, 13 Jul–16 Agu. **YTD TIDAK tercapai**: endpoint mengabaikan `halaman` (halaman 0/1/5/50 membalas 200 `news_id` yang sama) | `b00a8a23` |
| 21 | "CNBC dan detikFinance >>> cabut saja" | 2 sumber bermasalah ikut dipanen | Dicabut; 20 item lamanya dibuang dari `kabar.json` | `10e1f160` |
| 22 | "lokasi dropdown bener2 gak estetik" → "desain dropdown nya gini ya?" | Dropdown menggantung di tengah header; daftarnya digambar sistem operasi (putih terang di panel gelap) | Disatukan jadi satu kendali dengan kotak cari, pakai komponen `Dropdown` proyek yang bertema | `b9604e59` |
| 23 | "kok kosong gini?" (tab IDX cuma 1 baris) | Dedup ber-tautan meringkas **19 pengumuman jadi 1** — semuanya menunjuk satu URL generik | Kunci dedup jadi tautan+judul+waktu; tiap pengumuman kini menunjuk **PDF-nya sendiri** | `23ab2322` |

### Tanya PAPAN

| # | Permintaan Johan | Sebelum | Sesudah | Commit |
|---|---|---|---|---|
| 24 | "apakah ada sistem AI yang tidak bayar biaya?" → "kalibrasi dulu tanpa AI, baru sambungkan Gemini Flash" | Ambang narasi ditebak (>1%, <0,3%) | **Dikalibrasi dari 2.409 hari bursa**: gerak "kuat" = 1,27% (p85), "nyaris datar" = 0,28% (p30) | `d555b683` |
| 25 | "boleh buatkan tombol mengambang dengan icon P yang seperti AI, dan muncul kolom chat" | — | Tombol + panel percakapan | `d555b683` |
| 26 | "P itu boleh tapi tambah lah ada animasi nya dan teks 'AI' nya" / "animasi diluar ini ngapain sih, jadi overflow" / "berikan logo gradient" / "icon nya di perkecil" | Cincin conic berputar **bocor keluar** saat tombol memanjang | Cahaya bernapas di dalam bayangan (selalu terkurung bentuknya), gradasi 2 warna merek, 46 px di telepon | `038470c8` `581fb7fa` |
| 27 | "prioritaskan semua data kita **kecuali yang sifatnya privasi seperti akun kontributor**, dan serap teks di Papan ke Rule Engine" | Cuma menjawab ringkasan harian | Harga, valuasi, sektor, kinerja setahun, kepemilikan KSEI, grup konglomerat, kalender bursa — **tanpa menyentuh data akun kontributor sama sekali** | `fd1479d6` |
| 28 | "kalau perlu ada agent yang membantu untuk explorer glosarium sehingga tidak bnyk menggunakan gemini flash" | — | **75 istilah** ditambang dari korpus PAPAN sendiri, tiap entri bawa frekuensi + kutipan asli | `6e885a2c` `3d107035` |
| 29 | "kok tidak bisa jawab terus sih" (dari tangkapan layar) | "kondisi pasar sekarang" ditolak karena tak menyebut kata IHSG | Dijawab ringkasan hari itu. **Ketahuan dari uji peramban, bukan dari tes** — tes cuma menanyakan yang sudah kepikiran | `e04c2f02` |
| 30 | "scrollnya harus inline beginian, gutter tipis 2px saja cukup" | Batang gulir tebal bawaan sistem (15 px) | Aturan **bawaan** `.lantai ::-webkit-scrollbar` — 1 px, berlaku ke semua panel baru tanpa perlu disalin | `13489191` |

### Rilis & tata kelola

| # | Permintaan Johan | Sebelum | Sesudah | Commit |
|---|---|---|---|---|
| 31 | "cabut keduanya dari manifest sekarang" (edisi Mingguan & Bulanan) | Dua edisi terbit tanpa diminta; mingguan **21 dari 24 halaman identik karakter-per-karakter** dengan edisi harian | Dicabut dari manifest (PDF tetap tersimpan); rakit ulangnya jadi #166 | `c53606ec` |
| 32 | "sebaran data gini baiknya tanggal di kolom pertama ya? bayangkan misal dalam 3 bulan data sudah lebih dari 60" | Tanggal bukan kolom pertama, tanpa paginasi | Tanggal jadi kolom pertama + paginasi + filter | `c53606ec` |
| 33 | "#160 jalankan sekarang" | 3 objek SQL masih menyaring `'ditolak'` yang tak ada → **akurasi semua kontributor selalu 100%** | Diperbaiki + 7 berkas klien. Superadmin kini **96% (22/23)** | `24dc03c8` |
| 34 | "Track saja CLAUDE.md" | Ber-gitignore → aturannya tak terbawa ke worktree baru | Di-track (dicek dulu tak memuat rahasia) | `24dc03c8` |
| 35 | "waktu nya push live" | 48 commit lokal | Live: `778ec1c2..94958c5a`, 73 berkas, +17.054/−374 | — |
| 36 | "bukannya jumlah commit tadi ditulis 55, padahal sudah 61" | Saya menghitung dari commit teratas saat sesi dimulai → **selalu melebihkan** | Patokannya `origin/main`: `git rev-list --count origin/main..HEAD` | `def1c2f4` |

---

## Permintaan yang belum punya baris "sesudah"

Ini yang **menunggu Johan**, dan sengaja dipisah supaya tak tenggelam.

| # | Permintaan asli | Kapan | Yang menghalangi |
|---|---|---|---|
| 145 | "bar tembus" (bagian dari #107) | 15 Agu 2026 | Istilahnya tak punya rujukan di kode. Bar kapitalisasi yang melewati kotak, atau bar dua arah dari sumbu nol? |
| 146 | "divergensi tiga lapis" (definisi untuk #130) | 15 Agu 2026 | Lapis mana: harga vs volume, volume vs frekuensi, asing vs domestik? Urutannya menentukan seluruh perhitungan |

### Asal-usul #130 — kenapa istilahnya menggantung

Lahir **15 Agustus 2026**, commit `d697f049` *"tiga task baru masuk antrean pada
posisi ongkosnya"*, satu paket dengan #131a, #131b, dan #129. Bunyi barisnya
sejak awal cuma:

> `| 130 | Analisis volume & divergensi tiga lapis | Besar | Yang tak ada di aplikasi lain | terhalang #122/#108 |`

Kolom "kenapa layak" terisi, kolom "apa persisnya" **tidak pernah ada**. Waktu
itu #130 terhalang data (#122 OHLC & #108 harga buka belum dipanen), jadi
definisinya tak mendesak. Sekarang kedua penghalang itu sudah beres — dan yang
tersisa justru pertanyaan yang tak pernah ditanyakan.

**Itulah gunanya berkas ini.** Baris backlog yang cuma menyimpan *judul*
permintaan akan kehilangan maksudnya begitu penghalang teknisnya hilang.

---

## Chart PAPAN sendiri — jawaban singkat: **belum ada**

Ditanyakan Johan 16 Agu 2026: *"tadi saya baca chart Papan sendiri ada?"*

Yang **sudah** ada dan sering disalahsangka sebagai chart PAPAN:

| Yang terlihat | Sebenarnya |
|---|---|
| Chart di halaman `/chart` | **Widget TradingView** (`TradingViewChart.tsx`) — skrip mereka, digambar mereka |
| Grafik IHSG di Beranda | **Chart.js** floating bar yang dibentuk jadi lilin — buatan kita, tapi cuma IHSG, tanpa indikator, tanpa zoom |

Keputusan sudah diambil Johan 16 Agu: **opsi A** — mesin gambar
`lightweight-charts` (Apache-2.0, dipasang lokal, tanpa iframe dan tanpa
panggilan ke server TradingView), sedangkan **seluruh indikator dan overlay
tetap kode kita**.

Tahapnya (dari `rencana-berjalan.md`):

| Tahap | Isi | Status |
|---|---|---|
| 1 | #122 panen OHLC 5 tahun | ✅ 962 emiten, 37,3 MB |
| 2 | #108 harga buka IHSG | ✅ 8.849 hari 1990–2026 |
| 3 | Chart dasar: lilin + volume + zoom, satu emiten | ✅ 17 Agu 2026 — `/grafik`, `3d47eca5` (#63) |
| 4 | Indikator baku: MA, EMA, RSI, MACD, Bollinger | ☐ |
| 5 | #130 divergensi tiga lapis | ☐ butuh #146 |
| 6 | Overlay khas PAPAN: pita musiman, akumulasi broker, penanda Radar | ☐ |
| 7 | #129 bandarmologi multi-panel | ☐ butuh sumber broker per emiten |

**Pondasi + chart dasar (tahap 1–3) sudah berdiri.** Tahap 3 sudah dirilis
(rute `/grafik`, dijaga login) — tahap 4 (indikator) yang berikutnya kalau
diminta. Tahap 6 yang membuatnya tak punya pembanding — dan itu semua
bergantung data yang cuma PAPAN punya, bukan pada mesin gambarnya.

---

## Sesi 18 Agustus 2026 (malam) — panen statistik IDX pulih + statistik BULANAN

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 147 | Seluruh pemanen idx.co.id pindah dari `requests` ke `curl_cffi` | "statistik hari ini sudah rilis bro" | — (lapis data) | `scripts/idx_net.py` (baru), `download_idx.py`, `sinkron_emiten.py`, `fetch_broker_summary.py`, `fetch_fundamental.py`, `panen_kabar.py` | Endpoint IDX menjawab **403** ke `requests` walau header perambannya lengkap. Diagnosis "IP-nya diblokir" salah: URL yang sama lewat `curl_cffi impersonate=chrome124` menjawab **200** dari mesin dan IP yang sama | Satu pintu `idx_net.get()` — sesi curl_cffi bersama, header peramban lengkap, pemanasan cookie, backoff 5/15/45 dtk | Yang ditolak **sidik jari TLS**, satu lapis di bawah header. Menambah header, memperlambat laju, atau ganti IP tak menyembuhkan apa pun. Dijadikan satu modul supaya perbaikan berikutnya (ganti `IMPERSONATE`) cukup disunting di satu tempat — sebelumnya tiap pemanen menyalin bloknya sendiri | Selesai — `idx_net.py --periksa` HTTP 200/963 baris; GetBrokerSummary 200/88 broker; GetSecuritiesStock 200/962 saham; GetNewsSearch 200 (lewat `requests`: 403); `panen_kabar.py` 30+30 item IDX | Fixed |
| 148 | Playwright dibuang dari tiga pemanen | (bagian dari #147) | — | `download_idx.py`, `sinkron_emiten.py`, `fetch_broker_summary.py` | Tiga skrip meluncurkan Chromium hanya untuk memanggil API JSON: `download_idx` menggulir halaman Nuxt lalu memungut `a[href*='.pdf']`, dua lainnya memakai `page.evaluate(fetch)` karena akses langsung 403 | Panggilan HTTP biasa lewat `idx_net`; daftar PDF diambil dari `/primary/Statistic/GetStatistic` (ditemukan dengan membaca bundel `_nuxt/*.js`, bukan menebak URL) | Alasan asli memakai Playwright — "harus dari dalam halaman" — ternyata salah baca gejala: yang membedakan TLS, bukan asal permintaan. Ongkosnya nyata: luncur Chromium, `wait_until` yang sering timeout dari IP datacenter, dan penggerus HTML yang patah tiap IDX mengubah tata letak | Selesai — `download_idx.py --hari-ini` 11/11; `--jenis mingguan --semua` 33/33; `fetch_broker_summary.py --tanggal 20260818` 1/1; `sinkron_emiten.py` 963 emiten per 2026-08-18 | Fixed |
| 149 | Kejar statistik harian yang tertinggal | "statistik hari ini sudah rilis bro" | Beranda/Ringkasan (semua yang membaca `index.json`) | `data-idx/json/ds_260818.json`, `index.json` | Berhenti di **2026-08-14** (142 tanggal) | **143 tanggal**, terbaru `ds_260818` — IHSG 6.449,83 (+0,75%), hari bursa ke-146 | 15–16 Agu akhir pekan; **17 Agu Senin libur bursa (HUT RI)** — API IDX memang tak menerbitkan DS untuk tanggal itu, jadi tak ada yang tertinggal selain 18 Agu | Selesai — `GetStatistic type=daily` 10–18 Agu membalas tepat 6 berkas (10,11,12,13,14,18); `ds_260818` terunduh & terparsing | Fixed |
| 150 | Statistik **BULANAN** — belum pernah dipanen | "kirim agent panen yang monthly juga" | (belum ada halaman — untuk UI berikutnya) | `scripts/parse_idx_monthly.py` (baru), `download_idx.py` (`--jenis bulanan`, `--mundur`), `data-idx/json/ms_YYMM.json` + `index_monthly.json` | Tak ada sama sekali. Tab "Bulanan" halaman statistik IDX tak pernah disentuh | **11 periode** (Sep 2025 – Jul 2026) × 15 halaman terparsing. Bentuk berkas meniru mingguan (`ws_YYMMDD.json` + `index_weekly.json`) supaya UI memperlakukan harian/mingguan/bulanan sama | Varian **-E (Equity)** saja: `-B` (Bond) & `-SW` (Structured Warrant) bukan saham dan belum ada yang membacanya. Agu 2026 belum terbit — bulannya belum habis, itu bukan kegagalan panen | Selesai — `parse_idx_monthly.py --periksa` 11/11 lolos; invarian silang-halaman: jumlah nilai transaksi 11 sektor (hal 4) = total nilai pasar saham (hal 2/3) di **kesebelas** edisi, selisih ≤ 2 dari ~500.000 miliar IDR | Added |
| 151 | Arsip mentah PDF statistik | "PDF mentahnya wajib masuk `_arsip-mentah/` sebelum diparsing" | — | `download_idx.py` (`unduh()` lewat `arsip_mentah.ambil_atau_unduh`) | PDF diunduh langsung ke `data-idx/daily|weekly/`; unduh ulang berarti menembak jaringan lagi | Mentahnya masuk `_arsip-mentah/statistik/<ds\|ws\|ms>/` dan **dibaca lebih dulu** sebelum jaringan disentuh | Menambah ruas di parser jadi tak berbiaya jaringan sama sekali — dan endpoint IDX yang digedor menjawab 403 | Selesai — `_arsip-mentah/statistik/` **73 MB** (1 PDF harian + 11 PDF bulanan @ ~6,6 MB) | Added |
| 152 | Bulanan disambung ke pipeline yang sudah jalan | (bagian dari #150) | — | `.gitignore:21`, `JALANKAN_OTOMATIS.bat` (langkah 3b), `.github/workflows/update.yml:53` | `--jenis semua` cuma harian+mingguan; tak ada yang memanggil parser bulanan | `--jenis semua` kini ds+ws+ms, dan dua penjadwal memanggil `parse_idx_monthly.py --semua` | Pemanen yang tak pernah dipanggil sama saja dengan tak ada. PDF bulanan 6,6 MB/edisi ikut di-gitignore seperti daily/weekly | Selesai — grep memastikan ketiga berkas terpasang | Added |

**Ruas mingguan vs bulanan (UI perlu tahu).** Ada di mingguan, TIDAK ada di
bulanan: `indeks_global` (hal 21 bulanan cuma grafik), `transaksi_investor`
(hal 16 grafik donat), `dana_dihimpun` (tak ada padanan), serta kolom
frekuensi di `top_saham`/`top_broker`. Ada di bulanan, TIDAK ada di mingguan:
`sektor` (11 sektor IDX-IC), `syariah`, `sekuritas_tercatat`,
`indeks_kinerja` (1M/3M/6M/1Y/3Y/5Y/10Y + volatilitas), kolom pembanding YoY
dan YTD, serta Market PER/PBV.

---

## Sesi 18 Agustus 2026 (malam) — layar untuk statistik mingguan

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 153 | Halaman Statistik Berkala — arsip mingguan IDX akhirnya punya layar | "kan kemarin ada data weekly dan monthly sudah di panen tapi belum ada UI nya, kerjakan" | **/statistik** (baru), grup Pasar, kode `STA` | `app/src/lib/dasbor/statistikBerkala.ts` (baru), `app/src/views/dasbor/StatistikBerkala.tsx` + `.css` (baru), `app/src/lib/dasbor/menu.ts:98`, `app/src/lib/aksesHalaman.ts:130`, `app/src/App.tsx:35,84` | **33 berkas `ws_*.json` + `index_weekly.json` dipanen sejak lama dan tak pernah dibaca aplikasi** — `grep` untuk `index_weekly`/`ws_` di seluruh `app/src` mengembalikan nol | Satu layar: pilih pekan (panah + daftar 33 edisi), lima tab — Ringkasan · Saham · Broker · Indeks · Transaksi | Bentuknya "pilih satu edisi" karena hampir tiap angka di berkasnya SUDAH berpasangan dengan pekan sebelumnya. Pembanding sengaja TIDAK dihitung ulang dari edisi tetangga: arsipnya berlubang (pekan 16–17 Mar cuma dua hari bursa), jadi hasilnya akan beda dari terbitan resmi IDX tanpa satu pun galat | Selesai — 33 edisi di pemilih, 0 luber mendatar di 412px (kelima tab), 0 galat konsol, `tsc -b --force` bersih, `vitest` 623 hijau (16 baru) | Added |
| 154 | Ruas kosong tampil "—", bukan nol | (bagian dari #153) | /statistik | `statistikBerkala.ts` (`angka`, `persen`, `persenBanding`, `selisihBanding`) | — (kode baru) | `null`/ruas hilang → "—"; 0 yang memang tertulis di sumber tetap 0 | Terukur di data nyata: `net_asing` ADA di 32 edisi tapi TIDAK di edisi 14 Agu; `rentang_minggu` kosong di 3 edisi; `reit`/`dinfra`/`obligasi_pemerintah_usd` null di SELURUH 33 edisi. Nol di sana berbunyi "asing tidak bertransaksi" — pernyataan yang salah | Selesai — uji `statistikBerkala.test.ts` mengunci keduanya (kosong→"—", 0 sumber→0) | Added |
| 155 | Persen PANGSA dipisah dari persen PERUBAHAN | (temuan saat verifikasi #153) | /statistik | `statistikBerkala.ts` (`pangsa()`), `StatistikBerkala.tsx` (Top Saham/Broker, Rekap per papan) | Kolom `%` porsi pasar dicetak lewat `persen()` → **"+86,96%"** pada Pasar Reguler | `pangsa()` tanpa tanda → "86,96%", judul kolom jadi "% pasar" | Dua jenis persen hidup di berkas yang sama dan cuma dibedakan nama ruasnya: `top_saham[].persen` itu porsi total, `top_gainers[].persen` itu perubahan. "+86,96%" terbaca "naik 87%" padahal artinya "87% dari seluruh volume" — salah baca yang tak memunculkan galat apa pun | Selesai — uji `pangsa(86.96)==='86,96%'` vs `persen(86.96)==='+86,96%'` | Fixed |
| 156 | Bulanan: menunya TERLIHAT tapi MATI | "monthly ... belum ada UI nya" + batas "jangan menampilkan ruas karangan" | /statistik (chip "1 Bulan") | `StatistikBerkala.tsx:205`, `statistikBerkala.ts` (kepala berkas), `PemilihRentang.tsx` (+`opsi[].nonaktif`) | Rencana awal: bulanan meniru mingguan, jadi cukup ditukar sumbernya | Chip "1 Bulan" tampil `disabled` dengan alasan di `title`; pemuatnya sudah generik, layarnya belum | `ms_2607.json` mendarat di tengah sesi ini dan ternyata **bukan** cermin mingguan: `bulan_ini`/`bulan_lalu`/`tahun_lalu`/`mom`/`yoy`, gainers pakai `sebelumnya`/`penutupan`, plus blok tanpa padanan (`sektor`, `syariah`, `sekuritas_tercatat`, `indeks_kinerja`). Merendernya lewat komponen mingguan = panel kosong yang terbaca rusak; menebak artinya = angka karangan | Selesai (sengaja belum aktif) — chip `disabled=true` terverifikasi di peramban; `bacaIndeks` sudah tahan bentuk bulanan (uji urut `ms_2606`/`ms_2607`) | Added |
| 157 | Pass kebocoran | (batas tugas) | /statistik | `statistikBerkala.ts` (tipe `EdisiBerkala`) | Berkas sumber memuat `_sumber_pdf`, `_halaman_diambil`, `_halaman_dilewati` | Ruas ber-awalan `_` tak didaftarkan di tipe & tak pernah dirender | Halaman Metodologi hari ini sempat tayang dengan nama berkas internal tercetak di layar publik | Selesai — pindai teks kelima tab: nihil untuk `ws_2`/`ms_2`/`_sumber`/`.json`/`data-idx`/`/src/`/`.pdf`/`undefined`/`NaN` | Fixed |

**Kenapa `/statistik` publik.** Kunci `statistik` sudah dipasang di rute dan
di `PETA_MENU_KUNCI`, tapi barisnya belum ada di tabel `akses_halaman` —
kunci tak dikenal **fail-open** (`aksesHalaman.ts:30`), jadi halamannya
terbuka sampai Johan menetapkan tingkatnya dari tab Akses, tanpa menyentuh
kode lagi. Yang penting kuncinya ADA, supaya halaman ini tak jadi celah yang
tak kelihatan dari panel — persis yang dulu terjadi pada Top Stocks & Top
Broker.

**Yang belum dibuat, dan tidak dihalangi tata letak ini.** Deret lintas-edisi
(tren nilai transaksi 33 pekan dalam satu grafik) perlu memuat seluruh arsip
(±34 KB × 33 ≈ 1,1 MB); halaman ini memuat satu edisi saja. Pemilih edisinya
berdiri di kepala halaman, jadi deret itu cukup ditambahkan sebagai tab
keenam tanpa menyentuh yang sudah ada.

---

## Sesi 18 Agustus 2026 (malam) — bilah alat gambar Grafik Emiten

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 158 | Bilah alat gambar ala TradingView di `/grafik` | "ya pasang alat gambarnya" | /grafik | `app/src/lib/dasbor/gambarPustaka.ts` (baru), `useAlatGambar.ts` (baru), `gambarGrafik.ts`+`.test.ts` (baru), `app/src/components/dasbor/AlatGambar.tsx` (baru), `IkonMenu.tsx` (+9 ikon), `GrafikEmiten.tsx`/`.css` | Komentar di kepala `GrafikEmiten.tsx` sendiri menyebutnya SENGAJA tak ada: "bilah yang tombolnya tak melakukan apa-apa lebih buruk daripada tak ada bilahnya" | `lightweight-charts-drawing` (MIT, dimuat DINAMIS) — tujuh tombol utama (kursor, Garis Tren, Horizontal, Vertikal, Fibonacci Retracement, Persegi, Teks, Kuas) + dropdown "lainnya" berisi 61 sisa dari 68 alat, dikelompokkan per kategori pustaka | `InteractionHandler` bawaan pustaka ternyata TAK PERNAH menyelesaikan alat bertitik-SATU (garis horizontal/vertikal/teks) — cabang FSM `idle` tak memeriksa jumlah titik; dipakai apa adanya, tiga dari tujuh tombol utama akan jadi tombol mati. Ditulis FSM klik-klik sendiri (lebih pendek, berlaku seragam 1–4 titik, jadi SEMUA 68 alat — bukan cuma 7 — benar-benar menggambar) | Selesai — lihat laporan akhir sesi ini: `tsc -b --force` bersih, `vitest` 623/623 hijau (10 baru), bundel `/grafik` +7,5 kB gzip 2,46 kB (pustaka gambar 229 kB/36 kB gzip di chunk TERPISAH, dimuat cuma saat bilah disentuh). Verifikasi peramban dua-viewport TAK selesai — `/grafik` terkunci login dan agen ini tak boleh mengisi kolom sandi (lihat laporan) | Added |
| 159 | Penyimpanan gambar PER EMITEN, terikat waktu+harga (bukan piksel) | (bagian dari #158, spek eksplisit "gambar BBCA tak boleh muncul di TLKM") | /grafik | `gambarGrafik.ts` (`kunciGambar`, `uraiGambar`), `useAlatGambar.ts` (efek muat-ulang per `kode`) | — (fitur baru) | localStorage `papan:grafik-gambar:<KODE>`; anchor disimpan sebagai `{waktu, harga}` — string internal `lib/dasbor/kerangkaWaktu.ts`, BUKAN tipe `Time` mentah (BusinessDay/epoch) pustaka | Menyimpan `Time` mentah mengunci gambar ke representasi kanvas SAAT itu (epoch detik utk intraday). Konversi lewat `keWaktuChart`/`dariWaktuChart` yang SUDAH ada — satu tempat, sama seperti data lilin sendiri. Konsekuensi yang diketahui dan didisiplinkan (bukan disembunyikan): gambar lintas KELUARGA kerangka (harian↔intraday) tak dijamin tampil — bentuk waktunya beda tipe — tapi tak pernah rusak/nyasar, cuma tak tergambar sampai kembali ke kerangka yang sekeluarga | Selesai — `gambarGrafik.test.ts` 10 uji: kunci beda per emiten, serialisasi bolak-balik, baris rusak (versi/NaN/tanpa anchor) dilewati bukan menjatuhkan seluruh daftar | Added |

**Yang TIDAK diverifikasi lewat peramban, dan kenapa.** `/grafik` dijaga
`PenjagaHalaman` (login wajib, bukan bug WIP siapa pun). Aturan CLAUDE.md
global melarang mutlak mengisi kolom sandi, dan sesi agen ini tak punya jalan
lain masuk (tak ada token/cookie tersimpan, tak ada jalur bypass di kode).
Verifikasi yang SELESAI: `tsc -b --force` bersih, 623/623 `vitest` (10 baru),
build produksi membuktikan pemisahan chunk pustaka gambar, dan penelusuran
kode manual jalur-demi-jalur (klik→titik→gambar, drag-anchor bawaan manager,
Delete/Backspace, Escape, muat/simpan per `kode`, re-attach manager saat seri
harga dibongkar-pasang). **Johan perlu login sendiri di `localhost:5173/grafik`
untuk tangkapan layar dua ukuran layar + bukti gambar bertahan setelah muat
ulang** — server dev sudah dimatikan di akhir sesi ini, jalankan ulang
`npm run dev` di `app/`.

---

## Aliran dana asing per emiten (18 Agu 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 160 | Panen aliran dana asing per emiten dari `GetStockSummary` | *"panen kirim agent untuk panen data foreign"* | — (lapisan data; belum ada pembacanya) | `scripts/panen_asing.py` (baru, 250 baris) → `data-idx/json/asing/<KODE>.json` | `ForeignBuy`/`ForeignSell` ada di tiap respons `GetStockSummary` sejak dulu tapi **dibuang** — tak satu pun berkas menyimpannya; `download_idx.py` cuma menyimpan ringkasan level pasar per tanggal | 989 berkas per emiten, 2020-01-02 s/d 2026-08-18, median 1.593 baris. Ruas `[tanggal, beli, jual, volume, value, frekuensi]`; **beli/jual/volume LEMBAR, value RUPIAH, frekuensi kali** — ditulis di tiap berkas sebagai `"satuan"` supaya pembaca berikutnya tak menebak | Per emiten, bukan per tanggal: yang membacanya halaman satu emiten, dan membuka satu emiten tak boleh berarti mengunduh seluruh pasar. `beli` & `jual` disimpan TERPISAH — net menyembunyikan besar perputaran (beli 10 M jual 9,5 M ≠ beli 0,5 M jual 0). `net` sendiri tak disimpan karena turunan murni (`beli - jual`) dan menambah ~15% berat unduhan tanpa menambah informasi | Selesai — **0 tanggal gagal** dari 1.729 hari kerja (1.593 hari bursa, 136 hari libur balas 0 baris). Cek tangan 3 emiten × 3 tahun cocok PERSIS dgn endpoint langsung: BBCA 2026-08-14, ANTM 2023-04-17, TLKM 2020-06-02. 0 tanggal ganda, 0 tak urut, 0 beli/jual negatif. Swauji `--demo` lolos. Arsip mentah 1.729 berkas / 140 MB | Added |
| 161 | Panel "Aliran Asing" di Stock Detail — pembaca UI pertama utk data #160 | *"aliran asing belum ada UI nya, kerjakan juga"* | `/stock-detail` (tab Statistik) | `app/src/components/dasbor/PanelAliranAsing.tsx` (baru) + `.test.ts` (baru), `app/src/lib/dasbor/stockDetailData.ts` (+`useStockAsing`, tipe `AsingHarian`/`AsingData`), `app/src/views/dasbor/StockDetail.tsx` (render tab Statistik) | `data-idx/json/asing/*.json` (989 berkas, #160) tak dibaca satu halaman pun — panennya jalan, UI-nya belum ada | Panel lebar-penuh di bawah Laporan Keuangan: (1) strip `.rasio` Net 1/5/20 Hari — tiap kartu beli & jual TERPISAH, bukan cuma net; (2) persentil net hari ini vs 250 hari bursa terakhir emiten itu sendiri (`bar-tr`/`bar-fl`); (3) grafik garis net kumulatif per rentang (`PemilihRentang` + `LABEL_RENTANG`: 1 Minggu/1 Bulan/3 Bulan/YTD/Semua, preset kalender-hari-mundur meniru `BrokerSummary.tsx`); (4) tabel 15 hari transaksi terakhir. Semua satuan LEMBAR — **tak ada perkiraan rupiah**: satu-satunya cara jujur (lembar × harga penutupan historis) butuh sumber harga per-hari yang di luar lingkup berkas ini, jadi sengaja dilewati | Brief eksplisit minta empat hal itu (net per periode, grafik kumulatif "yang membuatnya berguna", konteks persentil "angka tanpa pembanding tak bisa dinilai", beli/jual terpisah). Emiten tanpa berkas (404) → `data: null` TANPA error (pola sama `useStockKeuangan`), panel tampil "Data aliran asing untuk {ticker} belum tersedia." — bukan galat, bukan nol. Dikerjakan pakai komponen kanonis #170 (`PemilihRentang`, `.chip-t`, `.panel`/`.panel-h`/`.panel-b`, `.rasio`, `.bar-tr`/`.bar-fl`, `.chart-wrap`) — nol CSS baru | Selesai — `tsc -b --force` bersih; `vitest` 632/632 hijau (18 baru, kalkulasi net/persentil/kumulatif). Cek tangan BBCA live (n=1.593, akhir 2026-08-18) cocok PERSIS dgn tampilan: net 1H +6.872.000, 5H +30.348.200, 20H +79.693.500, persentil ke-67. Dua viewport 1536×960×1.25 & 412×915×2.625 (muat ulang bersih, bukan resize dari desktop) — nol overflow horizontal, kanvas grafik pas kontainer. **Sertaan mendadak** (instruksi tambahan koordinator, berkas sama): tombol "Diskusi {ticker}" ke `/forum` dihapus dari `StockDetail.tsx` — rute itu sudah dicabut dari `App.tsx`/`menu.ts`, tombolnya jadi mengantar ke halaman kosong | Added |

---

## Perbaikan bilah alat gambar (18 Agu 2026, malam) — dua bug dari verifikasi Johan

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 162 | Layar penuh berhenti di ~460px, sisa layar hitam | *"full page ya full page"* | /grafik, mode layar penuh | `GrafikEmiten.css` (blok `:fullscreen`) | Rantai flex `.grf-panel:fullscreen > .panel-b > .grf-kanvas-bungkus > .grf-chart-wrap` putus di `.grf-kanvas-area` (simpul baru #185, bilah alat + bungkus kanvas) — tanpa `flex:1;min-height:0` di situ juga, kanvas jatuh balik ke tinggi bawaan 460px | Satu baris ditambahkan: `.grf-panel:fullscreen .grf-kanvas-area { flex:1; min-height:0 }` | Setiap simpul ANTARA elemen `:fullscreen` dan target akhirnya wajib ikut meneruskan `flex:1;min-height:0` — menambah satu wrapper baru berarti menambah satu baris CSS di rantai ini juga, bukan opsional | Selesai — tangkapan layar 1920×1080 layar penuh: kanvas mencapai dasar viewport, 0px hitam sisa (dibandingkan dengan tangkapan SEBELUM tambalan yang menunjukkan ~300px hitam) | Fixed |
| 163 | Chart ikut ter-pan/zoom selagi menggambar | *"waktu gambar kenapa chart nya ikutan gerap"* (gerak) | /grafik, alat gambar apa pun aktif | `useAlatGambar.ts` (`terapkanPan`, dipanggil dari `pilihAlat`, Escape, klik-kanan, dan efek deteksi seret-anchor baru) | Tak ada satu pun kode yang menyentuh `handleScroll`/`handleScale` chart — satu gerakan seret dibaca DUA KALI (pan bawaan chart + listener alat gambar) | `chart.applyOptions({handleScroll:false, handleScale:false})` selama alat gambar aktif ATAU sedang menyeret anchor gambar lama; dipulihkan APA ADANYA (bukan konstanta) saat keduanya lepas — lewat jalur selesai-normal, Escape (dua tahap), DAN klik-kanan (`contextmenu`, `preventDefault` menu bawaan peramban) | Tiga jalur keluar, bukan satu — jalur yang terlewat meninggalkan chart terkunci pan mati selamanya, persis peringatan koordinator soal "chart hang" | Selesai — `chart.options().handleScroll/handleScale` dibaca langsung dari devtools sebelum/sesudah tiap transisi (pilih alat → mati; Kursor/Escape/klik-kanan → pulih PERSIS termasuk `vertTouchDrag:false` yang sudah ada sebelumnya). Klik trusted (bukan event sintetis — LWC tak mengenalinya) menggambar Garis Horizontal, sumbu harga kanan identik sebelum/sesudah | Fixed |
| 164 | Bug KETIGA, ditemukan saat menambal #163: `chart.options()` mengembalikan referensi HIDUP | (ditemukan sendiri, bukan dari laporan) | /grafik | `useAlatGambar.ts:118` (`structuredClone`) | Kode pemulihan menangkap `chart.options().handleScroll` lalu langsung `applyOptions({handleScroll:false})` sesudahnya — panggilan KEDUA memutasi objek yang SAMA dengan yang baru ditangkap, jadi "nilai asli" yang disimpan ikut jadi `false` juga | `panAsliRef.current = structuredClone({...})` — memutus tautan ke state internal lightweight-charts | Ketemu lewat log `chart.options()` sebelum/sesudah tiap panggilan (bukan dari membaca kode — kodenya terlihat benar sekilas: tangkap-lalu-terapkan itu pola yang wajar). Tanpa tambalan ini, chart TERKUNCI pan mati selamanya sesudah alat gambar pertama kali dipakai — sekali, bukan tiap kali | Selesai — verifikasi sama dengan #163 (nilai pulih persis, termasuk sub-ruas yang tak pernah disentuh kode ini) | Fixed |
| 165 | Gambar tersimpan tak terlihat sesudah muat ulang (ditemukan saat verifikasi persistensi, bukan dari laporan bug) | (batas tugas — "buktikan gambar bertahan setelah muat ulang") | /grafik | `useAlatGambar.ts` (efek baru: `bacaGambarTersimpan(kode).length > 0` → `mintaPustaka()`) | Pustaka gambar (2,7 MB) cuma dimuat saat bilah disentuh (impor dinamis #185) — muat ulang halaman meninggalkan gambar tersimpan TAK TERGAMBAR sampai pembaca kebetulan menyentuh bilah, dan dari layar itu terbaca "gambarnya hilang" padahal datanya utuh di localStorage | Efek baru memeriksa `bacaGambarTersimpan(kode)` (murah — satu `localStorage.getItem`, BUKAN impor pustaka) di tiap ganti `kode`; kalau ada isinya, pustaka dimuat otomatis tanpa menunggu sentuhan bilah | Persis kegagalan senyap yang dilarang CLAUDE.md — data tak hilang, cuma tak digambar ulang, dan bedanya tak terlihat dari layar | Selesai — muat ulang: garis BBCA tetap ada. Ganti ke TLKM: tak ada garis (isolasi per-emiten utuh). Balik ke BBCA: garis ada lagi. Telepon 412×915×2,625: bilah terlipat jadi satu tombol pensil, garis tetap tampil di kanvas penuh-lebar | Added |

**Verifikasi peramban** — chrome-devtools MCP, `localhost:5173/grafik?kode=BBCA`, devtools dipakai bergantian dengan beberapa agen lain (dikonfirmasi lewat `select_page` eksplisit tiap tangkapan, sesuai arahan koordinator). Tiga bukti di atas + `npx tsc -b --force` bersih + `npx vitest run` 640/642 hijau (dua yang merah ada di `tanyaPapan.test.ts`, pekerjaan agen lain yang sedang berjalan di berkas yang eksplisit di luar lingkup tugas ini — dikonfirmasi lewat `git status` menunjukkan `tanyaPapan.ts` punya 33 baris perubahan tak ter-commit sebelum sesi ini dimulai). Commit `42060da9`.

---

## Kartu Analisa Emiten hidup di /kartu (18 Agu 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 166 | Kartu Analisa Emiten dari HTML statis jadi halaman live, 3 emiten | *"soal kartu ini bisa saya lihat live dimana ? buatkan 3 yang sudah live, di page apa ?"* | `/kartu` (baru), kunci akses `kta`, rail `KTA`, grup `analisa` | `scripts/riset/kartu_analisa.py` (+`--tulis`/`--semua`/`--maks`, +`asing_ringkas`/`ringkas_asing_dari`) → `data-idx/json/kartu/{ARCI,BUMI,WIFI,index}.json`; `app/src/lib/dasbor/kartuAnalisa.ts` (baru, tipe + pemuat + `bangunTesis`/`pembatalDalamAtr`) + `.test.ts` (baru); `app/src/views/dasbor/KartuAnalisa.tsx`+`.css` (baru); `app/src/App.tsx`, `menu.ts`, `aksesHalaman.ts`, `prefetchRute.ts` (route/menu/akses/prefetch); `app/src/dasbor/lantai.css` (+primitif generik `.blok`/`.baris`/`.lvl`/`.asal`/`.tesis`/`.catat`/`.tp-tbl`) | `docs/riset/kartu-analisa.html` — purwarupa ketik tangan, `kartu_analisa.py` sudah menghitung semuanya tapi cuma cetak ke terminal, tak ada jalur ke aplikasi | Rute `/kartu` publik (kunci `kta` fail-open, belum ada baris di `akses_halaman` — pola sama `statistik`), dropdown pilih emiten (kanonis `Dropdown`) baca `index.json`, `?kode=` dihormati. Tiap kartu: struktur harga, level S/R + n sentuhan, karakter emiten (ER + persentil), musiman, **aliran asing** (net 5h/20h + porsi thd volume pasar, LEMBAR — lihat #167), fundamental ringkas, tabel skenario first-passage, tesis (Yang terbaca/membatalkan/perlu diingat) DIRAKIT dari data lewat `bangunTesis()` — bukan diketik per emiten, supaya berlaku untuk emiten mana pun di `index.json` | Brief eksplisit melarang skor tunggal, bahasa ajakan, dan jalur berkas/nama fungsi di layar (diverifikasi lewat pass kebocoran, lihat bukti). Peringatan "pembatal di dalam 1 ATR" (kasus WIFI) WAJIB dihitung dari data, bukan diketik untuk satu emiten — `pembatalDalamAtr()` murni, diuji dengan angka WIFI (muncul) & BUMI (tidak) | Selesai. Angka ARCI/WIFI/BUMI dicocokkan MANUAL ke `docs/riset/kartu-analisa.html` — semua cocok persis (ARCI close 1.320 ATR14 69 ER 0,231/P77, S1 1.080/5 sentuhan, R1 first-passage 18,3%/14,7%/67,0%/harapan +2,05%; WIFI pembatal 3,30% < ATR 4,57%, R1 stop-dulu 51,7%; BUMI pembatal 14,67% vs ATR 5,04%, peringatan TIDAK muncul). `npx tsc -b --force` bersih (satu galat `__probe.test.ts` tak terkait, berkas untracked agen lain). `npx vitest run` 642 hijau (+7 baru). `npx oxlint` 0 galat dari berkas ini. Tiga viewport chrome-devtools (1920×1080×1, 1536×960×1.25, 412×915×2.625 mobile+touch) — 0px luber mendatar ketiganya, 0 galat konsol; browser dipakai bergantian ≥4 agen lain (tab sendiri berpindah URL sendiri berkali-kali, diatasi bikin tab baru + `select_page` eksplisit tiap giliran, ditutup sesudah). Pass kebocoran: `.json`/`data-idx`/`/src/`/`scripts/`/nama fungsi/`undefined`/`NaN`/`null` — nol kemunculan di teks yang benar-benar tercetak. **Insiden commit**: staged lewat `git add` satu-per-satu (bukan `-A`), TAPI antara `git add` dan `git commit` sesi ini, agen lain menjalankan commit-nya sendiri dan turut menyertakan staging area yang sudah saya isi — hasilnya seluruh berkas tugas ini masuk commit `93467a6c` berjudul **"fix(panen): buang Playwright mati dari update.yml, tabel status berhenti menipu"** (bukan punya saya). Isinya utuh & diverifikasi (`git show --stat 93467a6c` menunjukkan seluruh 13 berkas tugas ini), tapi pesannya bohong — sama persis pola yang diperingatkan CLAUDE.md, kali ini korban bukan pelaku. Tidak di-amend (aturan: jangan menimpa riwayat), dilaporkan di sini | Added |
| 167 | Blok Aliran Asing di kartu — koreksi brief pertengahan sesi | *"Blok 'Aliran asing berisi angka' DILARANG"* (brief awal) → dikoreksi koordinator: *"Itu sudah tidak berlaku. Panennya selesai beberapa menit lalu (commit `5793317d`)"* | `/kartu`, blok "Aliran Asing" tiap kartu | `kartu_analisa.py::asing_ringkas()`/`ringkas_asing_dari()`, `KartuAnalisa.tsx::BlokAsing` | Rencana awal: blok kosong "belum tersedia" untuk seluruh emiten | Net 5h/20h + beli/jual TERPISAH (bukan cuma net) + porsi dari volume pasar, satuan LEMBAR dibaca dari berkas (`satuan.beli`), bukan diasumsikan rupiah. Emiten tanpa berkas asing → `asing: null` di JSON → tampil "Belum tersedia", bukan 0 | Koreksi eksplisit koordinator: satuan lembar bukan rupiah (mengarang taksiran rupiah = "kebohongan yang terlihat presisi"), net saja dilarang (menyembunyikan beda sepi-dua-arah vs ramai-dua-arah), berkas kosong wajib berbunyi "belum tersedia" bukan 0 | Selesai — dihitung di Python (bukan TS, satu sumber kebenaran), diverifikasi manual: ARCI net 5h -4.764.400 lembar, WIFI +9.885.000, BUMI -154.801.400, porsi beli/jual thd volume pasar 20h masing-masing dicetak & dicocokkan ke output terminal `cetak()` | Added |

**Yang TIDAK selesai**: mode `--semua` (batch ratusan/ribuan emiten) sengaja belum dijalankan (sesuai brief — "JANGAN dijalankan sekarang"); baris `akses_halaman` untuk kunci `kta` belum dibuat di database (halaman publik sampai Johan mengatur tingkatnya dari tab Akses, pola sama `statistik`); Papan Pekerjaan ini ditulis SESUDAH insiden commit di atas, jadi entrinya menunjuk `93467a6c`, bukan commit tersendiri.

---

## Tanya PAPAN — mesin aturan diuji di LUAR bentuk basis pengetahuannya (18 Agu 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 168 | Korpus baru yang TIDAK diturunkan dari glosarium — 143 pertanyaan, empat set terpisah | *"lalu untuk rule engine sudah di ajari habis-habisan ? jgn setengah matang"* | /tanya-papan (mesin `jawab()`) | `tanyaPapan.test.ts` (`KORPUS_LUAR`, 79 kasus permanen) | Korpus ronde pertama diturunkan dari `glosarium.json` + `pengetahuan.ts`, jadi cuma menguji pertanyaan yang bentuknya sudah mirip isi basis pengetahuan — dan angkanya 112/112 hijau | Enam sudut baru: pemicu nyangkut di kata lain, ruas/periode/satuan keliru, bertingkat & bersyarat, susulan ganti-ruas/ganti-emiten, di luar jangkauan, bahasa informal | Korpus yang hijau sempurna pada bentuk yang dipakai menyetelnya tidak mengukur apa pun. Ukuran pertama korpus baru: **32 dari 58 dijawab SALAH SASARAN** — percaya diri, berangka, bukan jawaban pertanyaannya | Selesai — set setelan 58: 14 benar/12 tak tahu/**32 salah** → 50/6/**2**. Tiga set tahan-simpan berurutan yang tak dipakai menyetel: 43% → 30% → **20%** salah sasaran pada paparan pertama | Added |
| 169 | Pemicu pendek nyangkut di dalam kata lain — `punya()` dari `includes` jadi batas-awal-kata | (turunan #168) | semua blok | `tanyaPapan.ts` (`punya`) | `t.includes(k)` — "asing" kena di "m**asing**-masing", "siapa" di "per**siapa**n", "buka" di "pem**buka**an", "leader" di "**leader**board" | `new RegExp('\\b' + kata)` — batas AWAL saja, supaya "sektor**nya**"/"gainer**s**" tetap kena | Batas dua sisi memutus toleransi akhiran bahasa Indonesia; batas awal menutup jebakannya tanpa itu | Selesai — "berapa PER masing-masing sektor hari ini" dulu dijawab *"Asing net sell Rp1,03 triliun"* | Fixed |
| 170 | Kode saham yang disebut lebih dari satu tak lagi didiamkan | (turunan #168) | semua blok per-emiten | `tanyaPapan.ts` (`kodeDisebut()`, pembungkus `jawab()`) | `match(/\b[A-Z]{4}\b/)` — hanya yang PERTAMA; "harga BBCA sama BBRI berapa" dijawab BBCA saja tanpa penanda | Semua kode dikumpulkan; jawaban tetap satu emiten tapi membawa catatan *"(untuk BBRI tanyakan terpisah)"*; pertanyaan pembanding dijawab terus terang belum bisa | Diam soal emiten kedua membuat pembaca menyimpulkan emiten itu tak punya data | Selesai — catatan ditempel di pembungkus, bukan di belasan cabang (yang berarti ada yang terlewat sejak hari pertama) | Fixed |
| 171 | Ruas per emiten yang tak dihitung: dikatakan, bukan ditutupi profil harian | (turunan #168) | /tanya-papan | `tanyaPapan.ts` (`RUAS_BELUM`) | Blok "emiten disebut langsung" keranjang untuk SETIAP kalimat bermuatan kode — "volume BBCA", "EPS BBCA", "market cap BBCA", "dividen BBCA" semuanya dijawab *"naik +1,10% … peringkat 1 nilai transaksi terbesar"* | Tujuh keluarga ruas (volume/frekuensi, kapitalisasi, laporan keuangan, dividen, nilai transaksi, bid/offer, biaya & pajak) dijawab: belum dihitung di sini, ini yang ada, selebihnya di Stock Detail | Satu keranjang menjawab tujuh pertanyaan berbeda dengan kalimat yang sama, dan tak satu pun ruas yang ditanya ada di dalamnya | Selesai — 8 baris korpus, semuanya merah sebelum tambalan | Fixed |
| 172 | Periode diperiksa: masa depan ditolak, tanggal lain tak dikarang | (turunan #168) | /tanya-papan | `tanyaPapan.ts` (`MASA_DEPAN`, `MASA_LALU`, `REKOR_PENUH`) | "IHSG besok naik atau turun", "kapan IHSG naik lagi", "sebulan lagi IHSG berapa" dijawab ringkasan HARI INI; "harga BBCA bulan lalu" dijawab harga terakhir; "IHSG pekan lalu ditutup di berapa" dijawab persentase sepekan | Masa depan: ditolak terus terang (tak ada prakiraan). Tanggal lalu tertentu: menunjuk Chart/Stock Detail, tak menyodorkan angka hari berjalan | Jawaban prakiraan yang terdengar meyakinkan adalah kegagalan paling berbahaya di produk ini; angka hari ini untuk pertanyaan bulan lalu bentuk yang sama, cuma lebih halus | Selesai — 9 baris korpus | Fixed |
| 173 | Pertanyaan kapabilitas dijawab ya/tidak berikut batasnya | (turunan #168) | /tanya-papan | `tanyaPapan.ts` (`TANYA_KAPABILITAS`, `ABSEN`) | *"apakah PAPAN punya data intraday"* dijawab profil PAPAN — yang tak memuat kata "intraday" sama sekali | Tiga yang memang tak ada (intraday/real-time, notifikasi/alert, instrumen non-saham) dijawab "belum ada" + batasnya; sisanya jatuh ke entri batas-kemampuan | Brosur bukan jawaban untuk pertanyaan ya/tidak; penanya terpaksa menyimpulkan sendiri | Selesai | Fixed |
| 174 | Susulan boleh mengganti RUAS atau EMITEN | (turunan #168) | /tanya-papan | `tanyaPapan.ts` (`RUAS_SUSULAN`, `gantiSubjek`) | *"sektornya?"* sesudah "harga BBCA" dijawab sektor SE-PASAR; *"kalau BBRI?"* dijawab profil peringkat BBRI, bukan ruas yang sedang dibahas | Dua bentuk susulan itu dirakit ulang jadi pertanyaan penuh, sama seperti "berapa?"/"kenapa?" | Keduanya bentuk paling wajar diketik sesudah satu jawaban per-emiten, dan dua-duanya meleset diam-diam | Selesai — 7 baris korpus susulan | Fixed |
| 175 | `frksi` dikenali TANPA melonggarkan ambang `ara`/`arb` | *"frksi tak dikenali"* (sisa ronde pertama) | semua pencocokan teks | `teksTanya.ts` (`vokalHilang()`, `mirip()`) | Ambang panjang-kata menolak semua salah ketik di bawah 6 huruf — keputusan yang benar (ara/arb berjarak satu huruf, artinya berlawanan) tapi menutup "frksi"→"fraksi" juga | Jalur terpisah yang cuma mengizinkan PENGHAPUSAN VOKAL: kerangka konsonan wajib sama persis dan yang pendek wajib bisa diperoleh dengan membuang huruf. `ara`/`arb` tetap tak bisa cocok (kerangka "r" lawan "rb") | Melonggarkan ambang akan mengorbankan pasangan berbahaya; menambah jalur yang tak pernah menyubstitusi tidak | Selesai — `frksi harga berapa` kini menjawab fraksi harga; uji `ara`/`arb` lama tetap hijau | Fixed |
| 176 | Dua substitusi pada kata panjang melompat ke kata lain yang sah | (ditemukan di set tahan-simpan, bukan dari laporan) | semua pencocokan teks | `teksTanya.ts` (`mirip()`) | Ambang 2 edit untuk kata ≥8 huruf: "pem**buka**an" dan "pem**beku**an" berjarak tepat dua substitusi — *"apa arti pembukaan dan penutupan bursa"* dijawab aturan **pembekuan akun kontributor** | Dua edit hanya diizinkan kalau panjangnya BERBEDA (ada huruf hilang/tertambah); panjang sama ⇒ maksimal satu substitusi | Dua substitusi cukup untuk berpindah ke kata lain yang sah — jenis kesalahan yang sama dengan ara/arb, cuma di kata panjang | Selesai — "divergen"→"divergensi" (beda panjang) tetap jalan | Fixed |
| 177 | Huruf besar semua mematahkan deteksi ticker | (ditemukan di set tahan-simpan) | /tanya-papan | `tanyaPapan.ts` (`kodeDisebut()`) | "diketik kapital" dipakai sebagai penanda ticker — pada kalimat yang SELURUHNYA kapital tiap kata empat huruf ikut terbaca kode: *"SEKTOR APA YANG PALING LEMAH???"* dijawab **"Data sektor YANG belum ada"** | Kalau tak ada satu pun huruf kecil, kandidat wajib benar-benar ada di daftar emiten; kalau daftarnya belum termuat, tak ada kode sama sekali | Lebih baik tanpa kode daripada kode karangan | Selesai | Fixed |
| 178 | `\bper\b` masih dua arti: rasio PER vs kata depan "per" | (ditemukan di set tahan-simpan ketiga) | /tanya-papan | `tanyaPapan.ts` (`RASIO_PER`) | Ronde pertama sudah menyempitkan `includes('per')` jadi kata utuh — ternyata kata utuhnya sendiri masih dua arti: *"apakah ada data broker **per emiten** untuk semua saham"* dijawab **"PER pasar 14,20×"** | Kata depan dikecualikan lewat lookahead (per emiten/saham/hari/lembar/tanggal/…) | "per emiten" justru salah satu frasa paling lazim di pertanyaan pasar ini | Selesai — kini dijawab entri cakupan broker summary | Fixed |
| 179 | Sapuan kebocoran diperlebar ke SELURUH korpus, bukan cuma basis teks | Aturan CLAUDE.md 18 Agu 2026 #4 (*"satu pass khusus kebocoran"*) | /tanya-papan | `tanyaPapan.ts` (`saring()`, `RUAS_INTERNAL`); `tanyaPapan.test.ts` | Sapuan ronde pertama hanya melewati glosarium & pengetahuan | Seluruh 143 pertanyaan disapu; kalimat yang menyebut nama ruas internal dibuang UTUH (menghapus katanya saja meninggalkan kalimat cacat) | Menemukan kebocoran baru: catatan glosarium "arus kas" mencetak **`operating_cf`** — nama kolom berkas, tak berarti apa pun bagi pembaca | Selesai — sapuan `.py`/`.ts`/`.json`/`/src/`/`scripts/`/`lib/`/`data-idx`/`GetStockSummary`/`GetFinancialReport`/`GetBrokerSummary`/`yfinance`/URL/localhost/supabase atas 143 pertanyaan: nihil. ("XBRL" sengaja TIDAK dianggap bocor — istilah publik, disebut sadar di entri sumber data) | Fixed |

**Verifikasi**: `npx tsc -b --force` bersih, `npx vitest run` **723 hijau** (dari 623 di ronde pertama; +79 kasus korpus baru + sapuan kebocoran diperlebar), `npx oxlint src/` 0 galat. Devtools tak dipakai — ini logika murni, tak menyentuh tampilan.

**Yang masih gagal, dan alasannya dibiarkan**:

| Pertanyaan | Dijawab | Kenapa dibiarkan |
|---|---|---|
| `brp harga bbca`, `bbca gmn hari ini` | tawaran cabang "harga", atau "belum bisa saya jawab" | Kode huruf kecil sengaja TIDAK dideteksi. Banyak kata Indonesia sehari-hari adalah ticker sah (`DATA`, `BAIK`, `RAJA`, `MASA`), jadi menerima huruf kecil berarti "harga **data** historis" dibaca sebagai emiten DATA. Jawaban sekarang jujur DAN mengajari bentuk yang dikenali |
| `sektr apa yg menguat` | "belum bisa saya jawab" | Toleransi salah ketik hidup di pencarian glosarium/pengetahuan, bukan di pemicu blok data. Menyalakannya di blok data berarti tiap pemicu jadi pencocokan kabur — persis yang melahirkan kelas bug #175/#176 |
| `kenapa PER masing-masing emiten beda jauh` | PER pasar | Pertanyaan "kenapa" konseptual; menyaringnya menuntut membedakan "kenapa" penjelasan dari "kenapa" data, dan tebakan yang salah arah akan memotong pertanyaan sah |
| `saham naik turun itu wajar tidak` | daftar top gainers | Frasa "saham naik" memang pemicu sah blok gainers; kalimatnya betul-betul ambigu |
| `IHSG dibandingkan bursa Malaysia gimana` | ringkasan IHSG | Halaman Indeks Dunia memang memuat indeks dunia, jadi menolak mentah juga salah. Perlu keputusan isi (indeks mana yang boleh dibandingkan), bukan tambalan regex |
| `arus pasar modal Indonesia bagaimana` | "Belum ada edisi terbit" | "Arus Pasar" nama terbitan PAPAN sendiri; membedakannya dari "arus pasar modal" menuntut daftar pengecualian yang akan basi |
| `eps nya brp` (tanpa emiten) | definisi PER/PBV dari glosarium | Tanpa subjek, definisi adalah jawaban yang masuk akal — tapi penanya meminta angka. Menunggu ingatan subjek diperluas ke ruas keuangan |

---

## Jumlah saham resmi bursa — 51 emiten yang ruas per-sahamnya salah diam-diam (18 Agu 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 180 | Sumber `shares` yang andal untuk emiten yang `sharesOutstanding`-nya ketinggalan aksi korporasi | *"cari sumber `shares` yang lebih andal untuk 38 emiten ini — kandidat: `GetCompanyProfiles` IDX, atau field lain dari yfinance `info` (mis. `impliedSharesOutstanding`), atau derivasi dari `market_cap/last_price` sendiri sebagai fallback ketika rasio di luar batas wajar. Verifikasi dengan sumber independen (situs IDX resmi/RTI) sebelum menulis"* | Stock Detail, Bedah Emiten, semua ruas per-saham | `sinkron_emiten.py` (`ambil_daftar_resmi`), `fetch_fundamental.py` (`saham_idx`, blok `shares`) | `shares` = `sharesOutstanding` yfinance apa adanya. BBNI tersimpan 578.683.733 padahal resmi 36.924.339.786 (0,0157×); MSKY sebaliknya 5,0× KEBESARAN karena reverse split | `ListedShares` dari `GetStockSummary` IDX, disimpan sebagai `saham` di `daftar_emiten.json`. Dipakai kalau bedanya >2×; di bawah itu yfinance tetap menang (treasury/waktu potret, 101 emiten, wajar) | Endpoint yang dipakai `sinkron_emiten.py` SUDAH membawa `ListedShares` di payload yang sama — selama ini dibuang. Nol permintaan jaringan tambahan, dan sumbernya bursa itu sendiri | Selesai — **51 emiten** dikoreksi (bukan 38: `market_cap/harga` sebagai wasit melewatkan NEST, RIMO, IIKP, PIPA, MORA, MSKY, dan 8 lagi yang `shares`-nya kosong sama sekali). Sapuan akhir: 0 emiten beda >2× vs IDX. `npx tsc -b` bersih, `npx vitest run` 734 hijau | Fixed |
| 181 | Kandidat yang DITOLAK: `market_cap ÷ last_price` sebagai sumber | (turunan #180) | — | — | Usul di perintah: pakai `market_cap/last_price` sebagai fallback | Ditolak sebagai SUMBER; tetap dipakai sebagai detektor di `shares_masuk_akal()` | `market_cap` Yahoo dihitung dari `sharesOutstanding` versi Yahoo juga, jadi ikut basi di emiten yang sama. Terukur salah di 4 dari 51: AISA (tersirat 3,81 miliar, resmi 9,31 miliar), LPPF (1,17 vs 2,23 miliar), ZBRA (534 juta vs 2,51 miliar), MSKY | Selesai — pembanding independen: EPS laporan XBRL IDX (`net_income ÷ eps`) dan ekuitas/nilai buku. Ketiganya sepakat di 9,31 miliar untuk AISA, melawan `market_cap` sendirian | Fixed |
| 182 | `shares_masuk_akal()` dibuat mengalah pada tanda `shares_sumber` | (turunan #180) | tabel EPS Kuartalan | `lengkapi_fundamental.py` (`shares_masuk_akal`) | Guard #A0c menolak `shares` yang tak cocok `market_cap/harga` | Ruas baru `shares_sumber: "idx"` melewati guard | Tanpa ini AISA/LPPF/ZBRA tetap tanpa `q_eps` SESUDAH `shares`-nya dibetulkan — guard-nya justru mempercayai ruas yang basi | Selesai — uji baru `4f-bis` di `_uji()`, kasus AISA dihitung tangan (5.660.764.000.000 ÷ 9.311.800.000 = 607,91) | Fixed |
| 183 | `market_cap` dan `float_pct` ikut dikoreksi — kalau tidak, berkasnya bertengkar dengan dirinya sendiri | (turunan #180, ditemukan di sapuan regresi) | Stock Detail | `fetch_fundamental.py` (blok `mcap`, `float_pct`) | `market_cap` = `marketCap` Yahoo; `float_pct` = `floatShares ÷ shares` | `market_cap` = saham tercatat × harga (definisi bursa) untuk emiten ber-`shares_sumber = "idx"`. `float_pct` mencoba DUA basis (saham Yahoo lalu saham IDX) dan memakai yang jatuh di 0–100% | Membetulkan `shares` saja membuat `float_pct` BBNI terbaca 0,61% (dulu 38,84%) — regresi yang saya sendiri buat. `floatShares` Yahoo ternyata tak selalu sebasis `sharesOutstanding`-nya: sepasang di BBNI, sudah pakai saham terkini di AISA. Satu rumus skala tetap justru memburukkan (AISA sempat 1.669%) | Selesai — sapuan akhir: 0 emiten ber-`float_pct` di luar 0–100%, 0 emiten ber-`market_cap` beda >2× dari saham IDX × harga | Fixed |

**Verifikasi**: `npx tsc -b` bersih · `npx vitest run` **734 hijau** · `scripts/lengkapi_fundamental.py --uji` LOLOS · sapuan konsistensi 965 berkas (`shares` 0 · `float_pct` 0 · `market_cap` 0 pelanggaran di antara 51 yang disentuh).

**Yang TIDAK diperbaiki, dan alasannya**:

| Temuan | Jumlah | Kenapa dibiarkan |
|---|---|---|
| `market_cap` kosong/0 di emiten disuspensi (ARMY, BTEL, COWL, HOME, MABA, MTRA, NUSA, OCAP, POOL, SIMA, SKYB, SMRU, TRAM) | 13 | Sudah begitu sebelum sesi ini, bukan regresi. Yahoo tak memberi `marketCap` untuk saham yang tak diperdagangkan; menghitungnya dari harga terakhir yang sudah lama beku memberi angka yang terlihat hidup padahal bukan |
| `float_shares` tak masuk akal di emiten yang tak disentuh (BACH, DATA, DUTI, FASW, IBST, SUPR, ZONE) | 7 | Kualitas data Yahoo, `shares`-nya sendiri sudah cocok dengan IDX. Di luar lingkup perintah ini |
| CNTB, CNTX, NIPS tak ada di `GetStockSummary` | 3 | Disuspensi — tak muncul di ringkasan perdagangan hari mana pun. `saham_idx()` mengembalikan None dan `shares` yfinance dibiarkan apa adanya; guard `shares_masuk_akal()` tetap memblokir `q_eps` mereka |
| `q_eps` MDKA & MSKY masih >20× `eps` tahunan | 2 | `eps` (trailingEps Yahoo) yang basi, bukan `shares`. Akar berbeda, perlu pemeriksaan sendiri |

---

## B3 — kuartal DISKRET & lubang Q4 (19 Agustus 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 184 | Turunkan kuartal diskret atas data asli | Johan, 18 Agu: *"diskret saja biar bisa dibandingkan antar kuartal"* | — (lapisan data) | `scripts/turunkan_kuartal_diskret.py` | skrip lolos swauji, **belum pernah jalan atas data asli** | 949 tiker → `data-idx/json/keuangan_idx_diskret/`, 9.665 periode | Interim IDX itu KUMULATIF; tanpa diturunkan, kuartal tak bisa dibandingkan satu sama lain | Selesai — swauji lolos; jumlah Q1..Q4 vs audit **1.413 pasangan, 0 melenceng >5%**; banding ke yfinance median rasio **1,000**, 98,9% dalam ±25%, cuma 2 dari 2.532 yang ~2× (jebakan kumulatif tertutup) | — |
| 185 | Tutup lubang Q4 | Tugas B3: *"Q4 — bucket `tahunan` (audit) tak pernah disilangkan ke `kuartal`"* | Stock Detail → panel Laporan Keuangan, tab Kuartalan | `app/src/lib/dasbor/fundamentalGabungan.ts` (`bacaKuartalIdx`, `isoQ4Idx`), `app/src/views/dasbor/stock-detail/PanelLaporanKeuangan.tsx` (daftar periode) | kolom Q4 selalu jatuh ke Yahoo — IDX tak punya kunci 12-31 di bucket `kuartal` (0 dari 949 berkas) | Q4 = auditan setahun − TW3, berlencana `B`; kunci Q4 yang layak disuntikkan ke daftar periode | Ditutup di tempat rumusnya sudah hidup, bukan dengan menyambungkan berkas diskret: berkasnya harus di-fetch, di-sinkronkan, dan bisa basi terhadap sumbernya — sementara di `bacaKuartalIdx` yang kurang cuma bucket `tahunan` ikut masuk | Selesai — `npx tsc -b --force` bersih di luar `GrafikEmiten.tsx`/`KartuAnalisa.tsx` (agen lain), `npx vitest run` **745 hijau** (737 + 8 baru); TLKM/ASII/ICBP/UNVR Q4 hasil turunan **sama persis** dengan kuartal diskret yfinance; ALKA (tanpa berkas yfinance) mendapat kolom Q4 yang sebelumnya tak ada. **Verifikasi 2 viewport TERHALANG** — chrome-devtools terkunci sesi lain (PID 34120) | — |
| 186 | Kontrak Q4-tanpa-TW3 diputuskan `null`, bukan kumulatif | Tugas B3: *"kalau kamu menyambungkannya, jangan diam-diam mengubah salah satu perilaku itu"* | idem | `fundamentalGabungan.ts` (`bacaKuartalIdx`, cabang `pengurang == null`) | — (Q4 tak pernah dihitung) | Q2/Q3 tanpa pengurang **tetap** kumulatif berlencana `B·YTD` (tak disentuh); Q4 tanpa TW3 → `null`, Yahoo yang mengisi | Tanpa TW3, sisa Q4 itu angka SETAHUN PENUH — di kolom kuartal ia salah besar, bukan kurang tepat, dan angka setahun sudah punya rumahnya di tab Tahunan. 82 emiten akan kena kasus ini | Selesai — diuji eksplisit: `bacaKuartalIdx({}, 'revenue', '2025-12-31', tahunan)` → `{nilai: null, asal: null}`, sementara kasus Q2/Q3 lama tetap `idx-kumulatif` | — |

**Gerbang verifikasi — apa adanya**

| Gerbang | Hasil | Catatan jujur |
|---|---|---|
| Jumlah 4 kuartal ≈ tahunan auditan (ambang 5%, ruas laba) | 1.413 pasangan emiten-tahun diperiksa, **0 melanggar** | Ini **tautologi aritmetika**: Q1+(TW2−TW1)+(TW3−TW2)+(audit−TW3) = audit menurut definisi. Lolosnya membuktikan tak ada kesalahan hitung/pembulatan, **bukan** bahwa asumsi kumulatifnya benar. Yang membuktikan itu gerbang ketiga |
| Nol revenue diskret negatif | **15 dari 4.318** terisi (0,35%) — GAGAL, tak dipangkas | Semuanya cacat SUMBER: 13 karena kumulatif TURUN (restatement/penyesuaian audit), 2 karena TW1-nya sudah negatif di XBRL. Terparah **CDIA** (berkas USD, TW3 2025 tercatat 64,5 T sementara audit 148 jt — satu periode ikut skala rupiah) |
| Orde besaran vs `keuangan/` (yfinance, sudah diskret) | **LOLOS telak** — revenue median 1,000 (n=2.532), net_income median 1,000 (n=2.925), 98,5–98,9% dalam ±25%, rasio ~2× tinggal 3 dari 5.457 | Ini gerbang yang sungguh menguji asumsinya. TLKM/ASII/ICBP/UNVR Q4 cocok **sampai digit terakhir** dengan kuartal diskret yfinance yang dihitung independen |

---

## B2 — Compare symbols & Bar replay di Grafik Emiten (19 Agustus 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 187 | **Compare symbols** — tumpuk 1–3 emiten/indeks pembanding | Johan, sesudah membuka TradingView: *"Compare symbols, Bar replay penting juga ini"* | `/grafik` (Grafik Emiten) | `app/src/views/dasbor/GrafikEmiten.tsx` (`MAKS_BANDING`, `WARNA_BANDING`, `KODE_IHSG`, state `banding`/`dataBanding`/`basisPersen`, efek unduh + efek seri pembanding, memo `opsiBanding`/`bandingLegenda`), `app/src/lib/dasbor/grafikEmiten.ts` (`tutupSampai`), `app/src/dasbor/lantai.css` (token `--bnd1/2/3` dua tema), `GrafikEmiten.css` (`.grf-banding-*`) | Tiap kenaikan terbaca sebagai prestasi emitennya — tak ada apa pun di kanvas yang bisa menjawab "naiknya karena emitennya atau karena pasarnya" | Dropdown **+ Banding** di bilah atas (IHSG di kelompok sendiri, lalu 963 emiten); garis pembanding di panel harga; legenda dalam-kanvas menyebut % tiap baris **plus emiten utamanya sendiri**, dan menyebut TANGGAL basis normalisasinya | Skala **dikunci persen**, bukan disarankan: menumpuk BBCA (6.300) dan INET (288) di sumbu rupiah membuat yang kecil jadi garis rata di dasar kanvas — perbandingannya bukan sulit dibaca, ia salah. Basisnya bergeser sendiri saat sumbu digeser, jadi "+18%" tanpa tanggal basis adalah angka yang tak bisa ditafsirkan dan tetap terlihat masuk akal | Kode selesai — `npx tsc -b --force` bersih, `npx vitest run` **754 hijau** (745 + 9 baru), `npx oxlint` **0 galat**, `npm run build` jadi. **Verifikasi 3 viewport TERHALANG** — chrome-devtools MCP terkunci sesi lain (PID 34120, tab "SAKTI"), `isolatedContext` juga ditolak (kuncinya di level `userDataDir`, bukan konteks) | Added |
| 188 | **Bar replay** — mundurkan chart, maju selilin per klik, putar otomatis | idem | `/grafik` | `GrafikEmiten.tsx` (state `replay`/`putar`/`kecepatan`, memo `penuh` → `{lilin,volume}`, efek putar otomatis, bilah `.grf-replay`), `components/dasbor/IkonMenu.tsx` (`IKON_PUTAR`, `IKON_JEDA`) | Semua alat di kanvas dibaca SESUDAH hasilnya terlihat — tak ada satu pun yang menguji pembacaan tanpa sudah tahu jawabannya | Tombol `IKON_ULANG` di bilah atas memundurkan ke 70% deret; bilah bawah berisi DatePicker, panah maju/mundur selilin (`LangkahTanggal`), putar/jeda, keluar, dan chip kecepatan (`PemilihRentang`, 1–10 lilin/dtk) | **Potongannya di HULU**, satu memo, sebelum indikator/pola/penanda/legenda/garis pembanding dihitung. Memotong di hilir membuat MA 20 tetap dihitung dari data penuh sementara lilinnya mundur — seluruh guna replay hilang **tanpa satu pun galat**, dan hasilnya tetap terlihat wajar di layar | Selesai (kode) — 4 uji baru mengunci kausalitas: MA/EMA/RSI/MACD/BB/OBV pada deret dipotong di indeks 150 **identik** dengan nilai indeks itu pada deret penuh 220 titik; MA 200 pada potongan 150 titik tetap `null` walau pada data penuh sudah ada; tak satu pun temuan Double Bottom / Lonjakan Volume / tanggal Musiman bertanggal sesudah batas replay | Added |
| 189 | Titik pembanding dipasang pada waktu lilin utama, bukan tanggal hariannya sendiri | (bagian dari #187) | `/grafik` | `GrafikEmiten.tsx` (efek seri pembanding), `grafikEmiten.ts` (`tutupSampai`) | — | Untuk tiap lilin utama, nilai pembanding = tutup terakhir ≤ waktu lilin itu (pencarian biner) | Seri dengan waktu di luar waktu lilin utama **menambah titik ke sumbu waktu chart**: pada kerangka pekanan, lilin mingguan jadi tersebar renggang di antara ~4 tanggal harian kosong milik pembandingnya. Dan pencocokan persis tak cukup — kunci lilin pekanan jatuh di SENIN, bulanan di TANGGAL 1, keduanya kerap libur bursa | Selesai — 5 uji `tutupSampai` (cocok persis, tanggal tanpa lilin, sesudah lilin terakhir, sebelum lilin pertama → `null`, deret kosong → `null`) | Added |
| 190 | Kerangka intraday tak dapat pembanding — dan mengatakannya | (bagian dari #187) | `/grafik` | `GrafikEmiten.tsx` (efek seri pembanding, `bandingLegenda`) | — | Garis pembanding tak digambar di 5m–4h; legendanya berbunyi "tak berlaku di kerangka intraday" | Berkas pembanding harian, sumbu intraday epoch detik — bentuk waktunya beda dan lightweight-charts menolak mencampurnya. Terlepas dari itu, membandingkan satu tutup harian dengan 78 lilin lima menit menjawab pertanyaan yang sama sekali lain. Yang tak boleh: diam — daftar yang menyusut sendiri terbaca sebagai fitur rusak | Selesai (kode) | Added |

---

## B3 — Chip rentang kaki jadi jendela pandang, bukan potongan data (19 Agustus 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 191 | **Chip rentang berhenti mengubah ANGKA indikator** | Johan: *"kerjakan chart sama annualised nya"* (bagian chart) | `/grafik` (Grafik Emiten) | `app/src/views/dasbor/GrafikEmiten.tsx` (memo `penuh`, memo baru `awalRentang`, efek `setData`), `app/src/lib/dasbor/grafikEmiten.ts` (komentar `cariMusiman`) | Memo `penuh` memotong deret dengan `potongRentang` SEBELUM indikator dihitung — RSI/EMA/MACD/ATR menyemai bibitnya di batas rentang | Deret **PENUH** diserahkan ke `setData`; chip rentang cuma memasang `timeScale().setVisibleLogicalRange({from: awalRentang − 0,5, to: n − 0,5})` | Satu perbaikan menutup lima gejala: (1) RSI/EMA/MACD tak lagi berubah per chip, (2) MA 200 tak lenyap tanpa pesan di rentang pendek, (3) pencarian Double Bottom tak lagi menemukan pola berbeda per chip karena ATR-nya ikut bergeser, (4) kombinasi kerangka×rentang yang menyisakan satu lilin tak lagi ada, (5) penjaga `bandingLilin` tak lagi membandingkan ke angka basi. **Logical range, bukan `setVisibleRange` (waktu)** — batas bawah rentang kerap jatuh di akhir pekan/libur bursa dan tak punya lilin sendiri, sedangkan indeksnya sudah kita punya persis | Selesai (kode) — `npx tsc -b --force` **bersih** untuk berkas ini, `npx vitest run` **762 hijau** (termasuk 9 uji anti-bocor-masa-depan Bar replay dari #188 — tetap hijau, bukti potongan yang dibuang adalah yang benar). **Angka terukur (BBCA D, 2.469 lilin, tanggal 2026-08-18):** RSI 14 dari deret penuh **50,0892** vs dari deret yang dipotong 1M **42,9136** — selisih 7,18 poin pada tanggal & emiten yang sama; MA 200 **7.035,5** vs **null**; ATR 14 **153,3497** vs **140,4697**. Kolom "dipotong" itulah yang dulu tergambar saat chip 1M ditekan. **Verifikasi peramban TERHALANG LOGIN** — lihat catatan di bawah | Changed |
| 192 | Probe QA dipisah jadi **dimuat** vs **terlihat** | idem — *"pisahkan jadi dua ruas (dimuat vs terlihat), jangan biarkan satu nama menanggung dua makna"* | `/grafik` | `GrafikEmiten.tsx` (efek `setData`, `el.dataset.*`) | `dataset.jumlahLilin`/`tglPertama`/`tglAkhir` menanggung dua makna sekaligus begitu chip berhenti memotong | `jumlahLilin`/`tglPertama`/`tglAkhir` = yang **diserahkan ke `setData`** (dasar seluruh indikator); `jumlahTerlihat`/`tglTerlihatAwal`/`tglTerlihatAkhir` + `rentang` = **jendela yang dipandang** | Probe ini satu-satunya alat verifikasi untuk perbaikan #191, dan satu nama untuk dua makna persis itulah yang membuat kerusakannya tak terlihat selama ini | Selesai (kode) | Added |
| 193 | Chip rentang yang melampaui riwayat **dinonaktifkan**, dan `All` → `Semua` | idem — *"Nonaktifkan (bukan sembunyikan) chip yang melampaui riwayat kerangka aktif, dengan `title` berisi alasannya"* | `/grafik` | `GrafikEmiten.tsx` (memo `rentangOpsi`, efek pemulihan chip aktif), `grafikEmiten.ts:116,133` (`RENTANG_KAKI`, `RENTANG_KAKI_BAWAAN`) | 12 kombinasi (5m/15m/30m × 6M/1Y/5Y/All) tetap bisa ditekan dan tersorot, lalu menggambar satu bulan tanpa satu kata pun | Chip yang batas bawahnya sudah jatuh sebelum lilin pertama — di ATAS chip terkecil yang begitu — jadi `disabled` dengan `title` menyebut batas riwayatnya (pola `title` yang sama dengan tombol kerangka). **"Semua" tak pernah dimatikan**: ia jujur apa adanya, berapa pun riwayatnya | `PemilihRentang` **sudah** punya `nonaktif` + `judul` (#170 K2) — tak ada kelas atau komponen baru. Chip aktif yang mati karena kerangka berganti (mis. D → 5m selagi "1Y" aktif) dikembalikan ke bawaan; tanpa itu chip mati tetap tersorot dan tak bisa ditekan untuk keluar. Label lain **tidak** diganti ke `LABEL_RENTANG` — `grafikEmiten.ts:103-106` sudah mencatat alasannya (label panjang tak muat di kaki 412px) | Selesai (kode) — uji `RENTANG_KAKI_BAWAAN` disesuaikan ke `'Semua'`, tetap hijau | Changed |
| 194 | Baris **Annualised DIHAPUS**, diganti "Setahun (audit)" + "YTD (n kuartal)" | Johan — *"kerjakan chart sama annualised nya"* | `/stock-detail`, kartu **Kuartalan** (tab Statistik) | `KolomKuartalan.tsx:52,63,152-169,184` (`ytdKuartal`, `jumlahYtd`, dua baris tabel, pemuat laporan resmi) | Satu baris "Annualised" menjumlahkan kuartal yang KEBETULAN ada lalu menyebutnya setahun. 2026 baru Q1+Q2 → setengah tahun tercetak berlabel Annualised di **setiap** emiten; dan untuk tahun buku yang sudah tutup ia mengarang taksiran padahal angka resminya sudah ada di cakram (BBCA 2025 dicetak `43.156`, resminya `57.537` — meleset 25% karena Q3 tak dipanen) | Dua baris yang keduanya bisa diadu: **"Setahun (audit)"** = angka laporan resmi tahun buku yang sudah tutup, dan **"YTD (n kuartal)"** = jumlah Q1..Qn apa adanya, n diambil dari tahun berjalan lalu dipakai untuk SEMUA kolom, sehingga 2026 bisa disandingkan lurus dengan 2025 pada jumlah kuartal yang sama | "Annualised" tak bisa diadu dengan apa pun — bukan setahun, bukan YTD, bukan taksiran yang dinyatakan sebagai taksiran. Angka tersalin ke spreadsheet orang, catatan kakinya tidak, jadi kejujurannya harus ada di LABEL, bukan di catatan kaki. "Tahun buku sudah tutup" ditentukan dari ADA-tidaknya laporan setahun resmi untuk tahun itu, bukan dari kalender — kalau angkanya tak ada, tak ada yang perlu diklaim | Selesai (kode) — BBCA `Setahun (audit) 2025 = 57.537` sama persis dengan laporan resmi `57.537.287.000.000`; `YTD (2 kuartal)` 2026 `29.534` vs 2025 `29.016`. `tsc` bersih, `vitest` **761** hijau (patokan 754 + 7 uji baru) | Changed |
| 195 | TTM tampil **hanya** bila 4 kuartal kalender berurutan tanpa celah | idem — bagian dari perintah yang sama | `/stock-detail`, kartu **Kuartalan** | `KolomKuartalan.tsx:82` (`hitungTtm`) + baris tabel `:170` | `allQ.slice(0,4)` lalu `length === 4` — menjaga JUMLAH, bukan KETERURUTAN. Empat nilai teratas boleh merentang lima kuartal (BBCA: 2026Q2, 2026Q1, 2025Q4, **2025Q2**) dan tetap disebut TTM | Runtun dihitung mundur dari kuartal terlapor TERAKHIR; kurang dari empat yang rapat → `—` dengan `title` *"Butuh 4 kuartal berurutan, tersedia n"*. Tak ada "tampil dengan catatan" | Sapuan 633 emiten yang punya kuartalan: syarat lama meloloskan **629**, syarat baru **289** — **340 emiten (54%) selama ini memajang TTM palsu**. Bukan kasus pinggiran, jadi menutupnya tak bisa ditunda | Selesai (kode) — TLKM (2025Q3→2026Q2 rapat) tetap `17.134`; BBCA & ASII jadi `—`. Luber mendatar **0px** di 1920/1536/412 | Changed |
| 196 | Baris setahun **disembunyikan** bila laporan resminya bukan rupiah | idem — bagian dari perintah yang sama (*"pastikan tampilanmu tak meledak"*) | `/stock-detail`, kartu **Kuartalan** | `KolomKuartalan.tsx:140-143,152-160` | Tak pernah ada baris ini, jadi tak pernah ada risikonya — tapi menambahkannya polos akan menyandingkan angka dolar dengan kolom kuartal yang sudah dinormalkan ke rupiah | Kalau mata uang laporan setahunnya bukan IDR, selnya `—` dengan `title` menyebut mata uangnya. 98 dari 949 emiten kena | AADI: laporan setahun `760.180.000` USD di sebelah kuartal `2.557` (triliun rupiah) — meleset ~17.000× tanpa satu pun galat, persis jenis kerusakan yang lolos karena hasilnya tetap "sebuah angka" | Selesai (kode) — AADI baris setahun `—`, kuartal & YTD-nya tetap tampil | Added |
| 197 | Mata uang disimpan **per periode**, aritmetika lintas mata uang dilarang | Johan — *"tidak hanya CDIA saja, tapi semua emiten perlu di perhatikan"* | `/stock-detail` (panel Laporan Keuangan) + lapisan data | `panen_keuangan_idx.py` (`info_umum`, `ekstrak`, `simpan`, `lengkapi_mata_uang`, `jalankan_arsip`), `turunkan_kuartal_diskret.py:91` (`mata_uang`, `beda_mata_uang`), `fundamentalGabungan.ts:235` (`bacaKuartalIdx`), `stockDetailData.ts:220` (`StockKeuangan.mata_uang`) | Satu `currency` untuk SELURUH berkas, padahal mata uang & skala dideklarasikan per laporan. CDIA 2025: TW2 USD/Satuan Penuh, TW3 IDR/Jutaan, 2026 TW1 USD lagi | `mata_uang_laporan` (terbaca dari XLSX) + `mata_uang` (lengkap, yang tak terbaca ditaksir jangkar neraca) + `kurs_laporan`; `currency` tinggal ringkasan. Pengurangan dua periode beda mata uang → `null` berasal `beda-mata-uang` | `Q4 = audit − TW3` CDIA memberi pendapatan **−64,5 triliun** tanpa satu pun galat. Ruas "Conversion rate" kosong di CDIA, jadi mengonversi berarti memakai kurs karangan — dan hasilnya akan terlihat presisi persis seperti angka minus itu | Selesai — **6.574 dari 6.679** berkas arsip diperas ulang **nol jaringan** (105 gagal: XLSX rusak di arsip, "File is not a zip file" — periodenya tetap punya mata uang, cuma ditaksir bukan terbaca); catatan **9.665 → 9.665** (nol hilang); `mata_uang` menutup 9.665/9.665 periode (6.566 terbaca, 3.099 ditaksir); revenue diskret negatif **15 → 14** (CDIA hilang); `perbaiki_skala_keuangan --pindai` tetap **0**; lompatan neraca >50× tetap 19 tapi **10 di antaranya kini berlabel pergantian mata uang**; `tsc` bersih, vitest **33 hijau** di `fundamentalGabungan.test.ts` (29 + 4 baru), tiga swauji Python lolos | Added |

**Verifikasi peramban TERHALANG LOGIN.** `/grafik` ada di balik
`PenjagaHalaman kunci="grafik"` dan `children` sengaja **tidak di-mount** untuk
tamu — jadi kanvasnya, probe `data-*`-nya, dan legenda indikatornya tak ada di
DOM sama sekali. Profil `chrome-devtools-mcp` juga masih terkunci (PID 34120),
jadi peramban alternatif yang Johan izinkan (`claude-in-chrome` + pratinjau
bawaan) dipakai — keduanya profil bersih tanpa sesi Supabase. CLAUDE.md
melarang mengisi kolom sandi. **Yang masih perlu Johan:** login di jendela itu,
lalu tiga viewport (1920×1080×1, 1536×960×1,25, 412×915×2,625) untuk mengukur
`scrollWidth − clientWidth`, dan membaca RSI 14 di chip `1M` vs `Semua` langsung
dari legenda kanvas.

**#194-196 kena tembok yang sama.** `/stock-detail` juga di balik
`PenjagaHalaman`, jadi kartu Kuartalan tak pernah masuk DOM untuk tamu. Yang
bisa diukur tanpa sesi sudah diukur: angka barisnya lewat fungsi render yang
sama persis dengan yang dipakai layar (data nyata dari cakram, bukan contoh
karangan), dan luber mendatarnya lewat halaman uji berisi markup dan CSS yang
sama, diukur di tiga lebar — 1920, 1536, 412 — semuanya `scrollWidth −
clientWidth = 0`, tabelnya 366px di dalam wadah 366px pada 412. **Yang masih
perlu Johan:** satu tatap mata pada kartu Kuartalan sesudah login, terutama
apakah dua baris (setahun + YTD) terasa berlebih dibanding satu baris dulu.

### #198–199 — kolom pembanding & kurasi indikator (19 Agu 2026, sore)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 198 | Kolom pembanding XLSX 2025 diperah jadi **interim 2024** | Johan — *"kerjakan D1 sama D2"* | Lapisan data (`/stock-detail` membacanya) | `scripts/panen_pembanding.py`, `scripts/panen_keuangan_idx.py` | 9.665 periode diskret, 2024 hanya audit tahunan | **10.800 periode**; 827 dari 949 berkas bertambah 2024 TW1 (787) & TW2 (348) | Datanya sudah ada di `_arsip-mentah/` — nol permintaan jaringan, ±20 menit. Panen ulang ke IDX mahal dan sering 403 | **Selesai** — `b3e2e52b`. Gerbang kewajaran vs audit 2024: median Q1 **0,237** (n=665), Q2 **0,243** (n=250), 91–94% di 0,15–0,40. Gerbang yfinance **tak bisa memvonis 2024** (irisan tanggal nol) dan itu ditulis apa adanya di `docs/status-panen.md` | `docs/status-panen.md` |
| 199 | Katalog indikator dikurasi — hanya yang **terbukti bekerja** masuk menu | Johan — *"bnyk indikator yang kmu pasang tidak berfungsi mestinya pasang yang berguna saja"* | `/grafik` (menu indikator) | `app/src/lib/dasbor/katalogIndikator.ts:243`, `indikatorDibuang.ts`, `app/scripts/audit-indikator.mjs` | 457 entri pustaka masuk menu apa adanya | **365 BEKERJA** tampil; 73 `BUTUH_MASUKAN_LAIN` + 16 `RUSAK` + 3 `MATI` disaring | Indikator yang menyala tapi kosong lebih buruk daripada yang jujur tak ada. Disaring di `muatKatalog`, bukan di menu, supaya template lama gagal-anggun | **Selesai** — `1b377aae` (audit) + `50c74efe` (kurasi). `tsc` bersih; 765 uji di 40 berkas hijau. Tiap entri yang dibuang berjejak di `docs/riset/audit-indikator.tsv` | `docs/riset/audit-indikator.tsv` |
| 200 | Tab **Semua** — tabel penyaring (screener) seluruh emiten | Johan — *"backlog screener aja belum kmu kerjakan sampai detik ini, payah kmu tidak menepati janjimu"* | `/kartu?tab=semua` | `KartuAnalisa.tsx`, `kartuRingkas.ts`, `kartuRingkas.test.ts`, `useUrut.ts`, `useLayarSempit.ts` | Halaman Kartu hanya melayani satu emiten sekali lihat (dropdown) | Tabel 383 emiten, 9 kolom bisa diurut, chip saringan berkalimat kondisi, paginasi 100 (laptop) / 25 (ponsel) | Backlog C3 di `docs/ceklist-backlog.md`, tercatat sejak lama dan tak pernah dikerjakan karena berkas backlog kedua itu tak pernah dibaca | **Selesai & terverifikasi** — `1d58712b`. `tsc` bersih, **770/770 uji**. Layar: laptop 1536×960×1,25 dan telepon 412×915×2,625 — `scrollWidth − clientWidth` badan **0** di keduanya, tabel menggulung di wadahnya sendiri (807px dalam 394px). Saringan `< 1 ATR` memotong 383 → **131** dan kakinya ikut berubah; urut `%chg` turun teratas KIJA +34,96%, naik teratas TCPI −15,00%; ruas kosong tetap di bawah di kedua arah | — |
| 201 | `perbaiki_skala_keuangan.py` dari **penebak** jadi **pemeriksa** | Johan (lewat papan D1) — *"Skrip itu MENGOREKSI angka yang sudah benar: ZBRA 2019 kini 1000× dari nilai sumbernya"* | Lapisan data (`/stock-detail` panel Laporan Keuangan, `/kartu`) | `scripts/perbaiki_skala_keuangan.py`, `scripts/dasar_arsip.py` (baru), `scripts/panen_keuangan_idx.py:782` | Pembagi ditebak dari median tetangga lalu **ditimpa di tempat**; `suara=[-1000]` dengan `len(set(suara))==1` membaca SATU jangkar bicara + satu diam sebagai "sepakat" | Nilai acuan selalu diperas ulang dari `_arsip-mentah/` (6.574 periode, nol jaringan, disinggahkan di `_dasar.json`); `total_assets` memutuskan, `equity` menyanggah; arah NAIK wajib disepakati keduanya; TOLERANSI 2,0→1,5; tetangga beda mata uang dilewati | ZBRA 2019 arsipnya `total_assets` 5.577.552.029 tapi tersimpan 1000×. Jangkar aset 5,578e9/3,165e12 = 0,001762 → sisa 1,762 masih lolos toleransi 2,0 → menuduh; jangkar ekuitas 0,007654 → di atas 1/500 → **diam**. Diam terbaca sebagai setuju. Dugaan "median seluruh riwayat" tak terbukti — jangkarnya memang sudah tetangga terdekat. Sesudah ditimpa, tak ada lagi acuan: dibetulkan tangan pun dirusak lagi jalan berikutnya | Selesai `8ecabb81` — sapuan 949 berkas × 6.574 periode berarsip menemukan **tepat 3** korban (ZBRA 2019, ARGO 2019, SGER 2023), **35 nilai** dikembalikan; `--pindai` sesudah `--tulis` = **0** (idempoten); swauji **7→13 kasus**, semua lolos; lompatan neraca >50× **18→20** tapi yang **tak terjelaskan tetap 7** (ZBRA hilang, SGER ×2 berlabel mata uang nyata, ARGO muncul sebagai TEMUAN); revenue diskret negatif **16→16 dari 5.274** (daftar sama persis); `turunkan_kuartal_diskret.py` 949 ditulis ulang, 0 berubah | Added |

### #202 — gerbang kredit edisi (20 Agu 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 202 | Gerbang yang mencegah **kontributor tercantum tanpa karyanya masuk** | Johan, 20 Agu 2026 — *"kemarin kmu ada kesalahan daily kerjaan Erika gak dimasukkan di PDF Daily"*, lalu *"pasang gerbang nya dulu"*. (Kutipan ini dilengkapi agen induk sesudahnya; catatan lama di kolom ini yang menyatakan kutipannya tak diketahui sudah tak berlaku) | Kolofon PDF edisi harian (`keluaran/AP-*.pdf`) | `arus-pasar/build.py` — `arg_peta()`, `gerbang_setoran()`, `tulis_kabar_tak_terpakai()`, `halaman_kolofon()`, `main()`; uji `arus-pasar/test_gerbang.py` | Perakitan tak pernah membandingkan setoran DISETUJUI dengan emiten yang benar-benar dirakit. Kolofon menghitung kredit dari setoran yang disetujui, jadi penyetor dengan nol emiten di edisi tetap dapat angka | Build **berhenti** menyebut nama penyetor + ticker kalau ada setoran disetujui yang tak jadi emiten edisi. Jalan keluarnya `--tak-terpakai=TICKER:alasan;...` dan alasan itu **ikut tercetak**. Angka di kolofon = emiten yang benar-benar masuk; yang tak terpakai diakui lewat kalimat apresiasi bernama-ticker, tanpa angka | Alasan teknisnya sah dan tercatat — tapi di `masuk/2026-08-18/transkrip.json` ruas `_catatan`, berkas yang cuma dibaca mesin. Selama penyetor tak diberi tahu, ia mengulang filter yang sama besok. Dan selama kolofon menghitung yang disetujui, dari luar terbaca seolah karyanya dimuat | **Selesai** — `15749d9d`. `python test_gerbang.py` 6/6 lolos. Uji ujung-ke-ujung edisi 18 Agu: tanpa `--tak-terpakai` build **berhenti** menyebut "Erika Julianti : INET / TINS"; dengan alasan, kolofonnya berisi `Agitama — 8 emiten` + kalimat apresiasi bernama INET & TINS dan **nol** angka emiten kedua. `keluaran/AP-180826-E01.html` dikembalikan `git checkout` sesudah diperiksa — **tak ada edisi yang diterbitkan ulang**, `index.json` tak disentuh | — |
| 202b | Alasan "tak terpakai" **sampai ke penyetor** | Bagian C perintah yang sama | Lonceng notifikasi kontributor | Supabase migrasi `kabari_setoran_tak_terpakai_181` — fungsi `kabari_tak_terpakai()` + pemicu `setoran_kabari_dimuat` | Pemicu `setoran_kabari_kurasi` hanya menyala saat **status** berubah; mengubah `dimuat` tak membangunkan apa pun, jadi keputusan redaksi tak pernah terkirim | `AFTER UPDATE OF dimuat ... WHEN (old.dimuat AND NOT new.dimuat)` menyisipkan `notifikasi` jenis `kurasi` ke penyetornya, memakai `catatan_kurator` sebagai alasan | Tak ada tabel baru — `setoran.dimuat`, `setoran.catatan_kurator`, dan `notifikasi` sudah ada semua. Nada wajib apresiasi: pesannya membuka dengan "Terima kasih — setoran X lolos kurasi dan tetap terhitung untuk jenjangmu", alasannya di belakang | **Terpasang & teruji dengan baris uji.** Setoran uji `UJI-181/uji-gerbang.jpg` (milik `admin@papan.id`, bukan kontributor nyata) → notifikasi terbit dengan judul & pesan benar → baris uji + notifikasinya dihapus. Sesudah bersih: 89 setoran, 47 notifikasi, tier admin 2 — **sama persis** seperti sebelum uji. **Belum ada kontributor nyata yang dikabari** — menunggu izin Johan | — |

**Pemeriksaan retroaktif (edisi yang sudah terbit).** Tak ada yang
diterbitkan ulang.

| Edisi | Penyetor | Disetujui | Masuk edisi | Tak terpakai |
|---|---|---|---|---|
| AP-130826-E01 (13 Agu) | Superadmin | 22 | 22 | — |
| AP-140826-E01 (14 Agu) | **Agitama** | 6 | 5 | **AADI** (screenshot berfilter 1 bulan) |
| AP-140826-E01 (14 Agu) | Ali Supian · Bot Papan · Deonal Ramadhan · Janto · Warda W. | 17 | 17 | — |
| AP-180826-E01 (18 Agu) | Agitama | 8 | 8 | — |
| AP-180826-E01 (18 Agu) | **Erika Julianti** | 2 | **0** | **INET, TINS** (rentang tanggal bukan satu hari) |

Dua orang kena, empat setoran total. **Hanya Erika yang kolofonnya berbohong**
— kolofon 14 Agu memakai bentuk lama `kredit_kontributor` tanpa angka, jadi
Agitama tercetak "5 emiten" (jujur); AADI-nya hilang diam-diam tanpa satu pun
kabar ke penyetornya. Yang di 18 Agu tercetak "2 emiten" untuk nol karya.

### #203 — chip Bulanan Statistik Berkala dinyalakan (20 Agu 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 203 | Chip **Bulanan** di Statistik Berkala dinyalakan, ruas khas bulanan dapat layar | Johan, 20 Agu 2026 — *"kmu juga lupa yaa gak pasang datang weekly dan monthly? tidak ada UI tapi sudah di panen kan?"* | `/statistik` | `app/src/lib/dasbor/statistikBerkala.ts`, `app/src/views/dasbor/StatistikBerkala.tsx` | Chip **Bulanan** sengaja `nonaktif` sejak halaman ini dirancang — komentar `StatistikBerkala.tsx:206-217` bilang "bentuk datanya berbeda dari mingguan", dan 11 berkas `ms_2509..ms_2607` (Sep 2025–Jul 2026) tak pernah dibaca satu layar pun | Chip menyala. `top_gainers/losers(+LQ45)`, `top_saham`, `top_broker`, `index_movers` diadaptasi ke tipe mingguan lewat `bulananKeBanding()`/`bulananKePeringkat()` supaya `TabelBanding`/`TabelPeringkat` dipakai ulang. `ringkasan_pasar`, `asing`, `syariah`, `sektor`, `indeks_kinerja`, `rekap_transaksi`, `sekuritas_tercatat` dapat tabel sendiri (bentuknya sungguh beda, bukan sekadar nama ruas beda) | Data terpanen-tak-pernah-tampil — kelas masalah yang proyek ini sudah catat sendiri berulang kali (statistik mingguan, arus asing). Diukur dulu (bukan ditebak): kesebelas berkas diperiksa, bukan cuma `ms_2607` | **Selesai** — `899b3ba0`. `npm run build` bersih, **775/775 uji**. **Temuan tambahan saat verifikasi**: `top_saham.value`/`.volume` dan `top_broker.value`/`.volume` bulanan masing-masing ternyata DUA peringkat digabung dalam SATU larik tanpa penanda (`value`=Nilai bulan+YTD, `volume`=Volume bulan+Frekuensi bulan — terukur lewat `bulan_ini/persen_total` dibalik jadi total `ringkasan_pasar`, konsisten di kesebelas berkas). Merender apa adanya memajang React "duplicate key" DAN dua skala angka di bawah satu judul. `belahDua()` (baru, `statistikBerkala.ts`) memisahkannya jadi panel "Nilai (Bulan)"/"Nilai (YTD)" dan "Volume (Bulan)"/"Frekuensi (Bulan)" — diverifikasi nol duplikat kode di semua panel lewat DOM (`tbody tr td:first-child`). Layar: laptop 1536×960 dan telepon 412×915, dua tema (dark/light) — `scrollWidth−clientWidth` badan **0** di semua tab kedua jenis periode. **Catatan alat**: chrome-devtools MCP terkunci sesi lain (`chrome-devtools-mcp\chrome-profile`) — dilaporkan, tidak dimatikan; verifikasi memakai Claude_Browser (bukan Playwright), tanpa kontrol DPR eksplisit | — |

### #130 — Divergensi tiga lapis (20 Agu 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 130 | Pola **Divergensi tiga lapis** — harga + Stochastic + volume | Johan, 17 Agu 2026 — *"lebih kepada teknikal analisis dimana kamu harus bisa tentukan chart itu membentuk pola bearish divergent atau bullish divergen, kolaborasi dengan indikator stochastic, mungkin volume lebih baik dibanding umumnya."* | `/grafik` | `app/src/lib/dasbor/grafikEmiten.ts` (`cariDivergensi`, `stochUntukDivergensi`, `SPEK_POLA.divergensi`), `app/src/views/dasbor/GrafikEmiten.tsx` (`polaPerInstans`, `penandaPola`, daftar hasil, `PANDUAN_POLA`) | Nol jejak di `app/src` — terhalang Stochastic sampai 19 Agu 2026 | Pola keempat di menu Pola: dua arah (bearish/bullish), tiga derajat (kuat/sedang/lemah), penanda di seri harga + seri volume, daftar hasil berangka di bawah kanvas, lima entri panduan | Lapis 1 harga menentukan ADA-tidaknya pola (pivot yang sudah ada, bukan pencari kedua); lapis 2 Stochastic %K MENYATAKAN divergensinya; lapis 3 volume MENGESAHKAN derajat tanpa pernah membatalkan pola. %K-nya lewat `hitungInstans` jenis `stoch` supaya garis yang tergambar & angka yang dipakai pola tak bisa berselisih. Jendela volume menoleh ke belakang supaya tetap kausal (Bar replay) | **Selesai** — commit `88f7526c`; `npm run build` hijau (GrafikEmiten 262 kB, pustaka indikator tetap di potongan dinamis); `npx vitest run` **806 uji / 44 berkas** hijau, termasuk 12 uji baru + uji anti-bocor Bar replay yang membandingkan temuan deret penuh vs terpotong. Kalibrasi 916 berkas OHLC / 1,51 juta lilin: **2,83 temuan per 100 lilin** (Double Bottom yang sudah tayang: 2,43); rentang setahun median 6/emiten, p90 9, maks 16; kuat 30% · sedang 33% · lemah 37%. **Verifikasi layar BELUM** — chrome-devtools terkunci sesi lain (lihat catatan di bawah) | `CHANGELOG.md` belum disentuh |

**Verifikasi layar tertunda, bukan dilewati.** `localhost:5175` menjawab 200, tapi
chrome-devtools MCP menolak jalan: *"The browser is already running for
C:\Users\Johan\.cache\chrome-devtools-mcp\chrome-profile"*. Prosesnya dimulai
**20 Agu 2026 10:02:18** (PID 30588) — sebelum sesi ini menyentuh peramban sama
sekali, jadi ia milik sesi/worktree lain yang sedang berjalan. Sesuai aturan
global: TIDAK dimatikan diam-diam dan TIDAK diganti alat lain tanpa memberi
tahu. Menunggu keputusan Johan.

### #204 — Pemegang saham pengendali di Stock Detail (20 Agu 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 204 | Tarik informasi pemegang saham pengendali dari arsip XBRL, tampilkan di Stock Detail | *"Buktikan dulu isinya ada sebelum menulis pemanen penuh… Peras ke JSON per emiten… Tampilkan di Stock Detail. Sebut **tanggal laporan** yang jadi sumbernya; kepemilikan berubah, dan angka tanpa tanggal terbaca sebagai posisi hari ini. Emiten tanpa data: 'belum tersedia', **tidak pernah 0 atau 0%**."* (backlog B3) | `/stock-detail` (hero) | `scripts/panen_pengendali.py` (baru), `app/src/lib/dasbor/pengendali.ts` (baru), `app/src/views/dasbor/StockDetail.tsx` (hero `.sd-pengendali`), `StockDetail.css` (baru) | **Nol kata `pengendali`** di `scripts/` maupun `app/src` — fiturnya belum ada sama sekali | 949 emiten di `data-idx/json/pengendali.json`, tampil di hero sebagai `Pengendali: <kategori> · per laporan <tgl>` | Penghalangnya sudah hilang: arsip mentah XBRL (±6.679 XLSX) ada di cakram, jadi ruas ini **nol biaya jaringan** — persis alasan aturan "simpan mentah hasil panen" ditulis | **Selesai** — 949 emiten, 8 kategori (526 National Corporation, 205 Individual WNI, 92 Foreign Corporation, 53 Indonesian Government, 38 National and Foreign, 24 No Controlling Shareholder, 11 Individual Foreign). 949 berkas dibuka (satu per emiten, bukan 6.679) karena arsip ditelusuri dari periode terbaru. 16 dari 965 emiten di index fundamental tak punya data → **"belum tersedia"**, bukan 0. `npm run build` bersih, `npx vitest run` **821/821** | Added |

**Temuan yang membatasi lingkupnya — dilaporkan, bukan dipaksakan.** Ruas
`Informasi pemegang saham pengendali` (sheet `1000000`) berisi **KATEGORI, bukan
nama**. Disapu 20 Agu 2026: 120 berkas acak 2025/tw3 → 119 terisi, hanya enam
nilai berbeda. Seluruh **47 sheet** BBCA & ASII disisir mencari daftar nama
pemegang saham: tidak ada — yang muncul cuma pos neraca "piutang/utang pemegang
saham", bukan identitas. Jadi halaman boleh menyebut *"dikendalikan korporasi
nasional"* tapi **tidak boleh** menyiratkan ia tahu siapa. Nama pengendali tetap
harus dari sumber lain (KSEI, prospektus) — backlog #158/#155 belum tertutup
oleh ini.

**18 emiten kategorinya masih dari laporan 2019.** Itu sebabnya tanggal laporan
wajib ikut tampil, bukan pelengkap opsional.

### #205 — Valuasi vs sejarah: rerata historis + ambang vonis (20 Agu 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 205 | Tiap angka valuasi punya pembanding historis + vonis murah/wajar/mahal | *"Maksudnya: tiap angka valuasi punya **pembanding**, sehingga 'PER 12x' bisa dibaca 'murah/wajar/mahal **terhadap sejarahnya sendiri**', bukan cuma angka telanjang… **Ambangnya harus terukur, bukan ditebak.** Turunkan dari sebaran nyata… dan **tulis rumusnya di komentar** supaya bisa dibantah pembaca. **Emiten dengan riwayat terlalu pendek tak boleh diberi verdict.**"* (backlog A1) | `/stock-detail` (tab Statistik, panel *Valuasi vs Sejarah*) | `scripts/hitung_valuasi_historis.py` (baru), `app/src/lib/dasbor/valuasiHistoris.ts` (baru), `valuasiHistoris.test.ts` (baru), `app/src/views/dasbor/stock-detail/PanelValuasiHistoris.tsx` (baru), `StockDetail.tsx`, `StockDetail.css` | Cuma ruas mentah `pe_vs_sector_pct` (`stockDetailData.ts:210`) — **nol rerata historis, nol ambang vonis** | Deret P/E & P/B tahun buku 2019–2025 per emiten (`valuasi_historis.json`, 814 emiten). Ambang = **kuartil 25/75 dari sebaran emiten itu sendiri**; jangkar **median**, bukan rata-rata | Ambang universal ("P/E di bawah 15 itu murah") tak berarti apa-apa lintas sektor. Median dipilih karena sebaran P/E miring kanan dan satu tahun berlaba nyaris nol menarik rata-rata jauh lebih keras | **Selesai** — 15 uji baru, `npx vitest run` **821/821 di 45 berkas**, `npm run build` bersih. Sebaran vonis terukur (bukan kesan): **P/E 169 murah · 97 wajar · 61 mahal · 487 tanpa vonis**; **P/B 287 · 144 · 134 · 249**. Emiten berhak vonis: **338 (P/E)** dan **574 (P/B)** dari 814; 151 dari 965 emiten tak punya deret sama sekali → panel berbunyi "belum tersedia" | Added |

**Jebakan yang ditemukan saat mengerjakan — dan yang membuat versi pertama
salah total.** Harga di `ohlc/` **sudah disesuaikan pemecahan saham**, sedangkan
`eps` XBRL adalah angka apa adanya saat laporan terbit. Terukur: penutupan BBCA
akhir 2019 yang benar-benar diperdagangkan **33.425**, tapi `ohlc/BBCA.json`
menyimpan **6.685** (= 33.425 ÷ 5, pemecahan 1:5 Okt 2021) sementara `eps` 2019
tetap **1.159**. Membagi keduanya memberi **P/E 2019 = 5,8×** untuk emiten yang
nyatanya diperdagangkan ~29× — nol galat, nol tanda, dan dari layar terbaca
sebagai *"dulu BBCA sangat murah"*. UNVR kena hal yang sama (P/B melompat 12× →
57× antara 2019 dan 2020 semata karena pemecahan 1:5).

Karena itu **ruas per-saham tak dipakai sama sekali**. Yang dipakai jumlah
agregat dibagi jumlah saham HARI INI (`ListedShares`), basis yang sama dengan
deret harga: `P/E_Y = harga_akhir_Y × saham_kini ÷ laba_bersih_Y`. Diperiksa
ulang: BBCA 2019 = **28,6×**, UNVR 2019 = **43,5×** — cocok dengan kenyataan.

Dua jebakan yang sudah tercatat di `CLAUDE.md` **dihindari, bukan ditemui**:
(a) hanya periode **tahunan** yang dibaca dan hanya dari `keuangan_idx/` — jadi
kumulatif-vs-diskret tak pernah bertemu; (b) tak ada rasio yang diturunkan dari
rasio lain, semuanya dihitung dari jumlah mentah, jadi soal skala `der` persen
vs `der_q` rasio tak berlaku di sini. 632 periode ber-mata-uang USD **dilewati**
(bukan dikonversi — kursnya berubah tiap tahun dan menebaknya menambah galat
diam-diam).

**Catatan alat:** chrome-devtools MCP terkunci sesi/worktree lain
(`chrome-devtools-mcp\chrome-profile`, PID 30588 mulai 10:02:18) — sesuai aturan
global **tidak dimatikan** dan dilaporkan lebih dulu, tidak diganti alat
diam-diam.

**Koreksi jujur atas commit `4386d74d`.** Commit itu berjudul hanya B10 & B26,
tapi isinya **10 baris** — tiga baris milik agen lain (B8/D9, B9/D10, B17/D11,
menandai `e01b7861`/`78c92168`/`06787778` selesai) ikut terbawa karena
`git commit -- docs/antrean.md` mengambil keadaan berkas di direktori kerja,
termasuk suntingan agen lain yang belum di-*stage*. Aturan lama ("sebut
berkasnya di `git commit`, bukan cuma di `git add`") melindungi dari *index*
yang tercemar, **bukan** dari direktori kerja yang tercemar — dan pada berkas
yang dipakai bersama dua agen, itu celah yang berbeda. Isinya utuh dan benar;
yang salah cuma cakupan pesannya. Tidak di-*amend* sesuai aturan: riwayat yang
ditimpa lebih mahal daripada satu pesan kurang lengkap yang dicatat terbuka.

---

## A2 — Halaman Bedah Emiten, dua belas seksi (20 Agustus 2026)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 191 | Halaman **Bedah Emiten** 12 seksi | Tugas A2/#153, verbatim: *"halaman **Bedah Emiten** 12 section (backlog A2/#153) … Jangan mengarang daftar section sendiri kalau sudah ada"* | `/bedah-emiten` (baru) | `app/src/views/dasbor/BedahEmiten.tsx`, `BedahEmiten.css`, `app/src/lib/dasbor/bedahEmiten.ts` (+ `.test.ts`), `App.tsx` (rute lazy), `lib/dasbor/menu.ts` (`bedah-emiten`, kode `BDH`, grup Emiten), `lib/aksesHalaman.ts` (`PETA_MENU_KUNCI`) | Nol jejak sebagai halaman. Yang ada cuma `arus-pasar/build_bedah.py` + tab admin `BedahTab` — itu perakit **edisi PDF** bernama "Bedah", bukan halaman analisa | Dua belas seksi urut sesuai `workflow-fundamental.md` A2: kepala emiten + 3 vonis · aktivitas transaksi · aliran asing · lima langkah uang · lintasan laba · denyut kuartalan · kualitas laba · vonis valuasi dua sumbu · laporan keuangan + 2 tabel rasio · skor 5 pilar · panel khas PAPAN · glosarium | Daftar seksinya diambil dari rancangan yang sudah ada, tidak disusun ulang. Yang dipakai ulang bukan ditulis ulang: `PanelAliranAsing` (seksi 3), `PanelLaporanKeuangan` (seksi 9), `useStockFundamental`/`useStockAsing`, `valuasiHistoris.ts` (A1), `pengendali.ts` (B3), `GLOSARIUM` | Selesai (kode) — `npx vitest run` **864 hijau di 46 berkas** (821/45 + 27 baru + 16 milik agen lain), `npx vite build` bersih, bundel `BedahEmiten` 36,3 kB. **`npm run build` TERHALANG** oleh galat TS milik agen lain di `lib/dasbor/tanyaPapan.ts:666` (`Topik \| undefined`) — berkas itu di luar lingkup tugas ini dan tak disentuh. **Verifikasi layar TERHALANG** — chrome-devtools MCP terkunci sesi lain (profil `chrome-devtools-mcp\chrome-profile`, PID 26484 mulai 11:53:49); sesuai aturan **tidak dimatikan** dan tidak diganti alat diam-diam. `git b09255bc` | Added |
| 192 | Ruas ARUS hanya dari SATU sumber | Tugas A2: *"Menggabungkan per-ruas dengan 'yang tidak null menang' memberi angka hampir dua kali lipat tanpa satu pun galat — terukur TLKM 1,96×"* | idem | `bedahEmiten.ts` (`langkahMoneyFlow`, `deretTahun`, `deretKuartal`) | — | Pendapatan/laba/arus kas seluruhnya dari `fundamental/{KODE}.json` (`ttm_*`, `hist_*`, `q_*` — semuanya turunan yfinance yang sudah diskret). `keuangan/` dan `keuangan_idx/` **tidak pernah** bertemu di seksi mana pun kecuali seksi 9 | Satu-satunya tempat keduanya boleh bertemu adalah `PanelLaporanKeuangan`, yang memang sudah punya aturan penggabungan **dan** menandai asal tiap sel ke pembaca. Menyambungnya sendiri di seksi lain berarti menulis ulang aturan yang sudah ada, di tempat yang tak menandai asalnya | Selesai — dicatat di kepala berkas `bedahEmiten.ts` beserta angka terukurnya | — |
| 193 | Skala rasio diperiksa ke berkasnya, bukan ditebak dari nama | Tugas A2: *"`der` PERSEN, `der_q` RASIO (beda 99,4× median di 514 emiten); `roe` rasio, `dividend_yield` persen"* | idem | `bedahEmiten.ts` (`keP`, `marjin`, `derPersen`, `kualitasLaba`) | — | DER **selalu** ditampilkan persen: `der` apa adanya, `der_q` dikali 100 lebih dulu. Uji mengunci keduanya | Sapuan berkas BBCA/TLKM/ASII menemukan jebakan **kedua** yang belum tercatat di mana pun: `roe` RASIO (0,21818) sementara `hist_roe` di berkas yang **sama** sudah PERSEN (20,88) — beda 100× di dua ruas sekeluarga. Rerata ROE historis di seksi 7 dihitung tanpa mengali 100 lagi, dan itu ada ujinya | Selesai — uji *"roe rasio jadi persen, hist_roe SUDAH persen dan tidak dikali lagi"* | — |
| 194 | Vonis tak pernah diberikan pada data yang tak cukup | Tugas A2: *"Emiten berdata kurang tak boleh diberi vonis. 'Belum tersedia', tak pernah 0"* + *"Altman Z & F-Score: kalau ruasnya tak lengkap, katakan section itu tak bisa dihitung"* | idem | `bedahEmiten.ts` (`skorTotal`, `cagr`, `zonaAltman`, `bandFScore`, `panelKhas`, `ringkasTransaksi`) | — | Pilar tanpa bahan `skor: null` + kalimat `kurang`, dan **tidak dihitung 0** di skor total (yang ikut menyebut "dari berapa pilar"). CAGR `null` kalau salah satu ujung deret ≤ 0. Altman Z & F-Score berbunyi "Belum bisa dihitung". Rerata volume `null` kalau riwayat < 5 hari | Menghukum emiten yang datanya belum lengkap seolah fundamentalnya buruk adalah kesalahan yang tak pernah terlihat dari layar — angkanya wajar, cuma salah. Untuk F-Score, jebakan tambahan: berkas membawa `f_score_n` (BBCA cuma **6 dari 9** kriteria bisa dinilai), jadi bandnya dihitung dari **proporsi** — 4/6 bukan hal yang sama dengan 4/9 | Selesai — 6 uji khusus keadaan kosong, termasuk *"emiten tanpa data: semua pilar null, total null, bukan 0"* | — |
| 195 | Kunci akses `bedah-emiten`, bukan `bedah` | (turunan aturan #142 — ganti nilai, sapu pembacanya) | `/bedah-emiten` + `/admin/bedah` | `App.tsx` (`PenjagaHalaman kunci`), `lib/aksesHalaman.ts` (`PETA_MENU_KUNCI`) | Kunci `bedah` sudah dipakai tab admin perakit edisi PDF (superadmin) | Halaman publik ini memakai kunci **sendiri**, `bedah-emiten` | Memakai ulang `bedah` membuat SATU setelan menjaga dua hal yang tak berhubungan: membuka halaman analisa berarti membuka perakit edisi, dan menguncinya berarti mengunci halaman analisa untuk semua orang. Barisnya belum ada di `akses_halaman` → kunci tak dikenal fail-open, publik sampai diatur dari tab Akses tanpa menyentuh kode | Selesai — alasannya ditulis di kedua tempat pemakaiannya, bukan cuma di salah satu | — |

**Yang tidak dikerjakan, dan kenapa.** Seksi 2 memakai `asing/{KODE}.json`
alih-alih `ds_*.json` seperti tertulis di rancangan: berkas per-emiten itu
**sudah** membawa volume, nilai, dan frekuensi pasar harian (bukan cuma ruas
asing), jadi memuat berkas sepasar untuk satu emiten cuma menambah permintaan
tanpa menambah satu pun angka. Pemuatnya (`useStockAsing`) pun sudah ada.

---

## Sesi 20 Agustus 2026 — 3 backlog admin (akses induk-turunan, thumbnail, MBMA)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 196 | #173 — Akses induk-turunan (probvv/bulletin, seasonality-hari/seasonality) | "probvv ada di dalam bulletin; seasonality-hari di dalam seasonality. Sekarang keduanya diatur terpisah, jadi mengunci induk tak mengunci anaknya — setelan yang tak pernah berlaku, dan itu lubang akses, bukan sekadar janggal. ... hubungan induk–turunan dinyatakan di satu tempat, dan aturannya jelas — anak tak pernah lebih terbuka daripada induknya. Tulis aturannya di komentar." | `/bulletin`, `/seasonality`, `/admin/akses` | `akses_halaman.induk` (migrasi `akses_halaman_induk_anak`), `boleh_buka()`, `halaman_saya()`, `lib/aksesHalaman.ts`, `views/admin/AksesAdmin.tsx` | Dua baris `akses_halaman` lepas satu sama lain — mengunci `bulletin`/`seasonality` tak pernah ikut menyempitkan `probvv`/`seasonality-hari` di luar render bertingkat React (mis. lewat RPC/`akses_khusus` langsung ke anak) | Kolom `induk` (nullable, FK ke `kunci` sendiri) + `boleh_buka()` meng-AND-kan hasil baris dgn hasil induknya (rekursif) — ditegakkan di DATABASE, satu tempat, bukan didokumentasikan doang. Tab Akses menampilkan hierarkinya (indentasi + "turunan dari …") | Aturan proyek #142: ganti nilai/bentuk wajib sapuan pembaca. Sapuan `pg_proc`: cuma `halaman_saya()` (diperbarui). Sapuan `pg_policies` (qual+with_check): cuma `akses_baca_semua` (select true, tak berubah) | Selesai — `fd42aa11`. Diverifikasi transaksi rollback: `boleh_buka('probvv')` jadi `false` saat `bulletin` dikunci ke superadmin. Build bersih, vitest 852/852 | Feature/fix |
| 197 | #165 — Thumbnail dibuat saat unggah, bukan saat terlihat | "Gambar 420–520 KB dipakai di kotak 40px. Nol `toBlob`/`drawImage` di jalur unggah... Buat thumbnail saat unggah. Ukur sebelum/sesudah... Jangan hapus gambar aslinya." | `/admin` tab Unggah (Kotak Masuk) | `supabaseEdisi.ts:unggahScreenshot/urlScreenshotsThumb`, `UnggahHarian.tsx:mintaThumb`, RLS `unggah_screenshots`/`ada_alasan()` | Kotak 40px memuat berkas ASLI (420-776 KB) — penundaan lama cuma menggeser WAKTU permintaannya, bukan UKURANNYA | Companion `{tanggal}/{ticker}-{jenis}.thumb.webp` (maks 160px, WebP q0.75) dibuat via `<canvas>` browser sesudah unggahan asli sukses, fire-and-forget (kegagalannya tak menggagalkan unggahan). Asli TAK PERNAH disentuh; lightbox tetap full-res | Bandwidth kotak 40px semestinya proporsional dgn 40px, bukan resolusi kamera HP. RLS storage butuh cabang baru (pola nama lama `ELSE false`) + `ada_alasan()` diperluas (thumbnail mewarisi alasan dari path asli) | Selesai — `fd3df7c0`, migrasi `screenshots_thumb_upload_policy`. **Diukur nyata** (bukan taksiran): 12 screenshot 19 Agu (identik isi bucket), PIL resize+encode PERSIS logika `<canvas>`: 5.955,6 KB → 16,1 KB, **hemat 99,7%**. Build bersih, vitest 864/864 | Feature |
| 198 | #162 — Sebab penolakan unggah MBMA belum terbukti | "Penghalang lama (#161) sudah selesai. Cari sebabnya, buktikan dengan percobaan, jangan menyimpulkan dari ingatan. Kalau ternyata sudah tak terjadi, katakan begitu dan tutup barisnya — itu jawaban yang sah." | `/admin` tab Unggah | `setoran` policy `setoran_tulis`, migrasi `setoran_alasan_dan_kurasi` (`20260814113459`), commit klien `d9313d5e` | Diduga sebab kuota/emiten-bentrok/tanggal/format (empat kemungkinan lama) — none cocok saat diuji ulang, tak ada trace tersisa (baris gagal tak pernah tercipta, ditolak RLS `setoran_tulis` di tahap paling awal) | **DITUTUP — investigasi, bukan kode.** Bukti waktu: migrasi RLS "alasan wajib ≥20" landing 18:34:59 WIB, ~26 menit SEBELUM commit klien form-alasan 19:00:58 WIB — jendela balapan deploy. Baris MBMA bertanggal 14 Agu baru sukses tercatat 17 Agu (3 hari kemudian), cocok pola "gagal lalu disetor ulang". Diperiksa ulang 20 Agu (bukan ingatan): `ALASAN_MIN=20` klien vs `>=20` server SEKARANG selaras, 3 unggahan MBMA berikutnya (18,19 Agu) sukses bersih | Tak bisa dibuktikan mundur (baris gagal tak meninggalkan jejak apa pun — bukan cuma "dihapus", tapi tak pernah tercipta), tapi bukti waktu deploy + keselarasan klien/server saat ini cukup kuat utk menyimpulkan ini kejadian SEKALI di tanggal peluncuran fitur, bukan bug yang masih hidup | Ditutup 20 Agu — `docs/antrean.md` baris B19 dicoret dgn penjelasan lengkap. Tak ada kode diubah (tak ada yang perlu ditambal) | Closed (non-repro) |

---

## Sesi 20 Agustus 2026 — Tanya PAPAN: aspek broker, chip saran, sambungan kata ganti

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 206 | #172 — Aspek broker + chip saran lanjutan + sambungan kata ganti pada mesin Tanya PAPAN | "2. #172 — aspek broker + chip saran + sambungan kata ganti ... Aspek broker — tak ada cabang ke Broker Summary. ... Chip saran lanjutan — `interface Jawaban` tak punya ruasnya. ... Sambungan kata ganti — ... 'bagaimana valuasinya?' sesudah bertanya BBCA tak nyambung. Ubah bentuk `topik` supaya membawa kode emiten. Sapu seluruh pembacanya." | Panel Tanya PAPAN (seluruh halaman publik) | `lib/dasbor/tanyaPapan.ts` (`jawabBroker`, `RUAS_BELUM`, `RUAS_SUSULAN`, `SARAN_PASAR`, `RUAS_EMITEN`, `saranUntuk`, `jawab`), `components/dasbor/TanyaPapan.tsx`, `TanyaPapan.css` | Kata "broker" tanpa kata rangking cuma ditawari cabang tiga arah (`CABANG.broker`), tak ada jawaban angka. `Jawaban` tak punya ruas saran. `RUAS_SUSULAN` cuma cocok ruas TELANJANG ("valuasinya?"), bukan diawali kata tanya ("bagaimana valuasinya?") — susulan begini jatuh ke blok valuasi PASAR | `jawabBroker()` menjawab dari `h.broker_val` (field yang SUDAH termuat di fetch harian, sama dipakai `/broker` — tanpa fetch tahap-2); broker per emiten dijawab jujur belum tersedia (`RUAS_BELUM`). `Jawaban.saran` diisi terpusat di `jawab()`: market-wide dari `SARAN_PASAR`, per-emiten dari `RUAS_EMITEN` (ruas LAIN yang belum ditanyakan, dipetik dari daftar yang SAMA dengan `RUAS_SUSULAN`). `RUAS_SUSULAN` diberi awalan opsional `(bagaimana|gimana|berapa)?` | Cek ulang lebih dulu (bukan langsung eksekusi): `subjek` di `Topik`/`KonteksTanya`/`Jawaban` **sudah ada** sejak commit `a58a8968` (18 Agu) — jadi "topik cuma simpan JENIS" di deskripsi tugas sudah basi. Celah nyatanya sempit: RUAS_SUSULAN cuma cocok ruas telanjang, bukan versi berimbuhan kata tanya. `h.broker_val` dipilih atas fetch `bs_*.json` terpisah (disebut di tugas) karena datanya SUDAH ada di konteks harian, nol biaya jaringan tambahan | Selesai — `b938a577`. `npm run build` bersih, `npx vitest run` 869 hijau (46 berkas). Dicoba manual `localhost:5175`: desktop 1536×960 & mobile 412×915 (chrome-devtools MCP terkunci sesi lain, PID 26484 mulai 11:53:49 — **tidak dimatikan**, dialihkan ke Browser pane bawaan sesuai aturan lapor-dulu). "broker paling aktif hari ini" → 3 broker nyata dari `ds_260819.json` + link `/broker` + 2 chip saran; klik chip mengirim & menjawab benar; "harga BBCA" → "bagaimana valuasinya?" nyambung ke PER/PBV/ROE BBCA (bukan PER pasar); chip mobile tak overflow (`panelWidth 392 < viewportWidth 412`, `bodyScrollWidth === viewportWidth`). #171 (peta sinonim + salah ketik) dicek ulang dan ternyata SUDAH SELESAI sejak `a58a8968`/`a0ff1703` — `docs/antrean.md` B6/B7 ditandai selesai di `b2212e86` | Feature |

---

## Sesi 20 Agustus 2026 — SW#29 kalender, SW#36 kalkulator, #154 konteks data

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 207 | SW#29 — Kalender terlalu besar di desktop | "kalau segede gaban gini gak efektif di desktop... masak hanya lihat tanggal besar sekali", dikonfirmasi ulang di `/broker`: "ketemu lagi kalender segede ini gilaaak". Beri batas lebar yang masuk akal... **Ukur lebar selnya sebelum dan sesudah** dan laporkan angkanya | `/stocks`, `/broker`, `/sector` | `app/src/dasbor/lantai.css:1178` (`.csb-full`) | Grid `.csb-full` (area expand "Kalender penuh ▾" varian strip) dua kolom `minmax(380px,1fr) minmax(320px,1.2fr)` — kolom kiri (grid kalender) ikut melebar tanpa batas atas. Terukur nyata di 1536px (chrome, `getBoundingClientRect`): sel `.cg.ada` **119,9×46,4px**, `.cal-grid` 611,5px | Kolom kiri dikunci `minmax(380px,460px)`, kolom kanan tetap `minmax(320px,1fr)` (mengisi sisa, TIDAK dikembalikan jadi kosong). Terukur sesudah: sel **89,6×46,4px** (−25%), `.cal-grid` 460px pas, kolom kanan (`.csb-kanan`) 885,2px — tetap mengisi penuh | `.lantai > .panel:has(.cal-grid):not(:has(.cal-strip-bar))` (cap 520px) sudah ada tapi SENGAJA mengecualikan varian strip (komentar `lantai.css:1130-1143`: bar strip harus full-width). Bug sebenarnya bukan di situ — di grid `.csb-full` sendiri, yang riwayatnya (komentar `lantai.css:1173-1177`) pernah dicap 448px lalu SENGAJA dilepas ("sisa kanan kosong melompong"). Jadi dua perbaikan berbeda: kolom KANAN tetap `fr` (mempertahankan perbaikan lama), kolom KIRI dikunci (memperbaiki regresi baru) — tidak saling menghapus | Selesai — diverifikasi langsung di browser (`localhost:5175`, bukan cuma baca CSS): `/stocks` & `/broker` 1536×960 sebelum/sesudah persis angka di atas, `/sector` dikonfirmasi memakai komponen sama (`Kalender.tsx` satu sumber, otomatis ikut). Mobile 412×915: media query `max-width:760px` (`.csb-full{grid-template-columns:1fr}`) tak tersentuh perubahan, dicek tetap 1 kolom, sel 70,8px (masuk akal utk layar sempit). `npm run build` bersih, `npx vitest run` **864/864 di 46 berkas** | Fix |
| 208 | SW#36 — Kalkulator JIA terlalu lebar di monitor lebar | "Bukan bug CSS spesifik... Rekomendasi lama: batasi lebar halaman ~1400-1600px. **Periksa dulu apakah `.dasbor-main` sudah punya batas** — kalau memberi batas di sana, itu mengenai SELURUH halaman... Putuskan mana yang benar dan sebutkan alasannya" | `/kalkulator` | `lantai.css:816` (`.kalkulator-view`) | `.dasbor-main` TIDAK punya batas lebar (`flex:1;min-width:0;padding:14px`, sengaja — dipakai 11 halaman dasbor). `.kalkulator-view{width:100%}` — cap 1500px lama DIHAPUS 14 Agu karena dipasangkan `margin-inline:auto` yang menengahkan kartu dan membuka jurang kosong antara rail dan konten ("kosong kirinya", keluhan asli 14 Agu) | Diputuskan TIDAK menyentuh `.dasbor-main` (akan mengenai 11 halaman lain, di luar lingkup keluhan). `.kalkulator-view{width:100%;max-width:1600px}` — TANPA `margin-inline:auto` kali ini: rata kiri menempel rail seperti sebelumnya, kosong cuma muncul di KANAN pada monitor sangat lebar, bukan jurang di antara rail dan kartu | Prasangka sebelumnya keliru: masalah 14 Agu bukan capnya, tapi `margin-inline:auto`-nya. Ganti cap tanpa centering menyelesaikan keluhan lebar TANPA mengulang keluhan lama. 1600px dipilih (ujung atas rentang rekomendasi 1400-1600) supaya nol perubahan terlihat di viewport standar (1536px < 1600px) — cap ini murni jaring pengaman monitor ultra-lebar | Selesai — terukur di browser: 1536px tetap **1420px** lebar `.kalkulator-view` (sama persis sebelum/sesudah, x kiri tetap 102 — nol regresi terlihat), disimulasikan 2200px terpotong tepat **1600px** (x kiri tetap 102 — rata kiri terbukti, bukan cuma diklaim), 412px tetap fluid 396px. `.dasbor-main` tidak disentuh sama sekali → 11 halaman lain otomatis aman, tidak perlu sapuan tambahan | Fix |
| 209 | #154 — Peringatan konteks + tanggal metodologi di tiap halaman analitik | "Tiap halaman analitik harus menyebut kapan datanya dan dari mana metodologinya. Sekarang tak konsisten... Buat satu komponen kecil yang dipakai ulang, bukan menyalin kalimat ke tiap halaman. Sambungkan ke /metodologi. Tanggal datanya dibaca dari isi berkas, bukan dari waktu berkas ditulis... JANGAN membocorkan dapur" | Stocks/Broker/Sector/IndeksDunia/BrokerSummary/Radar/PetaInvestor/StatistikBerkala/Seasonality(+Harian+Komparasi)/KartuAnalisa | `components/dasbor/KonteksData.tsx` (baru), `lantai.css:807-816` (blok CSS `.konteks-data`/`.kd-tautan`) + satu baris impor+render di tiap halaman | Tak konsisten: StockDetail/PetaInvestor/BrokerSummary/StatistikBerkala/KartuAnalisa sudah punya label tanggal SENDIRI (format beda-beda), sisanya (Stocks/Broker/Sector/IndeksDunia/Radar/Seasonality) tak menyebut apa-apa. Tak satu pun bertaut ke `/metodologi` | Komponen `KonteksData` (props `tanggal: string\|null` ISO) render "Data per {tanggal format id-ID}" + tautan "Metodologi & sumber data →" ke `/metodologi`, dipasang penuh di 7 halaman yang belum punya label tanggal sama sekali (memakai `tanggalAktif`/`edisi.date_iso`/`publish_date` yang SUDAH ada di state masing-masing — tak ada fetch baru). 3 halaman yang SUDAH punya label sendiri (StatistikBerkala/Seasonality/KartuAnalisa) cukup ditambah tautan `.kd-tautan` polos, bukan komponen penuh (labelnya beda bentuk — edisi/"diperbarui X" — memaksa jadi ISO cuma menambah kerumitan tanpa nilai) | Kalkulator JIA **sengaja dilewati** — "murni kalkulator client-side + localStorage, TIDAK ada fetch data harian" (komentar asli `KalkulatorJia.tsx:22`), tak ada tanggal data untuk disebut. ChartIndeks **sengaja dilewati** — widget TradingView live pihak ketiga, bukan data kita. StockDetail/GrafikEmiten **di luar lingkup** (JANGAN SENTUH eksplisit). Tanggal SELALU dari state yang sudah dimuat dari isi JSON (`tanggalAktif`, `edisi.date_iso`, `investor_map.meta.json→publish_date`), tak satu pun dari `Date.now()`/mtime | Selesai — `npm run build` bersih (tsc + vite), `npx vitest run` **864/864 di 46 berkas** (nol regresi). Diverifikasi render nyata di browser (bukan cuma baca kode) untuk 6 halaman publik (Stocks/Broker/Sector/IndeksDunia/StatistikBerkala/KartuAnalisa) — teks "Data per 19 Agustus 2026" + tautan `/metodologi` muncul, DOM `getBoundingClientRect` konfirmasi tak overflow di 412px. 4 halaman (PetaInvestor/Radar/Seasonality/BrokerSummary) di balik `PenjagaHalaman` (butuh login) — **tidak diisi kolom sandi** (aturan proyek), diverifikasi lewat `tsc -b`/build saja. Pass kebocoran terpisah: grep diff untuk nama endpoint/jalur berkas internal/aturan skor — nol temuan, teks yang dirender cuma "Data per X" + "Metodologi & sumber data" | Feature |
| 210 | SW#20 — `hist_fcf`/`hist_bv` disebut masih mentah USD | *"Kelas bug yang sama dengan perbaikan 99 emiten sebelumnya; kedua ruas ini di luar cakupan perbaikan itu. **Ukur dulu berapa emiten yang terdampak**, baru betulkan."* | `/stock-detail` (Valuasi Interaktif, EPS historis), `/bedah` | `scripts/fetch_fundamental.py:508-518` (blok HISTORICAL METRICS), `scripts/lengkapi_fundamental.py` (`kurs_panen`, `hist_eps_baru`) | Tuduhannya `hist_fcf`/`hist_bv`. Terukur: keduanya SUDAH dikonversi — `hist_bv × shares` vs `hist_total_equity` = 0,9971 (USD, n=298) vs 1,0000 (IDR); `hist_revenue/hist_fcf` = 8,2 (USD) vs 9,03 (IDR). Yang mentah ternyata `hist_eps`: rasio 17.061 (USD) vs 1,0000 (IDR), dan 244 dari 405 nilainya tersimpan tepat 0,00 karena `round(ni_dolar/shares, 2)` | `hist_eps` dihitung dari net income yang SUDAH rupiah (akar, di `fetch`), plus penambal 967 berkas lama: jalur 1 `hist_net_income/shares`, jalur 2 EPS tahunan XBRL dikonversi memakai `mata_uang[<tanggal>]` lalu dibulatkan SESUDAHNYA. Kursnya diperas ulang dari cakram (`keuangan/` mentah vs `fundamental/` rupiah) = **17.876**, sebaran nol di 346 pasangan | Ruas per-saham yang salah 17.876× bersanding dengan `hist_bv`/`hist_fcf` yang benar, tanpa satu pun galat — dan yang 0,00 terbaca seperti "emitennya memang tak untung" | Selesai `9d7a556a` — USD: nilai 405→417, nol 244→**0**, rasio 17.061→**1,0000**; IDR: 3.439→3.641, nol 15→7. 259 berkas ditulis ulang. Swauji +4 kasus LOLOS | Added |
| 211 | D6 — 98 emiten pelapor USD tak punya baris "Setahun (audit)" | *"`fd.hist_*` punya angkanya dan sudah dikonversi, tapi bersumber **yfinance** — menyebutnya 'audit' akan jadi kebohongan. Cari jalan yang jujur... **Jangan memberi label audit pada angka yang bukan audit.**"* | `/stock-detail` (panel Kuartalan) | `app/src/views/dasbor/stock-detail/KolomKuartalan.tsx` (`nilaiSetahun`, `labelSetahun`) | Baris kosong seluruhnya untuk 106 emiten yang punya tahun buku berlaporan dolar — disembunyikan supaya tak meleset ~17.876× dari kolom kuartal yang sudah rupiah | Label mengikuti SUMBER, bukan sebaliknya: auditan menang kalau laporan tahun itu rupiah (dibaca per periode, bukan `currency` berkas), kalau tidak jatuh ke `hist_*` berlencana `Y`. "Setahun (audit)" hanya kalau SEMUA sel memang auditan; "Setahun (agregator)" kalau semuanya agregator; "Setahun" saja kalau campuran | Menyembunyikan barisnya memang benar — yang salah berhenti di situ. Tapi mengisinya di bawah label "audit" menukar angka agregator jadi laporan resmi tanpa satu pun tanda | Selesai `6c5d5dee` — 106 emiten bertahun-buku non-rupiah, barisnya kini terisi di 90 (NI), 90 (Rev), 104 (EPS); **457 sel** yang dulu "—" kini berangka, semuanya bertanda `Y`. BBCA tak berubah sedikit pun. 6 uji baru, build sukses, vitest 869 hijau | Added |
| 212 | D7 — 3.099 dari 9.665 mata uang masih TAKSIRAN | *"**Probe murah dulu** — jangan langsung panen besar... hitung biayanya (jumlah permintaan, perkiraan waktu) dan **laporkan sebelum menjalankan panen penuh**."* | Lapisan data (`/stock-detail` panel Laporan Keuangan, `/kartu`) | `scripts/panen_keuangan_idx.py` (`periksa_ketersediaan`, flag `--periksa-ketersediaan`) | Mata uang 3.099 periode ditaksir bukan dibaca; dugaannya mentah 2020/2021/2025-audit & 2026-TW2 tak lagi tersedia | Probe 4 permintaan DAFTAR (nol unduhan XLSX): **keempatnya MASIH DISAJIKAN** — 701 · 747 · 882 · 783 berkas ber-XLSX. Probe kedua mengunduh 12 berkas: 12/12 sukses, 0,3-0,7 dtk/berkas, **12/12 taksiran COCOK** dengan mata uang yang tertulis di berkasnya | Aturan 18 Agu #1: panen 2016-2019 pernah dikirim tanpa memeriksa apakah tahunnya ada, lalu 2018 menjawab `ResultCount 0` sesudah satu siklus agen habis. Sekarang langkahnya satu perintah dan daftar periodenya dibaca dari data, jadi tak bisa basi | Probe selesai `9f6c468c`. **Panen penuh BELUM dijalankan** — ongkos terukur 3.113 unduhan, ~1,4 GB arsip, ~31 menit, wajib IP rumahan. Menunggu keputusan Johan | Added |
| 213 | D8 — sembilan lompatan neraca belum terjelaskan | *"Empat wajar (ekuitas melintasi nol), sisanya diduga cacat di sumber IDX sendiri. Periksa satu per satu... Yang cacat sumber: tandai di data supaya pembacanya tahu, jangan diam-diam dibiarkan."* | `/stock-detail` (panel Laporan Keuangan) + lapisan data | `scripts/sapu_regresi_keuangan.py` (baru), `scripts/cacat_sumber.py` (baru), `scripts/turunkan_kuartal_diskret.py`, `PanelLaporanKeuangan.tsx`, `stockDetailData.ts` (`StockKeuangan.cacat`) | Sapuannya ad-hoc tiap sesi — angka yang sama tercatat 15, 18, 19, dan 20 di tiga catatan karena definisinya ikut bergeser. Dengan definisi terkunci: 45 lompatan, **11 tak terjelaskan** | Diperiksa satu per satu: **5 NYATA** (PANI injeksi aset 2022, ZBRA 2021, MKNT arus investasi = arus pendanaan Rp 822,925 mrd, BEEF ekuitas terkikis), **2 artefak pembagi** (vonis baru `ekuitas-dekat-nol`: MPPA −596 juta di atas aset Rp 3,2 T = 0,019%), **4 CACAT** di 2 emiten (LAPD 2021/2022, ARGO 2020/2021) — ditandai ruas `cacat`, angkanya sengaja tak diubah | Keempat yang cacat lolos SETIAP gerbang mekanis: perbaikan skala diam karena arsipnya tak ada, dan identitas `aset == liabilitas + ekuitas` tetap lolos **0,0%** karena ekuitas memang pos penyeimbang. Menebak nilai benarnya lalu menuliskannya persis kesalahan yang membuat ZBRA 2019 rusak permanen | Selesai `263bca8c` — 45 lompatan: 6 ditandai-cacat · 26 ganti mata uang · 5 ekuitas melintasi nol · 2 ekuitas dekat nol · 5 nyata-terperiksa · 1 sesuai berkas resmi · **0 tak terjelaskan**. Regresi: revenue diskret negatif 16/5.274 tetap, `--pindai` = 0, build sukses, vitest 869 hijau | Added |

---

## Sesi 20 Agustus 2026 — B28 Yahoo ^JKSE cadangan IHSG

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 214 | B28 — Yahoo `^JKSE` sebagai sumber CADANGAN IHSG | "Ukur dulu kecocokannya, jangan langsung dipakai... Cadangan, bukan pengganti... Asalnya wajib terlihat di layar. Pakai kosakata penanda asal yang sudah ada (lencanaAsal)... WAJIB period1/period2" | Kalender Bursa (`/stocks`,`/broker`,`/sector`,`/indeks`) + header IHSG seluruh halaman dasbor | `scripts/panen_ihsg.py` (`isi_cadangan_index`), `.github/workflows/update.yml` (step 5b), `app/src/lib/dasbor/dataHarian.ts` (`TanggalIndex.sumber`/`DataHarian.sumber`), `app/src/components/dasbor/Kalender.tsx` (badge `Y`) | `ds_*.json`/`index.json` cuma terisi kalau PDF resmi IDX berhasil diunduh — begitu IDX menolak IP runner GitHub (terjadi 19 Agu), harinya kosong total dan Kalender berbunyi "Tanpa data" sampai dipanen manual | Ukur dulu: 142/144 hari yang ada di IDX & Yahoo cocok, selisih terbesar 0,005 poin (galat pembulatan Yahoo, bukan beda sumber), median 0,00003%. `isi_cadangan_index()` menambal `index.json` + menulis `ds_<stem>.json` MINIMAL (cuma ruas IHSG — value/change/pct/prev/high/low) untuk hari bursa 14 hari terakhir yang `ds_*.json` ASLI-nya belum ada, ditandai `sumber:"yahoo"`. Kalender menampilkan superscript **Y** + tooltip pada sel bertanda itu (kosakata sama `PanelLaporanKeuangan`: Y = Yahoo Finance) | Window 14 hari WAJIB — tanpa batas, skrip akan "menambal" **8.707 hari** yang memang sengaja tak pernah dipanen PDF-nya (index.json baru mulai 2026-01-07). `update_index()`/`save_json()` DIPAKAI ULANG dari `parse_idx_pdf.py` (bukan ditulis ulang) — begitu PDF asli berhasil diparse, fungsi yang sama MENIMPA entri bertanda `sumber:"yahoo"` dengan data resmi tanpa ruas itu, tanda cadangan hilang sendiri | Selesai — `npm run build` bersih, `npx vitest run` **869/869 hijau (46 berkas)**, swauji `panen_ihsg.py --swauji` lolos (uji tambal + uji "berkas asli tak boleh ditimpa"). Dijalankan nyata dari IP rumahan: 2026-08-20 (hari ini, PDF IDX belum terbit saat dites) berhasil ditambal dari Yahoo, `ds_260820.json` + `index.json` terisi `sumber:"yahoo"`. Diverifikasi lewat `read_page` (chrome-devtools/Browser pane MCP dipakai sesi lain sehingga screenshot pixel tak bisa diambil — verifikasi lewat pohon aksesibilitas): tombol sel 2026-08-20 membawa `title="Cadangan Yahoo Finance — IDX belum merilis statistik resmi hari ini"`, sel 18/19 Agu (data IDX asli) tidak. `update.yml` step "Parse PDF"/"Cadangan IHSG"/"Commit" diberi `if: always()` supaya tetap jalan & commit walau step unduh PDF gagal (run tetap MERAH — sinyal blokir dipertahankan, cuma datanya tak lagi macet) | Feature |

**Yang tidak dikerjakan.** Tidak menambahkan cadangan untuk PitaKurs/header selain lewat `ds_<stem>.json` (sudah otomatis ikut karena `useDataHarian()` fetch by stem, dicoba nyata: header beranda menampilkan 6.498,60 dari cadangan tanpa error). Tidak mengubah `.dasbor-main`/komponen lain di luar Kalender & dataHarian — field lain (`world`/`sectors`/`vol_today` dst.) sengaja kosong di hari cadangan, bukan ditaksir dari Yahoo (Yahoo cuma punya OHLCV indeks, bukan statistik pasar penuh).

---

## Sesi 20 Agustus 2026 — C2 sisa: Wyckoff Phase (Grup 3) & Harmonic Pattern (Grup 4)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 215 | C2 Grup 3 — Wyckoff Phase | "Wyckoff Phase & Harmonic ... di SPLE ada itu" · "**Wyckoff**: sebaran 6 fase di seluruh emiten. Kalau satu fase memakan >60% atau ada fase yang nyaris tak pernah muncul, ambangnya salah — dan itu harus ketahuan sekarang, bukan setelah tayang" · "Aliran asing (`FNet`) ... **Satuannya LEMBAR, bukan rupiah** ... jangan menuliskannya sebagai rupiah di layar" | `/grafik` (menu Pola, kanvas, daftar hasil di bawah kanvas) | `lib/dasbor/grafikEmiten.ts` (`faseWyckoffDi`, `cariWyckoff`, `URUT_FASE`, `SPEK_POLA.wyckoff`), `views/dasbor/GrafikEmiten.tsx` (fetch `asing/<KODE>.json`, `NAMA_FASE`/`ARTI_FASE`/`WARNA_FASE`, penanda, daftar) | Tak ada. Spek `docs/spek-indikator.md` Grup 3 menyebut enam fase (Akumulasi → Markup Awal → Markup → Konsolidasi → Markdown Awal → Markdown), belum pernah dihitung | Fase diturunkan dari **perkalian dua sumbu** (struktur MA: MA20 di atas/di bawah MA50 × posisi harga: di atas keduanya / di antara / di bawah keduanya) = tepat 6, saling eksklusif dan menutup semua kemungkinan. **Pita tengah diputuskan FNet** persis seperti SPLE: net asing 5 lilin positif → Akumulasi, negatif → Konsolidasi; tanpa catatan asing jatuh ke struktur MA dan ruas `fnetDipakai` menyebutkan yang mana. Keluarannya SEGMEN (runtun fase sama), bukan label per lilin. Volume dilaporkan sebagai RVOL segmen, **tidak** ikut menentukan fase | Panduan SPLE mendaftar enam kondisi yang saling tumpang tindih dan dua di antaranya bernama sama dalam dua bahasa ("Markdown Early" / "Markdown Awal") — daftar berurutan semacam itu membuat jawabannya bergantung urutan `if`, hal yang tak terlihat dari layar. Dua sumbu menghapus ambiguitas itu tanpa membuang FNet. Volume tak dijadikan syarat karena syarat semacam itu diam-diam menurunkan Markup jadi Markup Awal hanya karena ruas volume beberapa hari kebetulan sepi (alasan yang sama dengan `volumeMenguat` Double Bottom) | Selesai — **kalibrasi 963 berkas `ohlc/`, 1.460.195 lilin ber-MA lengkap**: Akumulasi 12,8% · Markup Awal 8,4% · Markup 23,8% · Konsolidasi 10,2% · Markdown Awal 9,1% · Markdown 35,8%. Tak satu pun >60%, yang paling jarang 8,4% — keenamnya terpakai. Pita tengah 335.569 lilin, **54,2% dilabeli FNet**, sisanya cadangan struktur MA (915/963 emiten punya berkas `asing/`, dan berkas itu mulai 2020 sementara OHLC mulai 2016). Segmen rata-rata 9,2 lilin. `npm run build` bersih, `npx vitest run` **890/890 di 46 berkas** (869 → +21), `npm run lint` nol error. Uji anti-bocor-masa-depan baru: segmen yang sudah tertutup di dalam potongan **identik ruas demi ruas** dengan deret penuh. **Verifikasi layar TIDAK BISA** — chrome-devtools terkunci sesi lain (PID 3940, profil `chrome-devtools-mcp\chrome-profile`, mulai 13:42); tidak pindah alat diam-diam | Feature |
| 216 | C2 Grup 4 — Harmonic Pattern | "**Butterfly pakai standar Carney: AD/XA 1,27–1,618**" · "**BC/AB wajib divalidasi 0,382–0,886 LEBIH DULU**, sebelum rasio pola dicek" · "Pivot: **zigzag ayunan minimal 3%**" · "berapa pola per 100 lilin, dan berapa yang tersaring oleh validasi BC/AB" | `/grafik` (menu Pola, kanvas, daftar hasil di bawah kanvas) | `lib/dasbor/grafikEmiten.ts` (`zigzagPivot`, `RASIO_HARMONIK`, `cariHarmonik`, `SPEK_POLA.harmonik`), `views/dasbor/GrafikEmiten.tsx` (`NAMA_HARMONIK`/`WARNA_HARMONIK`, penanda X-A-B-C-D, daftar) | Tak ada. SPLE punya 4 pola tapi ruasnya selalu `null` sampai mereka perbaiki 9 Jul 2026 — jadi tak ada implementasi yang layak dipercaya untuk ditiru | Zigzag dari `cariPivotTinggi`/`cariPivotRendah` yang SUDAH ada (bukan pencari pivot ketiga), dipaksa berselang-seling + ayun min 3%. Lima titik berturutan = kandidat XABCD. **Gerbang BC/AB 0,382–0,886 diperiksa LEBIH DULU**, baru AB/XA & AD/XA dicocokkan ke pita nominal ± toleransi 0,05. **Butterfly AD/XA `[1.27, 1.618]` (Carney)**. Jendela deteksi = seluruh rentang tergambar, bukan 108 hari | Gerbang BC/AB sekaligus menjamin C tak melewati A, jadi tak perlu syarat geometri terpisah. Pita nominal ditulis sebagai rentang (nilai titik = rentang lebar nol) supaya ada SATU aturan pencocokan, bukan satu untuk nilai titik dan satu untuk rentang. 108 hari itu batas riwayat SPLE, bukan aturan pola — kita punya 10 tahun | Selesai — **kalibrasi 963 berkas, 1.505.030 lilin**: corong 131.764 titik zigzag → 128.104 jendela XABCD → 2.827 rasio cocok → **1.090 lolos gerbang = 0,07 pola per 100 lilin**, di 597/963 emiten. Jauh di bawah Divergensi 2,83 & Double Bottom 2,43 karena bentuk syaratnya berbeda (4 rasio atas 5 pivot vs 2 kondisi atas 2 pivot); hanya 2,2% jendela yang rasionya cocok pola mana pun — sejalan pengakuan SPLE sendiri "pola harmonic memang formasi langka". **Bukti koreksi 2**: gerbang membuang **1.737 dari 2.827 (61,4%)**, sebanding bug mereka (34→17); BC/AB terbesar yang benar-benar muncul di papan IDX **486,8×** dengan 56.273 kaki ber-BC/AB > 1. **Bukti koreksi 1**: Butterfly Carney 446 pola, dengan ambang Screener SPLE (≤1,42) tinggal 291 — **155 Butterfly sah hilang**. Kepekaan: toleransi 0,02→290 · 0,03→481 · 0,05→1.090 · 0,08→2.155 · 0,12→4.228 pola; ayunMin nyaris tak berpengaruh (2%→1.106 · 3%→1.090 · 5%→1.050 · 8%→858). `npm run build` bersih, vitest **890/890**, lint nol error. Uji anti-bocor: pola di dalam potongan identik ruas demi ruas dengan deret penuh. **Verifikasi layar TIDAK BISA** — alasan sama dengan #215 | Feature |

**Yang tidak dikerjakan.** Nol lapisan gambar baru — keduanya lewat jalur penanda/daftar pola yang sudah ada (`penandaPola`, `.grf-pola-daftar`), jadi **nol baris CSS baru** dan `lantai.css` tak disentuh. `CD/BC` dihitung dan ditampilkan tapi tidak dipakai menyaring (spek tak menyebutnya sebagai syarat). Berkas `asing/<KODE>.json` ditarik **hanya** kalau ada instans Wyckoff hidup — bukan ikut fetch OHLC — supaya pengunjung yang tak membuka pola ini tak menanggung satu unduhan tambahan per emiten. Pada kerangka pekanan/bulanan/intraday kunci tanggal FNet memang tak cocok dan seluruh pita tengah jatuh ke cadangan struktur MA; itu benar apa adanya karena net asing tercatat harian, dan layar menyebutkannya per segmen.

---

## Sesi 20 Agustus 2026 — B16 backup screenshot + klarifikasi "2 jenis orderbook"

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 217 | B16 — Backup harian folder screenshot bucket Supabase | "Isinya screenshot setoran kontributor — tidak reproducible kalau hilang... Mekanismenya belum diputuskan, jadi putuskan yang paling sederhana yang benar-benar jalan, kerjakan, dan sebutkan alasannya" | — (infrastruktur, bukan halaman publik) | `scripts/backup_screenshot.py` (baru) | Tak ada backup sama sekali — cuma ada di bucket Supabase `screenshots` | Skrip Python idempoten: baca manifest lokal `_arsip-mentah/screenshots/manifest.json` (89 baris `setoran.path`, diambil via Supabase MCP `execute_sql` — anon key TIDAK bisa baca tabel `setoran`, RLS `penyetor=auth.uid() OR superadmin`), unduh tiap path dari bucket **publik** (`storage.buckets.public=true` — GET langsung jalan tanpa signed URL, dibuktikan nyata) ke `_arsip-mentah/screenshots/berkas/<path>`, lewati yang sudah ada | Sempat dicoba RPC `SECURITY DEFINER` sempit (cuma expose path/tanggal/ticker/jenis) supaya Task Scheduler bisa perbarui manifest TANPA sesi Claude Code — **diblokir classifier izin otomatis** (ubah skema + grant ke `anon` butuh persetujuan eksplisit Johan). SQL lengkapnya dicatat di docstring skrip, menunggu keputusan | **Backup pertama SELESAI & nyata**: 89/89 berkas, **23,2 MB, 48,5 detik**. Jalan kedua (idempoten, dicoba nyata): 0 baru diunduh, 0,0 detik. `--swauji` lolos tanpa jaringan (berkas yang sudah ada tak memicu `requests.get`). Tidak menghapus apa pun di Supabase maupun lokal. **Otomatisasi penuh (Task Scheduler tanpa Claude Code) BELUM bisa** sampai Johan menyetujui migrasi RPC di atas — sampai itu, manifest diperbarui manual lewat Supabase MCP sebelum tiap jalan | Added |
| 218 | Klarifikasi "2 jenis orderbook" di folder data emiten lokal (RENCANA-REFACTOR-REACT.md §9 / antrean B16) | "ada pertanyaan lama yang belum terjawab... kalau kamu menemukan buktinya di cakram, laporkan; kalau tidak, katakan tak ketemu — jangan mengarang jawabannya" | — | `data emiten/CUAN/*.png`, `data emiten/DSSA/*.png` (di luar repo, root proyek) | Path folder "belum dikonfirmasi" sejak dicatat | **Foldernya KETEMU** — `data emiten/CUAN` & `data emiten/DSSA`, 10 screenshot per ticker, penamaan default Windows (`Screenshot <tanggal> <jam>.png`), bukan `<TICKER>-<jenis>.jpg` seperti bucket Supabase | Dibaca 4 dari 10 berkas (CUAN, tanggal berbeda-beda: 13 Agu, 06 Agu, 10 Agu, rentang 13Mei-13Agu) — **keempatnya layar "Broker Summary" yang SAMA PERSIS**, cuma beda tanggal/rentang tanggal di kolom kalender. Tak ada satu pun dari empat sampel yang memperlihatan tata letak KEDUA | **Klaim "2 jenis orderbook" TAK TERBUKTI** dari sampel yang diperiksa — dilaporkan jujur sebagai "tak ketemu buktinya", bukan ditebak/dikarang. Kemungkinan penjelasannya: istilah "2 jenis" merujuk ke sesuatu yang tak tercermin di 4 sampel (mis. 6 berkas sisanya berbeda, atau dua HALAMAN app sumber — Broker Summary vs Top Broker — yang kebetulan sampelnya tak kena) | Ditutup sebagai "tak ketemu buktinya (dari sampel)" — bukan "terbukti tak ada". Tak ada kode diubah | — |

## Sesi 20 Agustus 2026 — A2 rail meluber + A4 sisa #170 (K4/K6/K7)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 219 | A2 — rel navigasi rail meluber | "Dua jalan yang sudah dirumuskan: 1. `overflow-y:auto` pada rel ... 2. Kelompokkan submenu ... Kerjakan keduanya, dengan urutan itu ... Pastikan penjepitan itu masih benar sesudah perubahanmu" | Rail kiri desktop, semua halaman dasbor | `app/src/dasbor/lantai.css:191` (`.dasbor-rail-list{overflow-y:auto}`), `app/src/components/dasbor/Sidebar.tsx` (`RailGrup`, clamp `useLayoutEffect` baris 185-196), `app/src/lib/dasbor/menu.ts` (`MENU_GRUP`/`MENU_KELOMPOK`) | `docs/antrean.md` A2 dan `docs/ceklist-backlog.md` #175 mencatat dua jalan itu "belum dikerjakan sesi ini" (update 17 Agu) | Dicoba ulang ke kode nyata (aturan proyek: baris "belum" wajib dicoba ulang, bukan disalin dari dokumen) — **KEDUANYA sudah ada**, dari commit lama `b41fa755` ("rail dilonggarkan") yang mendahului dokumen di atas: rail sudah 5 kelompok (Pasar/Emiten/Aliran Dana/Analisa/Baca) + flyout, bukan 16 ikon satuan, DAN `.dasbor-rail-list` sudah `overflow-y:auto`. Tak ada baris kode yang saya ubah | Kedua jalan yang diminta memang sudah jadi sebelum sesi ini mulai; dokumen antrean/ceklist-nya yang basi, bukan kodenya | Diuji nyata di `localhost:5175` (dev server sudah jalan, dipakai bersama) lewat Browser pane devtools MCP, viewport **1536×700** (layar pendek, kasus yang jadi alasan #175): `.dasbor-rail-list` `clientHeight`=`scrollHeight`=**523px** — tak overflow, headroom 177px sebelum `overflow-y:auto` perlu aktif. Klik grup "Baca" (paling bawah, paling rawan clamp): flyout `top:371 bottom:600`, tak melewati `window.innerHeight:700` — penjepitan `Math.min(kotak.top, innerHeight - tinggi - 8)` di `Sidebar.tsx` masih benar sesudah flyout diperbesar D11. Diuji ulang di 1536×960, sama tak overflow. Docs diperbarui: `docs/antrean.md` A2 dicoret+ditandai selesai, `docs/ceklist-backlog.md` #175 diberi catatan penutup | Docs |
| 220 | A4 — #170 sisa: K4/K6/K7 | "Ketiganya dulu ditahan karena menunggu keputusan Johan. Johan 20 Agustus: 'kerjakan sampai tuntas'. Kalau salah satunya ternyata masih butuh keputusan yang tak bisa diturunkan dari spek, kerjakan dua lainnya dan katakan yang mana serta kenapa — jangan menebak selera" | Grup Konglomerat (Peta Investor), Broker Summary, Radar Watchlist | `app/src/components/dasbor/GrupKonglomerat.tsx`, `docs/spek-kendali.md` "Yang TIDAK dikerjakan tanpa keputusan Johan" | K4/K6/K7 tercatat "menunggu keputusan Johan" sejak 18 Agu | Dicoba ulang ke kode nyata — **ketiganya masih genuinely terhambat**, tak satu pun bisa dikerjakan tanpa menebak: K4 `GrupKonglomerat.tsx` **belum punya kendali rentang sama sekali** (fiturnya belum dibangun, bukan cuma kendalinya belum seragam) dan spek eksplisit melarang menebak artinya (rentang menghitung perubahan kepemilikan atau kinerja anggota grup — dua jawaban = dua halaman berbeda). K6 "kartu broksum juga perlu di update" tak menyebut apa yang salah dari kartunya; preset tanggalnya sudah seragam lewat K2/K5, sisanya tak bisa ditunjuk tanpa keterangan lebih. K7 Radar terhambat SUMBER DATA (masih di Gmail, belum masuk sistem) — bukan bentuk kendali, di luar cakupan tugas navigasi/kendali ini sama sekali, bukan soal keputusan selera | Mengerjakan salah satunya berarti menebak jawaban yang eksplisit didelegasikan ke Johan (K4) atau menebak maksud kalimat yang tak menyebut apa yang rusak (K6), atau membangun jalur data di luar lingkup halaman/komponen yang jadi milik tugas ini (K7) | **Tidak dikerjakan, sesuai instruksi** — dilaporkan blocked dengan alasannya masing-masing, bukan ditebak. Tak ada kode diubah. `docs/antrean.md` A4 diberi catatan "dicoba ulang 20 Agu, masih terhambat" | — |

## Sesi 20 Agustus 2026 — A3: dua sistem Arus Pasar paralel dipangkas jadi satu

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 221 | A3 — dua jalur Arus Pasar di satu repo: petakan, putuskan, kerjakan | "Repo punya **dua jalur** untuk hal yang sama, dan itu sudah berbiaya nyata ... Keputusannya masuk akal saat itu; yang tak pernah terjadi adalah **memutuskan mana yang menang sesudahnya**" · "**Putuskan dan kerjakan**, jangan cuma mengusulkan" · "**Apa pun pilihannya, jangan menghapus data**" · "**Jangan menerbitkan apa pun**" | `/bulletin`, `/admin/terbitan`, `/admin/edisi/:kode` (dibuang) | `app/src/lib/supabaseEdisi.ts` → `app/src/lib/supabaseSetoran.ts` (fungsi edisi dibuang, header batas ditambah), `app/src/views/EdisiView.tsx` (dihapus), `app/src/App.tsx:51,190` (lazy + rute dibuang), `app/src/views/EdisiUjicoba.tsx` (catatan rencana lama dibatalkan), migrasi Supabase `a3_pensiunkan_tabel_edisi` | Dua jalur: (1) berkas `arus-pasar/build*.py` → `keluaran/*.pdf` + `index.json`, dibaca `/bulletin` & Rak Terbitan; (2) tabel Supabase `edisi` lewat `supabaseEdisi.ts`, dibaca `/admin/edisi/:kode`. Batas kepemilikan tak pernah ditulis — 20 Agu satu agen menjalankan `git checkout -- arus-pasar/keluaran/` dan menghapus keluaran agen lain | **Satu jalur: BERKAS.** Tabel `edisi` di-DROP, `EdisiRow`/`daftarEdisi`/`ambilEdisi`/`simpanEdisi` dibuang, rute `/admin/edisi/:kode` dibuang, modul di-*rename* jadi `supabaseSetoran.ts` (isinya memang cuma setoran + bucket screenshots) | Diputuskan dari BUKTI, bukan selera: tabel `edisi` **0 baris** sejak dibuat; **nol penulis** di seluruh repo (`simpanEdisi` nol pemanggil, `build*.py` tak menyentuh Supabase sama sekali); satu-satunya pembaca (`/admin/edisi/:kode`) karena itu **selalu** menjawab "Edisi tidak ditemukan", dan tak ada satu pun tautan ke rute itu. Rak Terbitan sudah pindah ke `index.json` lebih dulu. Bonus keamanan: policy `admin_full_access` (`ALL to authenticated, using(true) with check(true)`) memberi hak tulis/hapus penuh ke SETIAP akun login termasuk kontributor Pemula — itu persis alasan agen Bulletin dulu menolak memakainya (`BACKLOG-SWEEP-VISUAL.md` #37); ia ikut jatuh bersama tabelnya. Nama berkas `supabaseEdisi.ts` yang tak lagi memuat satu pun fungsi edisi adalah sisa ambiguitas terakhir, jadi ikut diganti | **Selesai.** `npm run build` dari `app/` **hijau** (bukan `npx tsc --noEmit` — konfigurasinya beda). `npx vitest run`: **890 uji / 46 berkas**, tepat patokan. **Nol baris terhapus**: `select count(*) from public.edisi` = 0 diperiksa tepat sebelum DROP, dan bentuk DDL aslinya (+ peringatan jangan memakai ulang policy longgarnya) disimpan sebagai komentar di dalam migrasi supaya keputusannya bisa dibalik. Tabel diverifikasi hilang sesudahnya (`pg_class` = 0 baris). `keluaran/index.json` dan `edisi/_tahan.json` **tidak disentuh** — tak ada `generate_index.py` dijalankan, tak ada yang diterbitkan | Removed |


## Sesi 20 Agustus 2026 — Banding 5 emiten + ekspor gambar (Bedah Emiten)

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 222 | Banding sampai LIMA emiten di Bedah Emiten | "kerjakan compare 5 emiten dan export nya gambar saja" · "ambil kemampuannya, jangan rupanya" (soal SPLE, yang dinilai *"AI slop banget"*) · "Pilih ruas yang benar-benar berguna dibandingkan ... Lima kolom penuh 12 seksi akan jadi dinding angka yang tak terbaca" | `/bedah-emiten` — seksi baru **12 · Banding Emiten** (Glosarium bergeser ke 13) | `app/src/lib/dasbor/bandingEmiten.ts` (baru), `app/src/views/dasbor/BedahEmiten.tsx` (`SeksiBanding`, `SEKSI[11]`), `app/src/views/dasbor/BedahEmiten.css` (blok `.bdh-cmp-*`), `app/src/lib/dasbor/stockDetailData.ts` (`fetchAsing` diekspor; `useStockAsing` memakainya) | Halaman cuma bisa satu emiten. Pembanding SPLE cuma **dua** emiten | **Lima emiten jadi KOLOM, ruas jadi BARIS.** 21 ruas dalam 5 kelompok: Harga & Ukuran (harga, perubahan 52 minggu, kapitalisasi, nilai transaksi harian, volume vs rerata 20H) · Vonis Valuasi (P/E & P/BV sekarang + vonis vs sejarah sendiri + posisi vs median sektor) · Kualitas Laba (ROE, marjin bersih, DER, kas operasi ÷ laba, imbal hasil dividen) · Aliran Asing (net 20 hari, LEMBAR) · Skor & Pilar (skor total + kelima pilar). Pilihan keenam **ditolak dengan kalimat di layar**. Emiten yang sedang dibedah selalu kolom pertama | Saringan ruasnya satu pertanyaan: *apakah artinya berubah ketika disandingkan?* Yang berupa DERET (lintasan laba, denyut kuartalan, laporan keuangan) sengaja **tidak** ikut — disandingkan sebagai satu angka, deret kehilangan justru bagian yang membuatnya berarti. Emiten jadi kolom **bukan sekadar tata letak**: tabel yang emitennya jadi baris punya kepala kolom "ROE"/"Skor" yang mengundang satu ketukan untuk mengurutkan "emiten terbaik", padahal tak satu pun ruas di sini peringkat kualitas — larangan yang sama sudah berlaku di Screener (`kartuRingkas.ts`). Dengan emiten sebagai kolom, "urutkan" tak punya arti sama sekali. **Nol rumus baru**: semuanya meminjam `bedahEmiten.ts` + `valuasiHistoris.ts` apa adanya | Selesai — `npm run build` dari `app/` **hijau** (bukan `npx tsc --noEmit`). `npx vitest run` **912 uji / 47 berkas** (patokan 890/46 → +22). Ruas kosong `—` diuji eksplisit, termasuk marjin bank yang dilaporkan tepat 0. **Verifikasi layar TIDAK DIKERJAKAN** — koordinator membatalkannya (chrome-devtools satu profil, tak bisa paralel); dua ukuran & dua tema ditutup pemanggil | Feature |
| 223 | Ekspor tabel banding jadi satu berkas gambar | "export nya gambar saja" (tanpa PDF/Print/Share) · "Saya condong ke canvas ... kalau kamu mengukur dan menemukan alasan kuat sebaliknya, ambil jalan itu" · "Logika penyusun gambarnya dipisah jadi fungsi murni ... Tulis uji untuk bagian itu memakai konteks canvas tiruan" | `/bedah-emiten` seksi 12, tombol kamera di bilah atas | `app/src/lib/dasbor/bandingEmiten.ts` (`gambarBanding`, `ukuranBanding`, `namaBerkasBanding`, `PALET`, `unduhBandingPng`) | Repo **nol pustaka ekspor gambar** (`html2canvas`/`dom-to-image`/`html-to-image` tak ada satu pun) | **Kanvas, nol dependensi baru.** `gambarBanding(ctx, tabel, palet)` murni — menulis ke `CanvasRenderingContext2D` yang disuntikkan, tak menyentuh `document`/`window`. Pemicu unduh `unduhBandingPng()` terpisah, 12 baris, satu-satunya yang butuh peramban. Palet TETAP disalin dari token `.lantai` (bukan `getComputedStyle`), font stack SISTEM (bukan webfont halaman), gambar mengikuti tema yang sedang tampil | Yang menentukan bukan ukuran diff melainkan **bentuk kegagalannya**: pustaka tangkap-layar gagal SENYAP — webfont belum termuat diganti font lain, variabel CSS & `color-mix()` diabaikan — dan hasilnya tetap PNG yang terlihat jadi. Gambar yang beredar di grup tak punya kesempatan kedua diperiksa. Preseden kanvas → `toDataURL` → `<a download>` sudah ada di Grafik Emiten. Tanggal diambil dari ISI data (hari bursa terakhir di berkas transaksi + `updated` fundamental), **bukan** `Date.now()` | Selesai — 22 uji memakai **konteks kanvas tiruan** yang mencatat tiap `fillText`/`fillRect` + warna + font: tiap emiten tergambar di x berbeda, ruas kosong tergambar `—` dan nol pola `0`/`0,00%`/`Rp 0`, penanda PAPAN + tanggal + disclaimer tergambar, warna naik/turun ikut palet yang diberikan, teks panjang dipotong elipsis, latar penuh seukuran kanvas, dan **tiap perintah gambar jatuh di dalam batas kanvas** (pengganti "sudah dilihat sendiri"). **Pass kebocoran terpisah**: regex atas seluruh teks yang digambar menolak `data-idx`/`.json`/`fundamental/`/`lib/`/`.ts`/`endpoint`/`supabase`/`http` — nol temuan. Sebagai pemeriksaan tambahan, perintah gambarnya sempat diputar ulang jadi PNG 948×942 di luar peramban (Pillow) dan diperiksa mata di kedua tema; berkas dump & skripnya dibuang sesudahnya | Feature |

**Yang tidak dikerjakan.** Tanpa PDF, tanpa Print, tanpa Share — sesuai perintah. `lantai.css` **tidak disentuh** sama sekali; seluruh CSS baru di `BedahEmiten.css` dan hanya menyimpan penempatan & lebar (kotak cari `.af-cari`, tombol `.chip-t`, tombol ikon `.ti`/`.ti-grup`, tabel `.tbl` semuanya kanonis #170). Tabel **tidak bisa diurutkan**, disengaja. Baris pil emiten terpisah dibatalkan di tengah jalan — tombol lepas dipindah ke kepala kolomnya sendiri, satu tempat yang menyebut emiten itu, sekaligus menghapus kebutuhan kelas chip khas halaman yang akan mendefinisikan ulang bentuk kendali.


## Sesi 20 Agustus 2026 — Otomatisasi panen harian

| # | Tugas | Asal perintah | Halaman | Komponen (`file:baris`) | Sebelumnya | Jadi | Alasan | Status & bukti | Changelog |
|---|---|---|---|---|---|---|---|---|---|
| 224 | Rantai panen harian penuh di runner rumahan | "gmn ya jika semua data-data ini bisa otomatis gitu ?" — dilontarkan setelah tiga tangkapan layar berturut-turut: Kartu Analisa MBMA ("kirim agent data kok update ya ?"), Broker Summary ("ini juga kok gak update ya ?"), Seasonality ("kok gak update juga ya ?") | Semua yang membaca OHLC: Kartu Analisa, Screener, Watchlist, Bedah Emiten, Grafik, Seasonality | `.github/workflows/panen-harian-rumah.yml` (baru), `scripts/cek_kesegaran.py` (baru) | **Delapan pemanen tak masuk alur mana pun** — `panen_ohlc`, `panen_asing`, `sinkron_emiten`, `fetch_broker_summary`, `panen_seasonality`, `riset/kartu_analisa`, `hitung_valuasi_historis`, `panen_keuangan_idx`. Hanya jalan kalau ada yang mengetiknya | **Enam jadi satu rantai**, cron `30 10 * * 1-5` (17:30 WIB), `runs-on: self-hosted`, `shell: powershell`. Urutan mengikat: `sinkron_emiten` → `panen_ohlc` → `panen_asing` → `kartu_analisa --semua --tulis` → `hitung_valuasi_historis` → `panen_seasonality`, lalu gerbang kesegaran → commit → `git clean` | Gejalanya bukan bug melainkan **penjadwalan yang tak pernah ada**: IHSG sudah 20 Agustus karena `panen_ihsg` memang terjadwal, sementara turunannya masih 19 Agustus karena tak satu pun terjadwal. Urutannya mengikat karena tiap langkah **membaca hasil langkah sebelumnya** — menjalankan turunan lebih dulu menghasilkan angka kemarin yang terlihat segar. Runner rumahan, bukan awan: IDX menolak IP datacenter GitHub (`update.yml` merah 13, 14, 17, 18, 19, 20 Agu). **Dua sengaja tak ikut**: `panen_keuangan_idx` kuartalan dan sekali jalan bisa ribuan permintaan; `fetch_broker_summary` menunggu setoran kontributor + kurasi admin, jadi ia menunggu manusia bukan jam | Selesai — YAML sah (`yaml.safe_load`, 11 langkah). `python scripts/cek_kesegaran.py --uji` LOLOS; jalan nyata atas data hari ini mencetak `OHLC : 2026-08-19 pada 766 emiten` / `KARTU : 2026-08-19 pada 382 emiten` / `kesegaran turunan: LOLOS`. Commit `4948ed4d`. **Belum di-push** — menunggu perintah | Feature |
| 225 | Gerbang yang memeriksa ISI, bukan status langkah | Turunan dari #224 | — | `scripts/cek_kesegaran.py` | Tiap langkah panen memakai `continue-on-error` supaya satu pemanen mati tak menghentikan yang lain — harganya, kegagalan jadi tak terlihat | **Aturan: turunan tak boleh lebih tua daripada sumbernya.** Modus tanggal `ohlc/*.json` (`akhir`) dibanding modus `tgl` di `kartu/ringkas.json`; kartu lebih tua = keluar 1 | Persis yang lolos hari itu: `kartu_analisa.py --semua` **tanpa** `--tulis` menghitung 383 emiten, mencetak `[383/383]`, keluar dengan kode **0**, dan tak menyimpan apa pun — dari log, jalan yang tak menyimpan terlihat persis seperti jalan yang berhasil. Dipakai **modus**, bukan maksimum (satu emiten berlilin baru tak boleh membuat seluruh panen terlihat segar) dan bukan minimum (40 emiten yang berhenti diperdagangkan membeku di tanggal lama selamanya, dan itu bukan kegagalan panen). Pemeriksanya skrip tersendiri, bukan here-string PowerShell di dalam YAML — `'@` wajib di kolom 0 dan itu menabrak indentasi YAML, berkas ini sempat ditolak parser karenanya | Selesai — `--uji` swauji nol berkas dibaca, keempat asersinya menutup modus-vs-maksimum, modus-vs-minimum, dan aturan bandingnya. Commit `4948ed4d` | Feature |
| 226 | Sembilan temuan agen penyanggah atas struktur pasar & panel grafik | "kerjakan semua kalau perlu diskusi antar agent yaa" · "jangan sekali kali main tebak2an karena kita sudah punya data lengkap" | /grafik (panel, menu klik kanan), backtest struktur pasar | `app/src/views/dasbor/GrafikEmiten.tsx` (`panePerInstans`, `pastikanPane`, `paneTerlipat`, efek volume & pola, menu konteks, `salinTeks`), `app/src/lib/dasbor/strukturPasar.ts` (`cariPatahan`, `hitungPrz`, `LEBAR_PRZ_MAKS`), `app/scripts/backtest-struktur.ts` (menggantikan `.mjs`), `app/src/lib/dasbor/strukturPasar.test.ts` | Tiga tempat menomori panel sendiri-sendiri; `lipat` menyimpan INDEKS panel; menu klik kanan memanggil `coordinateToPrice` dengan koordinat seluruh bungkus; `navigator.clipboard?.writeText(...)` gagal senyap; `cariPatahan` memakai swing sebelum konfirmasinya ada; `hitungPrz` tanpa batas lebar; skrip backtest MENYALIN rumusnya dan tak pernah membaca `--tf=4h` | Satu rencana panel dipakai semua penggambar + `pastikanPane()` sebelum tiap `addSeries`; `lipat` berkunci **id instans**; butir "Salin harga" cuma ditawarkan di dalam panel harga; `salinTeks()` bercadang `execCommand` lalu `prompt`; Escape/gulir/ubah-ukuran menutup menu; gerbang `urut[p].i + n <= i`; PRZ menolak zona >40% lebar, `tengah<=0`, `bawah<=0`; backtest **mengimpor** `strukturPasar.ts` (Node 24 melucuti tipe sendiri) dan `--tf=4h` benar-benar mengambil 60m Yahoo lalu merakitnya lewat `rakitBar`/`kunci4Jam` yang sama dengan layar, mentahnya diarsipkan ke `_arsip-mentah/yahoo-60m/` | lightweight-charts MENJEPIT `addSeries(..., i)` ke `panes.length`, jadi meminta pane yang belum lahir diam-diam menaruh seri di pane orang lain — itu sebab volume mendarat di panel RSI dan panel %K menindih volume. Nomor panel bergeser tiap panel lahir/mati, jadi status lipat yang berkunci nomor membuat panel membuka sendiri, lahir terlipat, atau — kasus terburuk — panel pola terkunci jadi bilah 18% yang tak punya tombol buka. Backtest yang menguji SALINAN rumus mengukur kode yang tidak dikirim: terukur, angka yang membenarkan fitur ini dilebihkan hampir tiga kali lipat | **Selesai** — `npm run build` hijau, `npx vitest run` **1111 uji / 56 berkas**. Angka jujur (18 emiten, N=5): harian BOS **+5,2pp** (5 lilin) · +4,6pp (10) · +3,5pp (20); **4 jam** BOS +1,1pp · +2,1pp · **+0,4pp** — dicetak berdampingan justru karena lemah. Diverifikasi di layar (`localhost:5175`, laptop 1536×960×1,25 dan ponsel 412×915×2,625): RSI dulu lalu volume ke panel sendiri memberi **empat panel terpisah** (harga · volume · RSI · %K), nol galat konsol; klik kanan di panel harga memberi "Salin harga 5.375", di panel volume butir itu **hilang** (dulu "Salin harga 0"); Escape menutup menu; RSI yang dilipat **tetap terlipat** sesudah volume kembali ke panel harga dan nomornya bergeser 2→1 | Fix |
| 227 | Zig Zag & Volume Delta akhirnya bisa tampil (B30 sisa) | "lanjut" (menuntaskan antrean) — turunan Johan 20 Agu: "kamu bisa buat seperti ini ?" atas tangkapan layar daftar indikator TradingView | /grafik — menu Indikator | `app/src/lib/dasbor/katalogIndikator.ts` (`ID_PIVOT`, `ID_LILIN`, `SegmenMentah`, `LilinMentah`, `hitungSegmen`, `hitungLilin`), `app/src/lib/dasbor/grafikEmiten.ts` (`pivotPustaka`, `lilinPustaka`, `hitungSegmenInstans`, `hitungLilinInstans`), `app/src/views/dasbor/GrafikEmiten.tsx` (`segmenIndikator`, `lilinIndikator` + penggambarnya) | Keduanya DITOLAK masuk katalog: bentuk keluarannya bukan deret angka, dan kanvas belum punya jalur untuk segmen garis maupun deret lilin | Dua jalur bentuk baru. Zig Zag: 145 segmen bersambung dirangkai jadi SATU deret titik berlubang di panel harga. Volume Delta: seri candlestick di panel sendiri, warna dari tema kita | Bentuknya DIBUKTIKAN dulu lewat `app/scripts/periksa-bentuk-marker.mjs` atas OHLC BBCA nyata, bukan ditebak dari nama — `zigzag.lines` memberi `{time1,price1,time2,price2}` yang ujungnya bertumpuk (waktu kembar ditolak lightweight-charts, jadi disaring), `volume-delta.plotCandles.delta` memberi lilin berskala volume. Dirangkai jadi satu seri, bukan 145: benar rupanya, mahal ongkosnya | **Selesai** — `npm run build` hijau, **1135 uji / 57 berkas**. Uji katalog diperluas: tiap entri tanpa deret WAJIB punya tepat satu penggambar (penanda/segmen/lilin) — entri tanpa keduanya akan masuk menu lalu tak menggambar apa pun tanpa galat | Feature |
| 228 | Diary Pasar — kalender IHSG berwarna + tally + performa | "coba baca folder data ide" (dua tangkapan layar), lalu memilih "IDX Diary dulu (lebih murah)" | Beranda `/`, panel baru tepat sesudah Market Breadth | `app/src/lib/dasbor/diaryPasar.ts` (baru), `app/src/lib/dasbor/diaryPasar.test.ts` (21 uji), `app/src/lib/dasbor/__fixtures__/ihsg-250-hari.json`, `app/src/components/dasbor/PanelDiary.tsx`, `app/src/views/dasbor/Beranda.css` (blok `.dia-*`), `app/src/views/dasbor/Beranda.tsx` | Kalender Bursa yang ada tak mewarnai arah IHSG; tally hari naik/turun dan bar performa multi-periode belum ada sama sekali | Kalender bulanan lima kolom (Senin-Jumat) berwarna arah IHSG + tally 30 hari + tujuh bar performa (1D/5D/1M/3M/6M/YTD/1Y), bisa mundur sampai batas data | **Jendelanya 30 hari KALENDER, dan itu hasil pengukuran bukan selera**: panel RTI acuan menyebut sisi turun "12 days −527,254"; jendela 30 hari BURSA memberi angka yang sama sekali lain, 30 hari KALENDER memberi **12 hari, −527,250** — cocok sampai desimal ketiga. Sisi naik beda satu hari karena panel itu diambil saat bursa 21 Agustus masih berjalan sementara data kita berhenti 20 Agustus; selisih poinnya ~34, persis satu hari perdagangan. Nol unduhan baru: `ihsg_ohlc_ringkas.json` (13 KB) sudah ditarik halaman ini | **Selesai** — `npm run build` hijau, **1135 uji / 57 berkas**. Satu bug ditemukan SAAT DIUJI DI LAYAR, bukan saat ditulis: tanggal sel dihitung ulang dari (baris, kolom) dan meleset di bulan yang tanggal 1-nya akhir pekan — Agustus 2026 mulai Sabtu, seluruh pekan 3-7 Agustus dikosongkan tanpa satu pun galat (terbaca "12 hari libur" di bulan yang cuma punya satu). Ditutup di akar: tanggalnya kini IKUT di dalam kotak. Diverifikasi laptop 1536×960×1,25 & ponsel 412×915×2,625, kedua tema, nol galat konsol, nol gulir mendatar; mundur bulan sampai Agustus 2025 lalu tombolnya nonaktif sendiri | Feature |
| 229 | Mesin pola klasik — 9 pola reversal, garis digambar otomatis, backtest 3 kerangka | "bisa kan kmu drawing di chart langsung hanya dengan memilih pola tertentu ?" · "bukan asal tebak berdasarkan hasil benchmark dan backtesting data di 4H dan 1D bahkan kalau bisa 1W juga" | /grafik — menu + Pola, entri "Pola Klasik" | `app/src/lib/dasbor/polaKlasik.ts` (baru, mesin + dok backtest), `app/src/lib/dasbor/polaKlasik.test.ts` (11 uji), `app/scripts/backtest-pola-klasik.ts` (baru), `app/src/lib/dasbor/grafikEmiten.ts` (JenisPola+SPEK_POLA), `app/src/views/dasbor/GrafikEmiten.tsx` (memo klasik, penggambar garis, penanda, daftar temuan, panduan) | Chart cuma punya 6 pola; gambar 20 pola klasik Johan belum tercakup | SATU mesin di atas pivot zigzag: Double/Triple Top-Bottom, H&S (+inverted), Rising/Falling Wedge, Expanding Triangle. Pola SELESAI saat leher/garis tren ditembus CLOSE; kanvas menggambar kerangka pivot (titik-titik) + leher/garis tren (tebal) berwarna arah | Bebas bocor masa depan (patahan dicari mulai pivotTerakhir.i + jendela — pelajaran terukur cariPatahan), toleransi ATR bukan persen, pivot pola 5-titik diklaim supaya Double tak lahir dari pivot Triple. Angka backtest DIUKUR lalu ditulis (bukan dikarang; placeholder sempat tertulis lebih dulu dan diganti hasil ukur): harian +4,2pp @20 lilin (n=474), 4H +4,3pp @5 (n=347), 1W +7,5pp @5 (n=101); H&S +18,2pp & Double Bottom +11,8pp terkuat di harian; Rising Wedge NEGATIF di harian dan positif di 4H — dicetak di panduan halaman, tak disembunyikan | Kode selesai & terverifikasi layar (BBCA 1Y: wedge, leher DT, expanding tergambar; ponsel & laptop; nol galat konsol). 1146 uji hijau saat itu. **Commit menyusul satu paket dengan pola continuation** (#230) karena berkasnya sedang dilanjutkan agen | Feature |
| 230 | Pola continuation (7) — didelegasikan ke Sonnet dengan spek tertulis | "ini masukkan di pola chart fable kmu delegasikan ke model lain asal spec jelas dan detail" | /grafik — entri Pola Klasik yang sama | polaKlasik.ts + test + SPEK_POLA + paragraf statistik (oleh agen) | Baru 9 dari 16 pola gambar acuan | Flag/Pennant (dengan syarat TIANG terukur ATR), Ascending/Descending/Symmetrical Triangle — definisi mekanis per pola ditulis lengkap di prompt, agen wajib menjalankan ketiga backtest dan menyalin angka TERUKUR ke dokumentasi | Tiering model (aturan global): spek sudah tajam = Sonnet, bukan model sesi. **Masih berjalan** saat baris ini ditulis | Delegasi |
| 231 | Riset spek Auto Chart Patterns TradingView dari Chrome Johan | "fabel kmu kan bisa buka chart trading view di remote chrome coba kmu pelajari spec detail nya" | — (riset) | `docs/riset/spek-pola-tradingview.md` (baru) | Spek pola kita disusun dari buku teks, belum dibandingkan ke acuan yang Johan pakai | Tiga artikel Help Center dibaca langsung (All Chart Patterns, Double Top 43000653211, Triangle 43000653217) | Empat temuan yang MEMVALIDASI keputusan kita (pivot 5/5, patahan pakai close, leher datar DT) dan lima yang jadi bahan gelombang berikut: TARGET harga (tinggi pola diproyeksikan), EMPAT STATUS (Awaiting/Reached/Failed), gate integritas close, Trend Height, resolusi pola bertumpuk | Selesai — dokumen ter-commit; penerapan target+status menunggu agen #230 selesai supaya tak saling timpa | Riset |
| 232 | Target harga & status pola (spek TradingView) + continuation mendarat | "lanjutkan sampai agen selesai lalu terapkan target+status nya" · "secara default terbuka tanggal terakhir gini yaa" | /grafik — entri Pola Klasik; Beranda — Diary Pasar | `app/src/lib/dasbor/polaKlasik.ts` (ruas `target`/`batal`/`status`/`iStatus`, pasca-proses SATU tempat), `polaKlasik.test.ts` (18 uji), `app/src/views/dasbor/GrafikEmiten.tsx` (garis target putus-putus + status di penanda/daftar/panduan), `PanelDiary.tsx` (detail default tanggal terakhir) | Pola berhenti di "patah" — tanpa target dan tanpa nasib; agen Sonnet baru menyetor 7 continuation (1154 uji) | Target = tinggi pola (rentang pivot) diproyeksikan dari nilai garis yang ditembus; pembatal = ekstrem pola sisi berlawanan; status tercapai/gagal/menunggu dari lilin yang sudah lewat, garis target berhenti di lilin keputusan — persis konvensi TradingView | Pasca-proses satu tempat, bukan di 16 titik push yang pasti tercecer satu. Target dinilai dari EKSTREM, pembatal dari PENUTUPAN — asimetri yang sama dengan patahannya. Status TIDAK dipakai backtest (label masa lalu, bukan ramalan) | **Selesai** — commit `6a6a64ea` (paket 16 pola + target/status + delegasi agen). 1158 uji / 58 berkas, build hijau. Layar: BBCA 1Y target Rising Wedge 4.735,71 kuning menunggu; Falling Wedge gagal pudar (2026-06-17); Double Top tercapai (2025-12-11); ponsel nol gulir mendatar, nol galat konsol | Feature |
| 233 | Legenda pola salah + koreksi klaim backtest ke sapuan penuh | "ya perbaiki legendanya" · "tidak hanya di BBCA saja kan ? di semua emiten sudah di backtest ?" | /grafik | `GrafikEmiten.tsx` (legenda), `polaKlasik.ts` (dok), `app/scripts/sapu-pola-klasik.ts` (baru) | Legenda menulis "tak ada" saat 40 pola tergambar (`klasik.length` tak dijumlah); klaim backtest dari 18 emiten pilihan | Legenda "40 pola · 1 menunggu target"; sapuan 915 emiten / 22.046 pola MEMBALIK tanda: harian −2,5pp @20 (dulu +4,2pp), H&S −4,5pp (dulu +18pp). Dua pola tetap positif di kedua sapuan: Double Bottom & Ascending Triangle | Sampel yang seluruhnya likuid bukan sampel pasar; klaim di panduan & paragraf statistik diganti angka sapuan penuh TERMASUK yang negatif | Selesai — commit `654e...`/`2a6ba281` paketnya; 1172 uji | Fix |
| 234 | 16 pola jadi menu terpisah + XABCD/PRZ tergambar + label kanvas | "di pisah-pisah saja jgn jadi satu biar tidak berat" · "coba web search terkait pola PRZ atau Harmonic pattern coba perbaiki supaya muncul drawing nya" · "Higher High jadi HH... munculkan kode" | /grafik — menu + Pola | `grafikEmiten.ts` (NamaPolaKlasik+LABEL pindah rumah, 16 jenis pk-*, `jenisKlasik`/`namaPolaDariJenis`), `polaKlasik.ts` (ekspor ulang), `GrafikEmiten.tsx` (dispatch, gambar XABCD+PRZ via `hitungPrz`, `labelKanvas` di marker) | Menu satu entri gabungan; harmonic cuma titik tanpa garis; swing cuma lingkaran polos | 16 entri menu (mesin tetap satu); XABCD tergambar utuh + PRZ dua garis putus-putus amber dari C sampai D+10; HH/HL/LH/LL, BOS/CHoCH, huruf XABCD, dan singkatan pola (DT/IHS) tercetak di kanvas lewat ruas `text` marker | Label pindah ke grafikEmiten menghindari siklus modul ber-TDZ; `doubleBottom` lama ikut disembunyikan dari menu (dua entri "Double Bottom" kembar); data grafik dikonfirmasi 10 tahun penuh — dugaan "cuma 1 tahun" keliru | Selesai — commit `2a6ba281`, 1172 uji, terverifikasi ANTM 5Y | Feature |
| 235 | Tiga delegasi paralel: toolbar gambar · screener pola · audit sistem | "kirim delegasi kalau perlu" · "munculkan juga icon-icon dari titik 3 itu... hidden toolbar... magnet kuat dan magnet lemah... line gak ada setup modal warna ketebalan" · "itu juga bisa masuk di screener" | /grafik toolbar, /screener | (oleh agen — AlatGambar/IkonMenu/GrafikEmiten toolbar; pola-screener.ts+Screener; audit read-only) | — | Tiga agen Sonnet berspek tertulis; snapConfig pustaka sudah kupastikan ada sebelum delegasi (aturan: periksa asumsi murah dulu) | Tiering model: spek tajam = Sonnet; audit dilaporkan balik untuk kuputuskan | **Berjalan** saat baris ini ditulis | Delegasi |
| 236 | Empat agen setor + perbaikan audit + bilah saring + dot tooltip | "ok kerjakan semua itu sampai selesai lalu push live" | /screener, /grafik, /watchlist | lihat commit `2583bf7a` (audit), `~p237` (screener), `~p239` (toolbar/indikator/tooltip) | — | Agen screener: kolom Pola + 561 emiten berpola aktif; agen toolbar: ikon flyout + sliver + magnet 3 keadaan + modal gaya (deviasi Dropdown.tsx beralasan, diverifikasi); agen audit: 20 temuan → 8 ditutup (#1 tanggal campuran asing/ohlc 128 emiten, #8 grid template hilang, #15 watchlist crash-proof, dll); agen backtest lama: Lonjakan Volume NEGATIF ketiga horizon (n=203k), Harmonik satu-satunya positif +5,8pp; kurasi indikator: 27 Populer TradingView, audit 368 entri katalog 0 rusak | Verifikasi dot tooltip menuntut mouse NYATA: klik/hover sintetis tak menembus crosshair pustaka; jalur yang berhasil = elemen sasaran pointer-events:none + hover CDP. Playwright sempat dicoba dan DIHENTIKAN karena halaman perlu login (sandi haram diisi) — dicatat transparan | **Selesai** — 1193 uji / 60 berkas, build hijau, dua ukuran layar. Lanjut push | Feature |
| 237 | Gambar melewati lilin terakhir + harmonik dua-segitiga + PRZ di sumbu | "seperti nya ada batasan gambar nih, saya seret fibo melebihi candle gak bisa, harus di perbaiki ni" · "harmonic pattern nya kurang siip ini, coba cek lagi web search lagi" | /grafik | GrafikEmiten.tsx (ekor whitespace + setData), useAlatGambar.ts | Klik/tarik di kanan lilin terakhir ditolak (`param.time` null); harmonik hanya zigzag XABCD | Ekor whitespace 60 bar sesudah lilin terakhir (harian lompat akhir pekan, intraday ikut jarak bar) — area itu kini punya waktu; tali busur X→B & B→D putus-putus (dua segitiga konvensi TV), PRZ lineWidth 2 + nilai tercetak di sumbu harga | Cara resmi lightweight-charts memberi ruang kanan; riset web: XAB+BCD dua segitiga adalah tanda kenal harmonik | SELESAI — commit 27d5362a; bukti: jangkar fibo tersimpan `2026-09-09` (lilin terakhir 2026-08-21), screenshot desktop+mobile; tsc bersih, 1196 tes hijau, build sukses | feat(grafik) |
| 238 | Panen Lagi 21 Agu + turunan screener disegarkan | "kirim agent lain untuk panen" | semua halaman data | data-idx/json (1695 berkas) | Data terakhir 20 Agu; screener/pola_screener tertinggal di 20 Agu setelah agen selesai | ds_260821 + OHLC/asing 962 emiten + aliran_investor n=1596; bangun-screener.mjs & pola-screener.ts dijalankan ulang → keduanya 2026-08-21 (pola aktif 183) | Agen panen berhenti sebelum turunan; aturan "agen jangan menunggu proses latar" — pemanggil yang menuntaskan | SELESAI — commit 1e0b2557 | data(panen) |
