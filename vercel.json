{
  "buildCommand": "cd app && npm install && npm run build && mkdir -p dist/data-idx dist/arus-pasar && cp -r ../data-idx/json dist/data-idx/json && cp -r ../data-idx/radar dist/data-idx/radar && cp -r ../arus-pasar/keluaran dist/arus-pasar/keluaran",
  "outputDirectory": "app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
